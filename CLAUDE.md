# اصطلاحات العمل على Lynomia Business Hub

> توجيهاتٌ دائمة لأي جلسة عمل على هذا المستودع. اقرأها أولاً.

## 🔢 رقم النسخة — قاعدةٌ إلزامية

**مع أي تحديث يُدفَع، يُرفع رقم النسخة.** لا يُدفع تغييرٌ وظيفيّ دون رفع النسخة.

آليّة الرفع في كل دفعة:

1. **`VERSION`** — ارفع الرقم (semver): **minor** لميزة أو تحسين (2.81.0 → 2.82.0)، **patch** لإصلاحٍ صغير (2.82.0 → 2.82.1).
2. **`README.md`** — السطر الأول `# Lynomia Business Hub — vX.Y.Z` يطابق `VERSION`، وأضِف مدخلاً في **سجل الإصدارات** يصف التغيير.
3. النسخة تُقرأ حيّاً من `config('hub.version')` (الذي يقرأ ملف `VERSION`) وتُعرَض في ذيل الشريط الجانبي — فلا خطوة عرضٍ يدوية، يكفي رفع `VERSION`.

مثال مصغّر لدفعة:

```bash
printf '2.83.0' > VERSION
# حرّر README.md: السطر الأول + مدخل سجل الإصدارات
git add -A && git commit -m "vX.Y.Z وصف التغيير"
git push
```

**الحاجز الآلي:** خطّاف `.githooks/pre-push` **يرفض** أي دفعةٍ فيها commits جديدة دون
تغيير `VERSION` — فالقاعدة مفروضةٌ لا متروكةٌ للذاكرة. يُفعَّل ذاتيّاً عبر
`composer install` (سكربت `post-autoload-dump`). ولتفعيله يدويّاً في نسخةٍ جديدة:

```bash
git config core.hooksPath .githooks
```

للتجاوز عمداً في حالةٍ نادرة (دفعة وثائق بحتة مثلاً): `git push --no-verify`.

## ✅ اصطلاحات أخرى مُتَّبعة في هذا المستودع

- **الحزمة خضراء قبل الدفع:** `./vendor/bin/phpunit` كاملةً تمرّ (١٠١٢ اختباراً حالياً وتزيد). لا تُدفَع حزمةٌ حمراء.
- **إثبات لا ادّعاء:** كل عيبٍ أمنيّ يُكتَب **اختباراً يفشل أولاً** ثم يُصلَح. وكل ميزة تُختبَر وتُفحَص حيّاً على قاعدة معزولة قبل الدفع.
- **الإضافة لا الكسر:** لا حذف مسار، ولا تغيير عقد API، ولا هجرة مُدمِّرة. يُبنى على السكّتين القائمتين: سجل الوحدات `config/hub.php`، ومفتاح `(module, record_id)` متعدد الأشكال.
- **التنطيق والصلاحية أولاً:** كل قارئ/استعلام جديد يمرّ بـ`hub_scope` + `hub_can` + `hub_field_mode`. يُختبَر بمستخدم معزول قبل الدفع.
- **العربية أولاً (RTL):** الواجهة والتعليقات والرسائل بالعربية؛ المصطلحات التقنية في الكود بالإنجليزية كما هي.

## 🌿 الفرع والطلب

- التطوير على `claude/marhaba-fmuoui`، والفرع الأساس `main` (أُنشئ عند نقطة ما قبل التوحيد).
- الدفع: `git push -u origin claude/marhaba-fmuoui`، ولا يُفتح طلب دمج إلا بطلبٍ صريح.
