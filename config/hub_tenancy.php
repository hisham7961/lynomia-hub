<?php

/**
 * **انتماءُ الوحدةِ إلى شركة — إعلانٌ لا استنتاج.**
 *
 * كان عزلُ الشركاتِ في `hub_scope` مشروطاً بوجودِ عمودِ `company_id`:
 * وحدةٌ بلا عمودٍ **يسقط عنها العزلُ صمتاً**. والمراجعةُ الشاملة (F-04/F-16) عدّت
 * ثمانيَ وحداتٍ كذلك — وحادثةٌ منها تحمل اسمَ المشروعِ والعميلِ والسببَ الجذريّ،
 * فتُقرأ من شركةٍ أخرى ما يُردُّ ٤٠٤ إن طُلب من بابِ المشروعِ نفسِه.
 *
 * **والعلاجُ ليس ترقيعَ الثماني بل قلبَ الاصطلاح**: الصمتُ صار إغلاقاً لا فتحاً،
 * ومن لم يُعلِن انتماءَه يُردّ بـ`1 = 0` **ويسقط اختبارُ الإعلان**. فالبابُ التاسعُ
 * لا يُولَد صامتاً.
 */
return [

    /*
     * **مسارُ الانتماء**: الوحدةُ بلا عمودِ شركةٍ ولها أبٌ يحمله.
     * `col` عمودُ الأبِ في جدولِ الوحدة · `table` جدولُ الأب · `ref` عمودُ الشركةِ فيه.
     * والأبُ الفارغُ (`project_id IS NULL`) يُحكَم بـ`null_is_unowned` أدناه — نظيرُ
     * ما يفعله `hub_company_null_is_unowned` للعمودِ المباشر، فلا يُخترَع سلوكٌ ثالث.
     */
    'via' => [
        'incidents'   => ['col' => 'project_id', 'table' => 'projects', 'ref' => 'company_id'],
        'requests'    => ['col' => 'project_id', 'table' => 'projects', 'ref' => 'company_id'],
        'ideas'       => ['col' => 'project_id', 'table' => 'projects', 'ref' => 'company_id'],
        'deps'        => ['col' => 'project_id', 'table' => 'projects', 'ref' => 'company_id'],
        'deploys'     => ['col' => 'project_id', 'table' => 'projects', 'ref' => 'company_id'],
        'competitors' => ['col' => 'service_id', 'table' => 'services', 'ref' => 'company_id'],
        'plans'       => ['col' => 'service_id', 'table' => 'services', 'ref' => 'company_id'],
    ],

    /*
     * **الأبُ الفارغ**: سجلٌّ بلا أبٍ — أيُقرأ للجميع أم لا لأحد؟
     * `true` = الفراغُ «بلا مالك» فيُقرأ (نظيرُ `hub_company_null_is_unowned`).
     * والاختيارُ هنا **يُقرأ** لا يُحجَب: حادثةٌ أو فكرةٌ بلا مشروعٍ عملٌ داخليٌّ
     * عامّ، وحجبُها يُفرغ شاشاتٍ يوميّةً بلا مكسبِ عزلٍ حقيقيّ (لا تحمل انتماءً
     * أصلاً). ومن أراد التشديدَ يقلبها إلى `false` بلا لمسِ محرّك.
     */
    'null_is_unowned' => [

        /*
         * **التذاكر** (v2.544 · L2-05): تذكرةٌ بلا شركةٍ لا يملكها أحد، وحجبُها
         * يُظلم **طابورَ الدعمِ كلَّه** على كلِّ معزول — قِيس حيّاً: ثلاثٌ وعشرون
         * تذكرةً في قاعدةِ المحاكاة، يرى منها الموظّفُ المعزولُ **صفراً**، لأنّ
         * لا تذكرةَ تحمل `company_id` أصلاً (المبذورةُ والبريديّةُ وتذكرةُ
         * البوّابةِ سواء). والمكسبُ الأمنيُّ من الحجبِ معدوم: الصفُّ لا يُعلن
         * انتماءً ليُخفى به. وما يحمل شركةً يبقى محجوباً عن غيرِها.
         *
         * **وهذا نصفُ العلاج**: النصفُ الآخر أن تُختَم الشركةُ عند الإنشاء
         * (`ClientPortalController::ticketStore`) فتضيق دائرةُ غيرِ المُعلَن.
         */
        'tickets'     => true,

        /*
         * **ما بقي بلا أبٍ بعد الردم** (v2.546 · L2-09). العمى نفسُه تكرّر على
         * خمسِ وحداتٍ أخرى، وسببُه أنّ `inheritCompany` كانت تعرف مصدرَين اثنين
         * يصفان **صاحبَ الجلسة** لا السجلَّ: الشركةَ النشطة، وأولى شركاتِ المُنشئ
         * إن كان معزولاً. فما أنشأه غيرُ المعزول — المالكُ والإدارةُ وأكثرُ
         * الموظّفين — حُفظ بلا شركةٍ ولو كان انتماؤه بيّناً من أبيه. والدليلُ
         * الحاسمُ في `updates`: **١٨ من ٥٨٠** — أي ما كتبه معزولون وحدَهم.
         *
         * فأُضيف المصدرُ الثالثُ في الكود (`hub_company_from_parent`)، ورُدم ما
         * سبق من الأبِ في هجرةٍ غيرِ مدمّرة: المهامُّ ١٢١ من ١٢١ والعوائقُ ٦ من ٦.
         * وهذه المفاتيحُ تحكم **ما يبقى فارغاً بعد ذلك** — لا ما يمكن ردمُه.
         *
         * والقاعدةُ هي المعلَنةُ أعلاه حرفيّاً: صفٌّ **لا يحمل انتماءً** لا يُخفى
         * به شيء، وحجبُه يُفرغ شاشاتٍ يوميّةً بلا مكسبِ عزلٍ حقيقيّ.
         */

        // مهمّةٌ أو عائقٌ بلا مشروع: عملٌ داخليٌّ عامّ — وما له مشروعٌ صار مختوماً بالردم
        'tasks' => true, 'issues' => true,

        /*
         * **والعميلُ الملتبس** (قرارُ المالك: يُشتقُّ ما لا يلتبس ويُعلَن الملتبس).
         * العميلُ لا أبَ له، فتُستنتَج شركتُه من مشاريعه — وأربعةٌ من ستّةٍ في
         * قاعدةِ المحاكاة تُستنتَج يقيناً. أمّا **عميلٌ تخدمه شركتان** من المجموعة
         * (مستشفى السلام وأفق للاتصالات) فلا يسعُه عمودٌ مفرد: يبقى بلا انتماءٍ
         * فيُقرأ للجميع — **وتقول الشاشةُ لماذا** وتدعو المالكَ للحسم، بدل أن
         * يُحسَم بقرعةٍ تُثبِت نصفَ الحقيقة. والارتباطاتُ تتبع عميلَها.
         */
        'clients' => true, 'engagements' => true,

        /*
         * **ودليلُ الحسابات ليس بياناتِ شركة** بل هيكلَ المجموعةِ المحاسبيَّ
         * الواحد (الصندوق ١٠١٠ · الذمم المدينة ١٢٠٠ · ضريبة القيمة المضافة
         * ٢٢٠٠). لا مشروعَ له ولا عميل، وعزلُه بالشركةِ يُفرغ كلَّ شاشةٍ محاسبيّةٍ
         * على كلِّ معزولٍ بلا مكسب. فإعلانُه غيرَ مملوكٍ **وصفٌ لا تنازل**.
         */
        'accounts2' => true,

        'incidents' => true, 'requests' => true, 'ideas' => true,
        'deps' => true, 'deploys' => true, 'competitors' => true, 'plans' => true,
    ],

    /*
     * **الإعفاءُ الصريح**: وحدةٌ خارجَ المستأجِر أصلاً — **بسببٍ مكتوب**.
     * الخانةُ الفارغةُ مرفوضةٌ باختبار: الإعفاءُ قرارٌ يُبرَّر لا سهوٌ يُسكَت عنه.
     */
    'exempt' => [
        'restores' => 'اختباراتُ استعادةِ النسخِ الاحتياطيّة سجلٌّ تشغيليٌّ للنظامِ نفسِه لا '
            . 'بيانُ مستأجِر: لا مشروعَ لها ولا خدمةَ ولا عميل، وموضوعُها نسخةُ المنصّةِ '
            . 'كاملةً عبرَ الشركاتِ جميعاً. وحصرُها بشركةٍ يجعل سؤالَ «أتعمل نسخُنا؟» '
            . 'بلا جوابٍ لمن يملك شركةً واحدة. (المراجعةُ الشاملة · قرارٌ مُعلَنٌ لا سهو.)',
    ],
];
