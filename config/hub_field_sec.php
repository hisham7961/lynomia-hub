<?php

/**
 * **الحقولُ الحسّاسةُ لكلِّ وحدة** (Permissions 360 · 09.2 · 07.2 · النمطُ الآمن).
 *
 * الحقلُ المذكورُ هنا **يُحجب** عن كلِّ من لا يحمل مفتاحَ `fieldsec` على وحدتِه
 * (يقرؤه `hub_field_mode` قبلَ قواعدِ الدور — والمالكُ يرى دوماً). ليس محرّكاً ثانياً:
 * المفتاحُ يُخزَّن في نفسِ `role.matrix` كسائرِ المفاتيحِ الدقيقة، وقواعدُ الحقولِ
 * (`field_rules`) تبقى تعمل فوقَه لحامليه (ro/hide أدقّ).
 *
 * **هجرةٌ عديمةُ الخسارة** (grant_fieldsec_to_existing_roles): كلُّ دورٍ كان يرى
 * هذه الحقولَ يومَ التفعيل مُنح المفتاحَ — فلا يفقد أحدٌ وصولَه، ويصير الحقلُ
 * قابلاً للمنحِ والمنعِ من محرّرِ الأدوار.
 *
 * المفتاحُ هو **مفتاحُ الحقل** (key) في تعريفِ الوحدة — لا اسمُ العمود.
 */
return [

    // الموارد البشرية: الراتبُ ووثائقُ الهويّةِ والحسابُ البنكيّ (09.2)
    'hr' => ['salary', 'civilId', 'iban', 'iqama', 'passport', 'passExp'],

    // المشاريع: الميزانيّةُ والتكلفةُ الفعليّة (07.2 — تبويبُ الماليّةِ يتبعهما)
    'projects' => ['budget', 'cost'],

    // العروض: التكلفةُ الداخليّةُ التقديريّة (11.5 — هامشُ الربحِ يُشتقّ منها،
    // وكانت مكشوفةً لكلِّ حاملِ quotes:v). هجرةُ grant_fieldsec_quotes_banks
    // صانت الأدوارَ القائمة.
    'quotes' => ['cost'],

    // البنوك: الحسابُ ورصيدُه (11.5 — IBAN هويّةُ تحويلٍ ورصيدٌ نقديٌّ حسّاس)
    'banks' => ['iban', 'balance'],

];
