<?php

/**
 * **الكتالوج المرجعيّ للصلاحيات الدقيقة** (Permissions 360 · المرحلة 1).
 *
 * لا محرّكُ توثيقٍ ثانٍ: هذه الصلاحياتُ تُخزَّن في **نفس** `role.matrix` بمفاتيحَ مسمّاةٍ
 * إلى جانبِ (v/a/e/d)، ويقرؤها `hub_can($u, $module, $key)` كأيِّ عملية (المالكُ يتجاوز).
 * الغايةُ من إعلانها هنا مركزيّاً:
 *
 *   ١. **محرِّرُ الأدوار يحفظُها** — كان يعيدُ بناءَ المصفوفةِ من (v/a/e/d) وحدَها فيمحو
 *      أيَّ مفتاحٍ مسمّى صمتاً؛ الآن يجمعُ كلَّ مفتاحٍ مُعلَنٍ هنا فلا يُمحى عند الحفظ.
 *   ٢. **الفاحصُ يُفسّرها** — تظهرُ باسمِها في «لماذا يرى هذا المستخدمُ هذا؟».
 *   ٣. **التنطيقُ صريح** — كلُّ صلاحيةٍ دقيقةٍ لها اسمٌ وشرحٌ ووحداتٌ تنطبقُ عليها.
 *
 * لكلِّ صلاحية:
 *   label    اسمُها العربيّ (يظهرُ في محرِّرِ الأدوار والفاحص)
 *   hint     سطرٌ يشرحُ ما تمنحه بالضبط
 *   modules  الوحداتُ التي تنطبقُ عليها ('*' = كلُّها)
 *   group    تجميعُها في الشاشة
 *   risky    صلاحيةٌ حسّاسةٌ تُوسَم (لا تُدسّ بين البقية)
 *   implies  عملياتٌ تستلزمها ضمناً (docsec بلا v لا معنى له)
 */
return [

    // ── الاعتماد: حسمُ الطلبِ لكلِّ مجالٍ على حدة (بدل رايةِ approve الجامعة) ──
    // إضافةٌ لا كسر: الرايةُ تبقى «مفتاحاً رئيساً» يمنحُ الكلَّ؛ وهذا المفتاحُ منحٌ
    // أضيقُ لمن يُراد له حسمُ مجالٍ واحدٍ دون البقية. `custody.approve` كان مفتاحاً
    // حيّاً يمحوه المحرّرُ — إعلانُه هنا يحفظه (النتيجة 20.2).
    'approve' => [
        'label'   => 'اعتماد الطلبات',
        'hint'    => 'حسمُ (اعتماد/رفض) طلباتِ هذا المجال — منفصلٌ عن رؤيتِه وتعديلِه، وأضيقُ من رايةِ الاعتماد الجامعة.',
        'modules' => ['purchases', 'payroll', 'quotes', 'custody'],
        'group'   => 'الاعتماد والحسم',
        'risky'   => true,
        'implies' => ['v'],
        'legacy'  => 'approve',
    ],

    // ── التصدير: إخراجُ CSV لكلِّ وحدةٍ على حدة (بدل رايةِ exp الجامعة) ──
    // إضافةٌ لا كسر: رايةُ exp تبقى مفتاحاً رئيساً يصدّرُ كلَّ وحدةٍ يراها الحامل؛ وهذا
    // المفتاحُ منحٌ أضيقُ لتصديرِ وحدةٍ بعينها (الأهمُّ للبياناتِ الحسّاسة). حزامُ التصدير
    // (تجميدُ الطوارئ + تصعيدُ الحجمِ الكبير + بصمةُ التدقيق) يبقى كما هو فوقَ القرار.
    'export' => [
        'label'   => 'تصدير البيانات (CSV)',
        'hint'    => 'تصديرُ سجلّاتِ هذه الوحدةِ إلى CSV — منفصلٌ عن رايةِ التصديرِ الجامعة، والأجدى للوحداتِ الحسّاسة (موارد/رواتب/ماليّة/حضور).',
        'modules' => ['hr', 'payroll', 'fin', 'purchases', 'quotes', 'attend', 'custody', 'banks', 'clients', 'suppliers', 'contracts', 'projects'],
        'group'   => 'التصدير',
        'risky'   => true,
        'implies' => ['v'],
        'legacy'  => 'exp',
    ],

    // ── التصدير خارج الدوام: استثناءٌ مسمّى من حزام «نقل الملفات ممنوع خارج وقت العمل» ──
    // (الجولة 1 · F17) إقفالُ الشهرِ يقعُ ليلاً بطبيعته، وحزامُ ساعاتِ العمل كان يحظرُ
    // تصديرَ CSV على الجميع بلا استثناء. إضافةٌ لا كسر: القيدُ يبقى على من لا يحمل
    // المفتاح، وحاملُه يُستثنى **للتصدير وحده** وكلُّ استعمالٍ يُوسَم في التدقيق.
    'exportNight' => [
        'label'   => 'تصدير خارج الدوام',
        'hint'    => 'استثناءُ حاملِه من قيدِ ساعاتِ العمل لسحبِ ملفِّ وحدتِه — تصديرُ CSV (إقفالُ الشهرِ الليليّ) ومستنداتُها PDF/DOC — القيدُ يبقى على البقيّة، وكلُّ استعمالٍ يُقيَّد في التدقيق موسوماً «خارج الدوام».',
        'modules' => ['hr', 'payroll', 'fin', 'purchases', 'quotes', 'changeorders', 'attend', 'custody', 'banks', 'clients', 'suppliers', 'contracts', 'projects'],
        'group'   => 'التصدير',
        'risky'   => true,
        'implies' => ['v'],
    ],

    // ── الماليّة: قيدُ قبضٍ/صرفٍ يحرّك رصيدَ حسابٍ بنكيّ دون رايةِ تعديل البنوك ──
    // (الجولة 1 · F16) توجيهُ دفعةِ مستندٍ ماليّ لحسابٍ بنكيّ كان محسوماً بـ`banks:e`
    // الكاملة (تحريرُ الحسابات نفسِها) — فالمحاسبُ الذي يسجّل القبضَ يُردّ ٤٢٢
    // والأرصدةُ لا تتحرّك أبداً. إضافةٌ لا كسر: حاملُ `banks:e` يبقى يقدر، وهذا
    // المفتاحُ منحٌ أضيقُ يفتح تحريكَ الرصيد بالدفعة (وعكسَها) دون تحرير الحساب.
    'bankPost' => [
        'label'   => 'قيد قبضٍ/صرفٍ بنكيّ',
        'hint'    => 'توجيهُ دفعةِ مستندٍ ماليّ إلى حسابٍ بنكيّ فيتحرّك رصيدُه (قبضاً أو صرفاً، وعكسُ الدفعة كذلك) — منحٌ أضيقُ من رايةِ تعديلِ البنوك، للمحاسبِ الذي يسجّل الدفعاتِ دون تحريرِ الحسابات نفسِها.',
        'modules' => ['banks'],
        'group'   => 'الاعتماد والحسم',
        'risky'   => true,
        'implies' => ['v'],
        'legacy'  => 'e',
    ],

    // ── الوثائق: تصريحُ الأنواعِ الحسّاسةِ على مستوى الوحدة ──
    'docsec' => [
        'label'   => 'الوثائق الحسّاسة',
        'hint'    => 'رؤيةُ وتنزيلُ الوثائقِ المصنَّفةِ حسّاسةً في هذه الوحدة (هوية، جواز، عقد، صحّة، حساب بنكيّ، إقرار سرّية). بدونها تبقى الوحدةُ مرئيّةً لكنّ وثائقَها الحسّاسةَ محجوبة.',
        'modules' => ['hr', 'companies', 'suppliers', 'clients'],
        'group'   => 'الوثائق والملفات',
        'risky'   => true,
        'implies' => ['v'],
    ],

    // ── الحقولُ الحسّاسة: راتبٌ وهويّاتٌ وماليّةُ المشاريع خلفَ مفتاحٍ يُمنَح ──
    // النتيجتان 09.2/07.2 (النمطُ الآمن): الحقولُ المصنَّفةُ في config/hub_field_sec.php
    // تُحجب عمّن لا يحمل هذا المفتاحَ على وحدتِها (hub_field_mode يقرؤه قبل قواعدِ الدور).
    // هجرةُ grant_fieldsec منحته لكلِّ دورٍ كان يراها — فلا فقدَ فوريّاً، والحقلُ صار
    // قابلاً للمنعِ من محرّرِ الأدوار. قواعدُ الحقول (ro/hide) تبقى تعمل فوقَه لحامليه.
    'fieldsec' => [
        'label'   => 'الحقول الحسّاسة',
        'hint'    => 'رؤيةُ الحقولِ المصنَّفةِ حسّاسةً في هذه الوحدة (راتب، رقم مدنيّ، IBAN، إقامة، جواز — وميزانيّةُ المشروعِ وتكلفتُه، وتكلفةُ العرض، وحسابُ البنكِ ورصيدُه). بدونها تبقى الوحدةُ مرئيّةً وحقولُها الحسّاسةُ محجوبة.',
        'modules' => ['hr', 'projects', 'quotes', 'banks'],
        'group'   => 'الحقول الحسّاسة',
        'risky'   => true,
        'implies' => ['v'],
    ],

    // ── العهدة: تفكيكُ رايةِ «تعديل الأصول» (assets:e) الواحدةِ إلى سلطاتٍ أضيق ──
    // النتيجة 12.4: عمليةُ `e` واحدةٌ كانت بوّابةَ تسليمِ العهدةِ وتغييرِ الحالةِ
    // وإسنادِ المحطةِ **وتعديلِ مواصفاتِ الأصل** معاً. إضافةٌ لا كسر: حاملُ `e` يبقى
    // يقدرُ على الكلِّ (مفتاحٌ رئيس)، وهذه المفاتيحُ تمنحُ منحاً أضيقَ لدورٍ متخصّص
    // (أمينُ عهدةٍ يُسنِدُ دون أن يُعدِّلَ مواصفاتِ الأصل).
    'custodyAssign' => [
        'label'   => 'إسناد العهدة',
        'hint'    => 'تسليمُ/استردادُ العهدةِ وتصاريحُ النقل/الخروج — وضعُ الأصلِ في يدِ شخصٍ مُسمّى. منحٌ أضيقُ من رايةِ تعديلِ الأصول، دون المساسِ بمواصفاتِ الأصل.',
        'modules' => ['assets'],
        'group'   => 'الأصول والعهد',
        'risky'   => true,
        'implies' => ['v'],
        'legacy'  => 'e',
    ],
    'assetStatus' => [
        'label'   => 'تغيير حالة الأصل',
        'hint'    => 'تبديلُ حالةِ الأصل (نشط/صيانة/خارج الخدمة…) دون تعديلِ مواصفاتِه أو إسنادِ عهدتِه.',
        'modules' => ['assets'],
        'group'   => 'الأصول والعهد',
        'risky'   => false,
        'implies' => ['v'],
        'legacy'  => 'e',
    ],
    'assetStation' => [
        'label'   => 'إسناد الأصل لمحطة',
        'hint'    => 'ربطُ الأصلِ بمحطّةٍ ماديّة دون تعديلِ مواصفاتِه أو حالتِه.',
        'modules' => ['assets'],
        'group'   => 'الأصول والعهد',
        'risky'   => false,
        'implies' => ['v'],
        'legacy'  => 'e',
    ],
    'assetInventory' => [
        'label'   => 'جردُ الأصول',
        'hint'    => 'فتحُ/إغلاقُ جلساتِ الجردِ وإثباتُ فروقِها — منفصلٌ عن تعديلِ مواصفاتِ الأصل.',
        'modules' => ['assets'],
        'group'   => 'الأصول والعهد',
        'risky'   => true,
        'implies' => ['v'],
        'legacy'  => 'e',
    ],

    // ── الأجهزة الطرفيّة: تفكيكُ رايةِ المراقبة (monitor) الجامعة ──
    // النتيجة 12.3/14.3: إصدارُ أوامرِ الأجهزةِ وسكُّ رموزِ التسجيلِ (MDM) كانا
    // يُحسَمان برايةِ `monitor` الجامعةِ (رؤيةُ اللوحات). إضافةٌ لا كسر: المراقبُ/المالكُ
    // يبقيان يقدران، وهذان المفتاحان يمنحان الأمرَ/السكَّ لدورٍ تشغيليٍّ دون رايةِ المراقبة.
    'command' => [
        'label'   => 'أوامر الأجهزة',
        'hint'    => 'إصدارُ أوامرِ التحكّمِ للأجهزةِ الطرفيّة (قفل/مسح/تحديث…). منحٌ أضيقُ من رايةِ المراقبة الجامعة.',
        'modules' => ['endpoints'],
        'group'   => 'الأجهزة الطرفيّة',
        'risky'   => true,
        'implies' => ['v'],
    ],
    'enroll' => [
        'label'   => 'سكُّ رموز التسجيل',
        'hint'    => 'إصدارُ رموزِ تسجيلِ الأجهزة (MDM) المربوطةِ بأصلٍ مؤهّل. منحٌ أضيقُ من رايةِ المراقبة الجامعة.',
        'modules' => ['endpoints'],
        'group'   => 'الأجهزة الطرفيّة',
        'risky'   => true,
        'implies' => ['v'],
    ],

    // ── المشاريع: ربطُ الأصولِ بالمشروعِ منفصلٌ عن تعديلِ الأصول ──
    // النتيجة 07.6: تخصيصُ الأصلِ لمشروعٍ كان يتطلّب رايةَ `assets:e` الكاملة (تعديلُ الأصول)
    // مع `projects:v`. إضافةٌ لا كسر: حاملُ `assets:e` يبقى يقدر، وهذا المفتاحُ يمنحُ منسّقَ
    // مشروعٍ سلطةَ ربطِ الأصولِ بمشاريعِه دون سلطةِ تعديلِ الأصولِ نفسِها.
    'assetAssign' => [
        'label'   => 'ربط الأصول بالمشروع',
        'hint'    => 'تخصيصُ الأصولِ لمشاريعَ وفكُّ ارتباطِها — منحٌ أضيقُ من رايةِ تعديلِ الأصول، لمنسّقِ المشروع.',
        'modules' => ['projects'],
        'group'   => 'المشاريع',
        'risky'   => false,
        'implies' => ['v'],
    ],

    // ── بوّابة العميل: إدارةُ الأعضاءِ منفصلةٌ عن تعديلِ بيانات العميل ──
    // النتيجة 15.5: دعوةُ أعضاءِ البوّابةِ وإسنادُ أدوارهم وإلغاؤهم — وهي تُنشئ حساباتِ
    // دخولٍ خارجيّةً وقد تمنحُ دورَ «مالكِ العميل» — كانت محسومةً برايةِ `clients:e`
    // (تعديلُ CRM). إضافةٌ لا كسر: حاملُ `e` يبقى يقدر، وهذا المفتاحُ يفصلُ سلطةَ
    // إدارةِ الدخولِ عن تعديلِ بياناتِ العميل لمن يُراد له أحدُهما دون الآخر.
    'membersManage' => [
        'label'   => 'إدارة أعضاء البوّابة',
        'hint'    => 'دعوةُ أعضاءِ بوّابةِ العميلِ وإسنادُ أدوارهم وإلغاؤهم (إنشاءُ حساباتِ دخولٍ خارجيّة). منحٌ أضيقُ من رايةِ تعديلِ العملاء.',
        'modules' => ['clients'],
        'group'   => 'بوّابة العميل',
        'risky'   => true,
        'implies' => ['v'],
        'legacy'  => 'e',
    ],

    // ── المشاريع: تفكيكُ سلطةِ «تعديل المشروع» الواحدةِ إلى مجموعاتِ كتابةٍ مسمّاة ──
    // النتيجة 07.4: `projects:e` واحدةٌ كانت تكتب الفريقَ (managerId/members)
    // والماليّةَ (budget/cost/revExp) والبنيةَ التقنيّة (url/staging/git/prod) معاً.
    // إضافةٌ لا كسر: هجرةُ grant_projects_write_groups منحت المفاتيحَ الثلاثةَ لكلِّ
    // دورٍ يحمل `projects:e` — فلا فقدَ فوريّاً، وصار سحبُ مجموعةٍ من محرّرِ الأدوار
    // ممكناً: حقلٌ في `writes` يصير **قراءةً فقط** لمن لا يحمل مفتاحَه (hub_field_mode).
    'projTeam' => [
        'label'   => 'فريقُ المشروع — كتابة',
        'hint'    => 'كتابةُ مديرِ المشروعِ وأعضائِه — بدونها تبقى الحقولُ مرئيّةً قراءةً فقط لحاملِ التعديل.',
        'modules' => ['projects'],
        'group'   => 'المشاريع',
        'risky'   => false,
        'implies' => ['v'],
        'legacy'  => 'e',
        'writes'  => ['projects' => ['managerId', 'members']],
    ],
    'projFin' => [
        'label'   => 'ماليّةُ المشروع — كتابة',
        'hint'    => 'كتابةُ الميزانيّةِ والتكلفةِ والإيرادِ المتوقَّع — بدونها قراءةٌ فقط (ورؤيتُها أصلاً خلف fieldsec).',
        'modules' => ['projects'],
        'group'   => 'المشاريع',
        'risky'   => true,
        'implies' => ['v'],
        'legacy'  => 'e',
        'writes'  => ['projects' => ['budget', 'cost', 'revExp']],
    ],
    'projTech' => [
        'label'   => 'تقنيّةُ المشروع — كتابة',
        'hint'    => 'كتابةُ روابطِ الإنتاجِ والتجريبِ والمستودعِ — بدونها قراءةٌ فقط لحاملِ التعديل.',
        'modules' => ['projects'],
        'group'   => 'المشاريع',
        'risky'   => false,
        'implies' => ['v'],
        'legacy'  => 'e',
        'writes'  => ['projects' => ['url', 'staging', 'git', 'prod']],
    ],

    // ── الموارد البشرية: فتحُ حسابِ الدخولِ للموظّف منفصلٌ عن رايةِ إدارةِ المستخدمين ──
    // (الجولة 2 · G15) فتحُ حسابِ موظّفٍ جديدٍ كان محسوماً برايةِ `users` الجامعة — وهي
    // سلطةُ النظامِ كلِّه (كلُّ حساب، وكلُّ دور، وكلُّ إيقاف). فالتعيينُ الواحدُ كان يحتاج
    // أربعةَ أشخاص: HR تُنشئ الملفَّ ثم تنتظر المالكَ ليفتح الحساب، ومسؤولُ التقنية ينتظر
    // كليهما. إضافةٌ لا كسر: المالكُ وحاملُ الرايةِ يبقيان يقدران (مفتاحٌ رئيس)، وهذا
    // منحٌ أضيقُ يفتح **حساباتِ الموظّفين من ملفّاتهم** دون سلطةِ شاشةِ المستخدمين.
    // ولا `legacy` له: مفتاحُه الرئيسُ رايةٌ لا عمليةُ وحدةٍ، فلا يُشتقّ من `hr:e`
    // — وإلا ورثه كلُّ من يحرّر ملفّاً وهو ما لم يكن يملكه أحدٌ قبل اليوم.
    // وحارسُ التصعيد يسري عليه: حاملُه لا يمنح دوراً يملك إدارةَ المستخدمين (Staff).
    'staffAccounts' => [
        'label'   => 'فتحُ حساباتِ دخولٍ للموظّفين وإيقافُها',
        'hint'    => 'فتحُ حسابِ دخولٍ لموظّفٍ من ملفّه الوظيفيّ (بدورٍ مختارٍ وكلمةِ مرورٍ مؤقّتة) وربطُه بحسابٍ قائم — منحٌ أضيقُ من رايةِ إدارةِ المستخدمين، ولا يمنح حاملُه دوراً يملك إدارةَ المستخدمين ولا دورَ المالك.',
        'modules' => ['hr'],
        'group'   => 'الموارد البشرية',
        'risky'   => true,
        'implies' => ['v'],
    ],

    // ── الملفّات: الإرفاقُ كتابةٌ مسمّاةٌ لا ظلَّ للعرض ──
    // النتيجة 13.2: الإرفاقُ على سجلٍّ كان يكفيه `v` (قرارٌ منتجيٌّ قديم: مشاهدٌ يُرفق
    // مستنداً داعماً). إضافةٌ لا كسر: هجرةُ grant_attach منحت المفتاحَ لكلِّ دورٍ كان
    // يرى — فلا فقدَ فوريّاً — وصارت للمالكِ رافعةٌ لسحبِ الإرفاقِ من دورِ قراءةٍ بحتة
    // دون سحبِ الرؤية. حاملُ `e` يبقى يُرفق بلا هذا المفتاح (مفتاحٌ رئيس).
    'attach' => [
        'label'   => 'إرفاق الملفات',
        'hint'    => 'رفعُ مرفقاتٍ على سجلّاتِ هذه الوحدة دون سلطةِ تعديلِها — حاملُ التعديل (e) يُرفق دائماً.',
        'modules' => '*',
        'group'   => 'الوثائق والملفات',
        'risky'   => false,
        'implies' => ['v'],
        'legacy'  => 'e',
    ],

];
