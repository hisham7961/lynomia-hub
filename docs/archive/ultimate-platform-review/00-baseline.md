# ٠٠ — خطُّ الأساس المُجمَّد (Phase 0 Freeze)

> **قاعدةُ هذه الوثيقة: كلُّ رقمٍ فيها مقيسٌ بأمرٍ نُفِّذ، لا منقولٌ من تقريرٍ سابق.**
> التقاريرُ السابقة تُذكَر مصدراً للمقارنة لا مصدراً للحقيقة.

## ١ · هويّةُ اللقطة

| البند | القيمة | كيف قيست |
|---|---|---|
| المستودع | `hisham7961/lynomia-hub` | `git remote` |
| الفرع | `claude/lynomia-hub-enterprise-upgrade-xn4p2t` | `git branch --show-current` |
| HEAD | `1d906261277b5fd1e92bf559374f2a455453b6a0` | `git rev-parse HEAD` |
| النسخة | `2.540.1` | `cat VERSION` |
| شجرةُ العمل | **نظيفة** (0 تغييرٍ غيرِ مُدرَج) | `git status --porcelain` |
| التزامنُ مع الأصل | **0 دفعةٍ محليّةٍ غيرِ مدفوعة** | `git rev-list --count HEAD ^origin/…` |
| تاريخُ التجميد | 2026-09-17 | — |

> **ملاحظةٌ على التعليمات:** وردت في التكليف نفسُ القيم (`v2.540.1` / `1d90626`) بوصفها
> «معلوماتيّة»، وقد **تحقّقت** هنا بالفعل: الرأسُ الحاليُّ هو نفسُه، فلا تقدّمَ جرى بعد
> إعدادِ التكليف. ولو كان الرأسُ قد تقدّم لبُني خطُّ الأساس على الرأسِ الفعليّ لا على ما في النصّ.

## ٢ · حجمُ المنصّة (مقيسٌ حيّاً)

| البُعد | العدد | الأمر |
|---|---|---|
| الوحدات (`config/hub.php`) | **85** | `count(config('hub.modules'))` |
| المسارات — المجموع | **632** | `Route::getRoutes()` |
| — واجهةُ الويب | 513 | تصنيفٌ بالبادئة |
| — `/api/v1` | 31 | |
| — `/api/mobile/v1` | 88 | |
| الأفعال: GET / POST / PUT / PATCH / DELETE | 283 / 300 / 18 / 2 / 30 | `$route->methods()` |
| الهجرات | **250** | `ls database/migrations \| wc -l` |
| النماذج (Models) | **152** | `glob` |
| المتحكّمات | **135** | `find app/Http/Controllers` |
| أصنافُ الخدمة (`app/Support`) | **136** | `glob` |
| الوسطاء (Middleware) | **18** | `glob` |
| أوامرُ الطرفيّة | **25** | `glob` |
| القوالب (Blade) | **365+** | `find resources/views` |
| سجلُّ القدرات (FeatureRegistry) | **137** | `FeatureRegistry::all()` |
| — ENABLED / SYSTEM_INVARIANT / NOT_CONFIGURED / DEFERRED / READY / EXTERNAL | 107 / 11 / 9 / 4 / 3 / 3 | `counts()` |
| مواصفةُ OpenAPI | **182 مساراً · 172 مخطّطاً** | `docs/openapi.json` |
| عملياتُ الصلاحيّة الأساسيّة | 4 (`v` عرض · `a` إضافة · `e` تعديل · `d` حذف) | `PermissionInspector::OPS` |

## ٣ · بوّابةُ الاختبار عند التجميد

قيست على **هذا الرأس بالضبط** قبل دفعةِ v2.540.1 وأُعيد تأكيدُها على CI بعدها
([run #763](https://github.com/hisham7961/lynomia-hub/actions/runs/35150309505)):

| المحرّك | اختبارات | تأكيدات | سقوط |
|---|---|---|---|
| SQLite (`phpunit.xml`) | **4317** | 31124 | **0** |
| MySQL محلّيّاً (MariaDB 10.11، `phpunit.mysql.xml`) | **4317** | 31136 | **0** |
| MySQL على CI (MySQL 8.0.46) | 4317 | — | **0** |
| PHP 8.2 · sqlite على CI | 4317 | — | **0** |
| بوّابةُ OpenAPI | مطابقة | — | **0 انحراف** |
| `composer audit` | — | — | **0 ثغرة** |

## ٤ · عالَمُ المحاكاة الحيّ (لِطبقة ٢)

قاعدةٌ معزولةٌ عن `hub_test` وعن الإنتاج، باقيةٌ من محاكاةِ الشهر:

| الجدول | الصفوف |
|---|---|
| `users` | 33 |
| `employees` | 31 |
| `companies` | 3 |
| `roles` | 14 |
| `projects` | 16 |
| `tasks` | 121 |
| `tickets` | 22 |
| `assets` | 104 |
| `work_updates` | 580 |
| `documents` | 160 |
| `comments` | 22 |
| `audits` | 2010 |
| **الجداول** | **188** |

الأدوات: MariaDB 10.11 حيّة · Playwright 1.56.1 + Chromium في `/opt/pw-browsers` · 4 أنوية · 15GB ذاكرة.

## ٥ · ما سبق من تدقيقات (مصدرُ مقارنةٍ لا مصدرُ حقيقة)

`docs/` يحوي بالفعل: `final-audit/` · `month-simulation/` · `human-simulation/` ·
`permissions-360/` · `information-architecture/` · `features/` · `collaboration/` ·
`entity360/` · `project360/` · `work-os/` · `mobile-readiness/` · `endpoint/` ·
`control-plane/` · `discoverability/` · `performance/` · `expert-council/`.

**هذه المراجعةُ لا تثق بأيٍّ منها.** كلُّ ادّعاءٍ فيها يُعامَل فرضيّةً تُختبَر من جديد.
التقريرُ الختاميُّ السابق (v2.474.0) أعلن «0 تسريب» و«0 حالةٍ كاذبة» — وهذا بالضبط
ما ستحاول طبقةُ ١ ووكيلُ التحدّي (20) كسرَه.

## ٦ · قاعدةُ عدمِ الهدم

لا يُحذف مسارٌ ولا وحدةٌ ولا شاشةٌ ولا إعدادٌ ولا صلاحيّةٌ ولا واجهةُ API ولا بياناتٌ
تاريخيّة. جردُ القدراتِ أعلاه (85 وحدة · 632 مساراً · 137 قدرة · 182 مسارَ API) هو
**المرجعُ الذي يُقارَن به في طبقة ٦** — وشرطُ القبول: `القدراتُ المحذوفة = 0`.
