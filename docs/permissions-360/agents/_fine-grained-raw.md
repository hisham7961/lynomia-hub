# المفاتيحُ الدقيقةُ المقترحة (خامٌ من التدقيق — تُقنَّن في المرحلة 1)

> 120 مفتاحاً مقترحاً عبر 20 ورشة. تُدمَج وتُقنَّن في `05-permission-catalog.md`.


## نواة التوثيق

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `<module>.export` | تصدير الوحدة (CSV) | Gates CSV/bulk export per module in ModuleController::exportBelt, instead of the single global exp flag authorizing export of every viewable module. | module view (v) — a user may read a module in-app yet not be allowed to bulk-exfiltrate it. | sensitive | flags.exp (grant <module>.export to every module a role has v where exp was set, then retire the global flag) — REQUIRES extending RoleController::data() op loop beyond v/a/e/d or exports stay unpersistable. |
| `approvals.decide.payroll / approvals.decide.purchases` | اعتماد المسيّرات / اعتماد أوامر الشراء | Separates who may DECIDE payroll runs vs purchase orders vs generic approvals, replacing the single approve flag that unlocks all decision domains. | viewing an approval/payroll/purchase record (v) — deciding is a distinct, higher privilege currently fused into one cross-domain flag. | owner_only | flags.approve (map to all three initially, then narrow) — new keys, needs editor op-loop support. |
| `perf.view` | لوحة الأداء (بيانات الأفراد) | Individual/team performance dashboards (PerformanceController, hub_top_links perf/sales/kpis). | cost/financial dashboards and digital-assets — currently all fused under monitor. | sensitive | flags.monitor |
| `costs.view` | التكاليف والربحية | Org cost/profitability dashboards (CostController, servicecosts, capacity, recs). | individual performance data (perf.view) and digital-assets — unrelated sensitivities behind one monitor flag. | sensitive | flags.monitor |
| `digital.view` | مركز الأصول الرقمية | Digital assets center (DigitalAssetsController:19) — credentials/domains/servers inventory surfaced via the analytics monitor flag. | performance/cost analytics — an inventory of digital assets is a different privilege from viewing KPI charts. | owner_only | flags.monitor |
| `audit.trace` | أثر الطلب النظامي | Per-request system trace / internals (SystemTraceController:21). | reading the audit log (audit.view) — request-internals tracing is a deeper privilege than reading the tamper-evident trail. | sensitive | flags.audit |
| `updates.review` | مراجعة التحديثات/التقارير | The distinct 'review/approve a submitted update' action separate from editing the record. | edit (e) on the module — reviewing/deciding on a submission is not the same as authoring it. | sensitive | new (currently no separable key; approximated by e or by ReportReview::canReviewAny/hr:v) |
| `projects.fin.view` | ماليات المشروع | A project's financial fields/tabs separate from the project record itself. | projects.v — viewing a project should not imply viewing its budget/cost financials. | sensitive | field_rules on projects financial fields already provide per-field hide/ro; promote to an explicit key only if a whole financial sub-view (not single fields) must be gated. |
| `hr.salary.view (already separable — documented, not new)` | راتب الموظف | Salary field visibility on hr module. | hr.v (viewing the employee) — separable. | owner_only | ALREADY EXISTS via field_rules[hr][salary]=hide\|ro (templates pm/auditor use it). No new key needed; listed to confirm coverage. |
| `users (flag — confirm, not new)` | إدارة المستخدمين | User admin screen; the module 'users' is flag-governed, its matrix cells are inert (ModuleController::resolve 404s on users). | any matrix module — users is deliberately outside the matrix. | owner_only | flags.users (correctly wired; do not add a matrix key). |

## مسارات الويب

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `collab.oversight.view` | رقابة الاتصالات — قراءة القنوات والرسائل المباشرة | Access to OversightController (read every channel message + every DM thread for compliance). | hub_monitor (analytics only) and normal conversation membership; this is org-wide message surveillance. | owner_only | new — currently gated by role.name === setting('collab.oversight_role') (raw_role). Migrate to a role.flags key or matrix key so it appears in the role editor and is granted like every other permission; keep the stepup + mandatory-reason belt. |
| `attend.export` | تصدير سجلات الحضور | CSV export (m.export / m.bulk export) of the attend module specifically. | attend view (matrix v) and export of other modules; today ALL export is one global 'exp' flag. | sensitive | exp flag — a user granted exp today can export attendance and payroll and ICCID alike. |
| `payroll.export` | تصدير الرواتب | CSV export of payroll/salary-bearing modules (payroll, hr salary columns). | payroll view and generic export; salary is the highest-sensitivity export yet shares the single exp flag with every other module. | sensitive | exp flag (exportBelt->hub_exporter). Per-module export keys let owners grant low-risk exports without unlocking salary exports. |
| `analytics.financial.view` | لوحات مالية — الربحية وتكلفة المشاريع | CostController project P&L / margins / hour-rates and other financial rollups. | analytics.workforce.view and analytics.tech.view; currently all three are the same 'monitor' flag. | sensitive | monitor flag — splitting lets a workforce analyst see capacity without seeing project profit & loss. |
| `analytics.tech.view` | لوحات البنية التقنية — الخوادم والأصول الرقمية | TechWorkspaceController / DigitalAssetsController infrastructure inventory dashboards. | financial and workforce analytics; bundled today under one monitor flag. | sensitive | monitor flag. |

## API/الجوّال

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `hr.report.view` | عرض تقارير العمل اليومية للفريق | GET /api/v1/reports/daily (teamDaily) and its web equivalent — team-wide daily work/productivity compliance for all employees (names, depts, review_status, hours). Should be a distinct key checked alongside hub_scope+company scope. | hr.v — viewing an individual employee record is not the same privilege as pulling every employee's daily productivity/compliance across the company. A line manager may see their reports without seeing the whole HR directory, and vice versa. | sensitive | hr:v (current gate is hub_can('hr','v')); migrate existing hr:v holders to also receive hr.report.view for no behavior change, then let owners split them. |
| `fin.export` | تصدير/سحب دفتر المالية عبر API | Bulk programmatic pull of the whole finance ledger via GET /api/v1/fin (list, per up to 100, ?fields=, time filters) and GET /api/mobile/v1/sync/fin (entire dataset via cursor). Separates whole-ledger extraction from single-record viewing. | fin.v — reading one invoice in the UI is materially different from exfiltrating the entire finance ledger (all kinds: sales, purchases, expenses) through the list/sync API in one automated sweep. The API list endpoint is an export vector gated identically to single view. | owner_only | fin:v (migrate current fin:v holders to fin.export initially = no change; owners then revoke bulk pull from roles that only need in-app single-record view). |
| `hr.export` | تصدير/سحب بيانات الموظفين عبر API | Bulk pull of employee/HR records via GET /api/v1/hr list and GET /api/mobile/v1/sync/hr (whole employee table, cursor-paged) as opposed to viewing one employee. | hr.v — same distinction as fin.export: whole-table programmatic extraction (PII: names, salaries where visible, contacts) versus single-record lookup. Currently one hr:v unlocks both. | owner_only | hr:v (initial mapping = hr:v holders get hr.export; then owners scope it down). |
| `fin.client_view` | عرض العميل المنسّق للمالية | Column- and kind-curated finance view for client accounts (kind∈{sales,receipts}, client-safe columns only) enforced server-side for the generic path, matching ClientPortalData curation. Would let the generic client-reachable /fin (CRUD/sync/api-v1) be gated to the curated projection instead of full fin fields. | fin.v (full internal finance view incl. purchases/expenses and cost columns). Today a client role granted fin:v reaches the generic path with the FULL field set (minus field_mode hides) while the dedicated portal deliberately restricts kind+columns — no key expresses 'client-safe finance only'. | sensitive | new — the safe migration is to make the generic client path route through ClientPortalData curation for account_type=client rather than introduce a matrix key; key listed to capture the missing distinct privilege. |

## التنقّل/الاكتشاف

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `updates.review` | مراجعة تقارير الفريق اليومية | Visibility+access of the reports-review center (reportsr / reports.review) and per-report accept/return actions — reviewing OTHER employees' daily work reports. | updates.v/updates.e (submitting/editing one's own report). Today ReportReview::canReviewAny grants review to anyone with updates.e OR hr.v OR owner, so the edit privilege silently confers review authority over the whole team. | sensitive | updates.e |
| `attend.export` | تصدير كشف الحضور الشهري | Downloading/exporting the monthly attendance & payroll-attendance sheet reached from the attmonth center (reports.monthly). Needs its own IA export destination. | attend.v (viewing attendance in-screen). Currently attend.v\|\|hr.v both view the sheet and can export the full payroll-grade dataset in one privilege. | sensitive | attend.v |
| `projects.fin.view` | عرض ماليات المشروع | Financial roll-ups/figures surfaced on project-centric navigation: appsprojects center (appsproj), project workspace cards, and the financial block of journey (client 360). | projects.v (that a project exists / its non-financial data). appsproj center appears for apps.v\|\|projects.v regardless of finance rights; journey already separates via fin.v — the nav center should carry the same distinct key. | sensitive | fin.v |
| `identity.view` | مركز الهوية والمسح | Visibility of the identity/scan center (identity.center) which serves both asset-custody and product-cataloging audiences. | assets.v alone. Controller admits assets.v OR products.v, but the catalog reveals it only to assets.v — a products-only cataloger cannot discover the very center built for them. | normal | assets.v OR products.v |
| `admin.control.view` | نظرة التحكّم والتنبيهات (للمراقب) | A visibility flag that lets the administration IA domain / gear bar admit the monitor- and mobile-gated admin centers (control, alerts, mobileplatform) instead of the fixed owner/users/audit/secrets wrapper. | the four admin-bar flags (users/audit/secrets/owner). control/alerts are monitor-gated and mobileplatform is mobile-gated, but the wrapper omits both, so these centers are undiscoverable to their intended delegated admins. | sensitive | monitor flag (control/alerts) + mobile flag (mobileplatform) |

## اللوحات/العدّادات

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `analytics.perf.view` | عرض لوحة الأداء المؤسسي | PerformanceController@index company KPIs (revenue, margin, growth, active partners, projects, critical issues) — the org-wide financial/performance dashboard. | Per-employee execution ratings (analytics.people.view) and per-project P&L (analytics.cost.view). Today all three ride the single monitor flag. | sensitive | flag:monitor |
| `analytics.people.view` | عرض مؤشرات أداء الأفراد | Per-employee KPI/execution rows and manager rating in PerformanceController::peopleKpis and WorkforceController overview (ExecutionStats::people/org). | Company-level aggregates (analytics.perf.view): seeing the firm's revenue differs from ranking individual employees' completion/on-time/rating. | sensitive | flag:monitor + field hr.perf (rating already respects hub_field_mode(hr,perf)) |
| `analytics.cost.view` | عرض التكاليف والربحية | CostController index (project P&L, hourly rates/salaries) and services (service costs + MRR). | Performance dashboard (analytics.perf.view): cost/profitability and labor rates are a separate financial sensitivity from execution metrics. | sensitive | flag:monitor |
| `kpis.manage` | إدارة تعريفات المؤشرات | KpiController store/update/toggle/move/destroy — creating and editing KPI formulas. | Merely viewing KPI cards/values (the kpis dashboard widget / kpis.index read). Building the measurement definition is stronger than reading its number. | normal | flag:monitor |
| `analytics.capacity.view` | عرض القدرات والأثر | CapacityController capacity/quality/recommendations/impact (org-wide utilization and dependency map). | Financial dashboards (perf/cost): operational capacity is separate from revenue/profit exposure. | normal | flag:monitor |

## واجهة/أزرار

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `<module>.export` | تصدير <الوحدة> | Ability to CSV-export a specific module's rows (list export + bulk 'export selected'). Would be checked alongside module 'v' so export of low-sensitivity modules can be granted without unlocking bulk exfiltration of hr/payroll/fin/custody. | Reading the module in-app (module.v) and the current single global 'exp' flag that unlocks export of ALL 85 modules at once. | sensitive | exp flag — migrate current exp-flag holders to export on the modules they already have 'v' on; keep 'exp' as master for owners. |
| `entries.post` | ترحيل القيود المحاسبية | Posting a balanced journal entry to the general ledger (entries.post) — an irreversible, locking financial action. | Drafting/adding/removing journal lines on a draft entry (currently both are entries.e). | sensitive | entries.e |
| `stockmv.post` | ترحيل حركات المخزون | Confirming/posting a stock movement so it moves the real item balance (and cancel/reverse which reverses the balance). | Editing a draft movement's fields (currently both are stockmv.e). | sensitive | stockmv.e |
| `fin.pay` | تسجيل مدفوعات المالية | Recording a payment / settling a financial document (fin.act do=pay), which moves cash and the selected bank/box balance and auto-advances state. | Editing invoice/bill fields (currently both are fin.e). | sensitive | fin.e |
| `quotes.convert` | تحويل عرض السعر إلى عقد/فاتورة/مشروع | Converting an accepted quote into a binding contract, invoice, or external project (quotes.act do=contract\|invoice\|project). | Editing/sending/accepting a quote draft (currently quotes.e; project conversion additionally needs projects.a). | sensitive | quotes.e |
| `projects.fin.view` | عرض ماليات المشروع | A single named key for project P&L visibility (cost, profit, margin, budget, cost_delta) covering BOTH the project-360 finance tab and the costs center per-project view. | Viewing the project itself (projects.v). Today this privilege is split/duplicated: field-mode hide on cost+budget for the 360 tab vs the 'monitor' flag for /costs — the two disagree. | sensitive | monitor flag OR field_rules(projects) cost+budget hidden — unify both onto this key. |
| `payroll.approve` | اعتماد/صرف المسيّر | Approving and paying a payroll run (release of salaries). Server already requires the 'approve' flag for approve(); this makes it an explicit, discoverable per-module key rather than an implicit flag the payroll UI doesn't reflect. | Generating/adjusting a draft run (payroll.e). | sensitive | approve flag (already enforced server-side for approve) |

## التعاون

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `integrations.messaging.manage` | إدارةُ مركز المراسلة (بريد/تلجرام/الطابور) | Viewing and configuring outbound messaging: SMTP host/credentials, Telegram bot token+channel, sending test messages, and retrying the failed outbox. Currently MessagingController::gate() is hub_is_owner() only, so the only way to let a non-owner administer messaging is to make them a full owner (owner bypasses everything). | Full account ownership — a distinct, delegable 'operate the messaging integration' privilege that does not also grant every other module, flag and matrix op. | owner_only | secrets flag (RISKY) — SMTP password/Telegram token are encrypted secrets; map from the existing secrets flag, defaulting to owner-only until explicitly granted. |
| `collab.client_channel.create` | إنشاءُ قناة/غرفة بجمهور عميل (client/both) | Creating a Conversation whose audience is client or both (a surface that pushes messages to external client accounts), via ConversationController::store or the collab center create panel. | Creating an ordinary internal channel/group, which any authenticated internal user may do; this key gates only the externally-visible client-audience variant. | sensitive | new — no current gate exists (store only validates client_id against hub_client_ids); introduce as a new key mapping from projects:e where the room is project-bound, else owner-only. |

## المشاريع

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `projects.fin.view` | عرض ماليّة المشروع (الربحية) | Project360 finance tab + /costs?p= + /costs list: hub_project_pl output (revenue, collected, labor/server/tool/external cost, profit, margin, budget variance, delay cost). | projects.v — viewing the project record, delivery status, team, tech. Today ANY projects.v holder sees full P&L by default. | sensitive | field_rules.projects.cost=hide && projects.budget=hide is the only current lever (defaults open); /costs additionally keyed off the monitor flag. Migrate: grant projects.fin.view where cost/budget currently visible OR monitor flag set. |
| `projects.fin.edit` | تعديل أرقام المشروع المالية | Writing budget/cost/rev_exp on the project (config/hub.php:354-371) which drive hub_project_pl and change-order baseline. | projects.e (which also edits members/manager/tech) and projects.members.manage. | sensitive | projects.e |
| `projects.members.manage` | إدارة أعضاء ومدير المشروع | Writing manager_id and members[] (config/hub.php:251-265). | projects.e — currently the same op also edits financials and technical infra. | normal | projects.e |
| `projects.tech.view` | عرض البنية التقنية للمشروع | Viewing url/staging/git/prod (config/hub.php:330-384) in Project360 overview + record. | projects.v — business view of a project vs its infra/repo endpoints. | sensitive | field_rules.projects.url/staging/git/prod=hide (only current lever; client is hard-hidden via $pcCliHide). |
| `projects.tech.edit` | تعديل البنية التقنية للمشروع | Writing url/staging/git/prod endpoints. | projects.e and projects.members.manage. | sensitive | projects.e |
| `projects.activity.view` | عرض سجل العمل والتقدّم على المشروع | Project360 activity tab work_updates stream (hours, blockers, per-employee daily reports, review_status) — projects.blade.php:36,331-380. | projects.v — currently ungated by any updates permission; should equal updates.v (which hub_related already enforces for the delivery tab). | sensitive | updates.v |
| `projects.notes.view` | عرض ملاحظات الإدارة على المشروع | projects.notes field (config/hub.php:405) — internal management notes. | projects.v. | sensitive | field_rules.projects.notes=hide (client hard-hidden via $pcCliHide). |
| `updates.review` | مراجعة واعتماد تقارير العمل | reports.review center + reviewAct (accept/needs_revision/reopen) via ReportReview. | updates.e — editing/creating a work update vs sitting in judgment on others' reports. | sensitive | updates.e (+ hr.e / owner keep review) |
| `projects.asset.assign` | تخصيص أصل لمشروع | AssetProjectController assign/end (web+API). | assets.e — editing an asset record vs binding it to a project. | sensitive | assets.e (still also require projects.v) |
| `changeorders.apply` | تطبيق أمر التغيير على المشروع | ChangeOrderController::apply — mutates project rev_exp/budget baseline. | changeorders.e — editing a CO draft vs committing it to the project's financials. | sensitive | changeorders.e (+ already requires projects.e — good precedent) |
| `projects.archive` | أرشفة/إيقاف المشروع | Moving project to status متوقف/ملغى and blocked/hold_reason (migrations 2026_09_10_000002). | projects.e — day-to-day edits vs governance close/hold. | sensitive | projects.e (status field) |
| `projects.export` | تصدير قائمة المشاريع | m.export CSV of projects (may include budget/cost columns if visible). | global exp flag — currently one org-wide flag governs export of every module. | sensitive | exp flag |

## الموارد البشرية

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `hr.salary.view` | عرض الرواتب والبدلات | Visibility/serialization of salary + allow (allowances) on the profile, list, export, API shape(), and payroll-derived displays. Checked in the field-mode/serialization layer so it covers web + API + mobile at once. | hr.v — viewing the employee record (name/dept/title/status) does NOT imply seeing compensation. Currently no positive gate: salary is visible to any hr:v unless owner sets field_rules hr.salary=hide on every role. | sensitive | field_rules hr.salary + hr.allow (invert current opt-out hide → positive grant; owner keeps by is_owner bypass) |
| `hr.finpii.view` | عرض البيانات المالية والتعريفية (IBAN/الرقم المدني) | Visibility of iban (bank transfer account) and civil_id (national ID) fields across profile/list/export/API/search. | hr.v and hr.salary.view — bank account + national ID are identity/financial PII with theft/impersonation impact distinct from pay amount and from basic record view. Default-open today. | sensitive | field_rules hr.iban + hr.civilId (opt-out) → positive |
| `hr.iddocs.view` | عرض وثائق الهوية (الإقامة والجواز) | Visibility of iqama, iqama_exp, passport, pass_exp. | hr.v, and from the expiry-alert surface — the team directory ALREADY hides iqamaExp/passExp via field_mode for alerts, proving the doc numbers are a distinct privilege from seeing that an employee is a person. Default-open today. | sensitive | field_rules hr.iqama/hr.iqamaExp/hr.passport/hr.passExp (opt-out) → positive |
| `hr.contact.view` | عرض بيانات الاتصال الشخصية | Personal phone, personal email, nationality, emergency contact. | hr.v — a directory reader needs name/dept/title but not home phone / next-of-kin. Default-open today; also reachable via ?q= (Searchable respects hide, so this key would also close the search oracle). | sensitive | field_rules hr.phone/hr.email/hr.nationality/hr.emergency (opt-out) → positive |
| `hr.attendance.view` | عرض حضور الموظف داخل الملف 360 | The attendance-derived columns (time_in, time_out, physical presence, late) surfaced by the 360 'reports' tab via Employee360::dailyReports. | updates.v (reviewing daily work reports) — currently the reports tab shows raw attendance times gated ONLY by updates:v, so a report reviewer sees attendance even when denied the attend module. Should map to the EXISTING attend.v, not a brand-new key. | sensitive | attend.v (already exists — reuse it; require attend:v for the time_in/time_out/physical fields in dailyReports) |
| `hr.export` | تصدير ملفات الموظفين | Emitting employee rows to CSV (m/hr/export + bulk export). | hr.v and the single global 'exp' flag — module view ≠ bulk extraction, and one global flag currently unlocks export of ALL 85 modules (whole employee table incl. every field visible to the role). A per-module export key lets salary/PII export be granted separately from viewing. | sensitive | flag exp (holders keep hr.export during migration) |

## الحضور/التقارير

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `reports.view_team` | عرض تقارير الفريق اليومية (المحتوى) | مركز التقارير (reports.index)، تفصيل اليوم (reports.day)، API reports/daily، شاشة «فريقي اليوم» — محتوى بند العمل done/problems/next وحالة امتثال كل الموظفين | attendance.monthly.view: الشهري يعرض حالة الحضور والساعات فقط دون نص التقرير؛ view_team يفتح نص التقرير الكامل | sensitive | hr.v (guardTeam + teamDaily + WorkdayController@team) |
| `attendance.monthly.view` | عرض الحضور الشهري | شبكة الحضور الشهرية reports.monthly + سجل الموظف reports.monthly.employee — حضور/انصراف/ساعات/تسميات حالة الامتثال المحتسبة، بلا نص تقرير | reports.view_team: لا يكشف done/problems/next؛ للمحاسب لا للموارد البشرية | sensitive | attend.v \|\| hr.v (guardMonthly) |
| `attendance.export` | تصدير الحضور الشهري (CSV) | reports.monthly.export — تنزيل صف لكل موظف×يوم لكل الشركة دفعة واحدة | attendance.monthly.view: العرض على الشاشة ≠ الاستخراج الجماعي القابل للتسريب؛ مفتاح مستقل يسمح بحجب التصدير مع إبقاء العرض | sensitive | attend.v \|\| hr.v (يرث من attendance.monthly.view للهجرة ثم يُنزع للأدوار التي لا يجب أن تصدّر) |
| `reports.review` | مراجعة التقارير اليومية (قبول/تنقيح/إعادة فتح) | طابور المراجعة reports.review + الإجراء reports.review.act — تغيير review_status وإشعار الكاتب وإعادة فتح تقرير مقبول | reports.view_team: العرض لا يقرر؛ المراجعة تكتب حالة وتُشعر الموظف | sensitive | العرض: hr.v \|\| updates.e (canReviewAny)؛ القرار: hr.e \|\| (updates.e + مشروع ضمن النطاق) (canReview) |
| `reports.review.decide` | البتّ في مراجعة التقرير | reports.review.act فقط (POST) — الفعل الكاتب منفصلاً عن رؤية الطابور | reports.review (view): يفصل من يرى الطابور عمّن يبتّ فيه؛ يجعل قيد النطاق صريحاً بدل فرع hr.e غير المنطَّق | sensitive | hr.e \|\| updates.e+project (وهو الفرع الذي يجب إضافة تنطيق الشركة إليه — انظر F2) |
| `attendance.finalize` | ختم الأثر الفعّال للحضور | reports.finalize — كتابة compliance_outcome/note على صف الحضور (غياب/معذور/حاضر) بأثر على كشف الراتب | reports.review: قبول التقرير لا يمس الساعات ولا الأثر الفعّال؛ الختم يعدّل الحالة المحتسبة للراتب | owner_only | hr.e \|\| owner (يجب أن يمر عبر hub_scope الصارم لا hub_company_scope — انظر F1) |

## الماليّة

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `projects.fin.view` | عرض ماليّة المشروع (ميزانية/تكلفة/إيراد/هامش) | Seeing project budget, cost, revExp, and the derived P&L/margin (CostController P&L, projects.blade $pcFin block, and the budget/cost/revExp field rows). | projects.v — viewing a project's identity/status/progress/team without exposing its financial figures. | sensitive | new — today only expressible as field_rules {projects:{cost:hide,budget:hide,revExp:hide}} (opt-in) plus the 'monitor' flag for the P&L dashboards. |
| `quotes.fin.view` | عرض تكلفة وهامش العرض (داخليّ) | Internal cost, profit, margin, MRR/ARR on quotes (quotes.blade showInternal card, SalesController internal flag, QuoteController::diff cost/mrr/arr). | quotes.v — reading the quote, its client-facing totals and line prices without the internal cost/margin. | sensitive | field_rules {quotes:{cost:hide}} — promote the existing field-rule convention to a first-class matrix key. |
| `fin.export` | تصدير بيانات المالية (CSV) | CSV export (m.export / m.bulk do=export) of finance modules fin, entries, accounts2, payroll, quotes, purchases, banks, budgets. | fin.v / module.v — viewing on-screen vs bulk-extracting the full financial ledger to a file. | sensitive | flag:exp — split the single org-wide export flag into per-domain export ops (map current exp holders to fin.export during migration). |
| `purchases.approve` | اعتماد أوامر الشراء | PurchaseController::approve state transition (بانتظار الاعتماد → معتمد). | purchases.e — editing/submitting a PO vs authorizing spend on it. | sensitive | flag:approve — replace hub_approver() with a purchases-scoped op; map current approve-flag holders. |
| `payroll.approve` | اعتماد مسيّر الرواتب | PayrollController::approve (locks the run, generates the payroll journal). | payroll.e — generating/adjusting a draft run vs authorizing the whole salary disbursement. | owner_only | flag:approve — a full-payroll approval must not share the same flag as a small PO approval. |
| `quotes.approve` | اعتماد إرسال العروض فوق العتبة | Override of the quote send threshold (amount/discount/margin-floor) in QuoteController::send (QuoteController.php:585 uses hub_flag(approve)). | quotes.e — sending an in-policy quote vs approving one that breaches amount/discount/margin governance. | sensitive | flag:approve. |
| `entries.post` | ترحيل القيود (قفل الدفتر) | EntryController::post — irreversibly locking a journal entry into the ledger. | entries.e — building/editing draft entry lines vs the irreversible posting act. | sensitive | entries.e — carve posting out of the edit op; map current entries.e holders to also get entries.post initially. |
| `fin.pay` | تسجيل الدفعات (تحريك رصيد بنك) | FinController::pay — recording a payment that moves a bank balance and posts a journal. | fin.e — editing an invoice's fields vs settling it against cash/bank. | sensitive | fin.e (+banks.e already required) — optional split so bookkeepers can edit invoices without moving cash. |
| `banks.figures.view` | عرض IBAN والرصيد البنكي | iban and balance fields on bank_accounts. | banks.v — knowing an account exists / its name & currency vs seeing its IBAN and live balance. | sensitive | new — currently either field_rules hide (opt-in) or nothing; IBAN is type 'text' not 'sec' so it is not even reveal-gated. |
| `changeorders.fin.view` | عرض قيم أوامر التغيير (value_delta/cost_delta) | value_delta and especially the internal cost_delta on change orders (also surfaced in project meta.baseline). | changeorders.v — seeing a change order's scope/status vs its internal cost delta. | sensitive | field_rules {changeorders:{cost_delta:hide}}. |
| `payroll.salary.view` | عرض قيم الرواتب | Per-employee base/allow/net figures on payroll runs and lines. | payroll.v (list of runs) — vs the individual salary amounts inside a run. | owner_only | field_rules on payroll line figures (currently none) — high-sensitivity, worth a dedicated key. |

## الأصول/النقاط

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `endpoints.view` | عرض أسطول النقاط الطرفية | Read of the device fleet: EndpointCentreController index/show, and the /m/endpoints generic list/show/export. | The `monitor` analytics flag (which also unlocks salary/cost/performance dashboards) and from command-issuing authority. | sensitive | flag:monitor (fleet-read half of it) |
| `endpoints.command` | إصدار أوامر تشغيلية للأجهزة | Issuing the non-critical commands refresh_inventory, refresh_posture, apply_policy (EndpointProtocolController::issue). | endpoints.view (read) and endpoints.command.critical (isolate/lock). Currently NOT separable — any monitor-flag holder can push apply_policy with no step-up. | owner_only | flag:monitor |
| `endpoints.command.critical` | عزل/قفل جهاز طرفي | The destructive isolate and lock commands (EndpointCommand::STEPUP_TYPES). | endpoints.command (operational). Should be grantable independently while retaining the existing step-up. | owner_only | new (currently flag:monitor + hub_require_stepup) |
| `endpoints.enroll` | سك رموز تسجيل الأجهزة | Minting enrollment tokens (EndpointEnrollController::mint) — provisioning a new device trust anchor. | endpoints.view and endpoints.command. Device-provisioning authority currently rides the read-analytics `monitor` flag. | owner_only | new (currently flag:monitor + hub_require_stepup) |
| `endpoints.edit` | تعديل ربط الجهاز (حامل/أصل/محطة) | Editing device.employee_id/asset_id/station_id via /m/endpoints update, and delete/restore of device rows. | endpoints.view. Today this is granted by matrix endpoints:e/d on the generic module with NO monitor gate and outside the audited endpoint flows. | sensitive | endpoints.e (matrix, existing but ungoverned by the fleet invariant) |
| `assets.custody.assign` | إسناد العهدة للأشخاص (تسليم/استرداد/نقل) | CustodyController handover, recover, permit (transfer/exit) — placing an asset in a person's custody and issuing exit/transfer permits. | assets.edit (technical record edit). Someone who should only update a device's specs/status must not automatically be able to hand company assets to employees or authorize their removal. | sensitive | assets.e |
| `assets.status` | تغيير حالة دورة حياة الأصل | CustodyController::changeStatus (Custody::transition) — the locked lifecycle status field. | assets.custody.assign (who holds it) and assets.edit. Technical lifecycle authority. | normal | assets.e |
| `assets.station` | إسناد الأصل لمحطة | CustodyController::assignStation writing the locked station_id. | assets.custody.assign (person custody) and stations.e (managing the seat itself). | normal | assets.e |
| `assets.inventory` | جلسات جرد الأصول | InventoryController freeze/scan/reconcile/close — running physical inventory sessions. | assets.edit and assets.custody.assign. A stock-taker needs to freeze/scan but not hand out custody or edit lifecycle. Currently all ride assets:v/e. | normal | assets.v (read/scan) + assets.e (freeze/reconcile) |

## الملفّات

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `documents.download` | تنزيل بايتات المرفقات | Serving actual file bytes (att.dl, att.view/stream, att.zip, file.show, mobile files download/stream) — currently any record it collapses to the parent module 'v'. | documents.view / module 'v': seeing that an attachment exists in a record's list and its metadata (name, kind, dates) vs actually pulling the file down. | sensitive | parent module 'v' (download currently gated only by record view) |
| `documents.attach` | إرفاق/رفع ملفات على السجل | Uploading files onto a record (att.store, inboxdocs.store chunked path, mobile files attach/upload) — writing to a record's document set. | module 'v' (view): today a read-only viewer can attach files to any record they can merely see; this key would require an explicit attach privilege distinct from viewing. | sensitive | new (currently mapped implicitly from module 'v') |
| `documents.share` | مشاركة مستند خارج النظام | Creating external, unauthenticated share links to arbitrary uploaded files (DataRoom store) and, by extension, sending documents to external parties. | flag:secrets (RISKY) which today gates the whole Data Room bundle; and contracts 'a' for esign sends. A dedicated share key separates 'may exfiltrate a file to an outside link' from other secrets-flag capabilities. | owner_only | flag:secrets |
| `documents.delete` | حذف مرفقات الغير | Deleting attachments a user did not upload (att.destroy beyond own uploads). | module 'e' (edit record): today deleting others' attachments piggybacks on module edit; a viewer who uploaded may already delete own. This isolates 'purge any document on this record'. | sensitive | module 'e' |

## الإدارة/الأمن

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `vault.reveal` | كشف قيمة السرّ من الخزنة | فكّ تشفير وعرض النصّ الصريح لحقلٍ من نوع sec عبر ModuleController::revealSecret. اليوم يُمنح بأيٍّ من علَمَي secrets أو copySec (hub_copy_secrets) — فلا فصل بين «الاطلاع» و«النسخ». | dataroom.share.create (نشر مستند لطرفٍ خارجي عبر رابطٍ عامّ) — كشفٌ داخليّ مقابل نشرٍ خارجيّ؛ وأيضاً distinct من مجرّد عرض بطاقة السجل بلا كشف السرّ | sensitive | secrets flag (وحالياً copySec أيضاً يمنحه — يجب أن يُلغى كمسار كشف) |
| `dataroom.share.create` | إنشاء رابط مشاركة خارجي عامّ | سكّ رابطٍ مؤقّتٍ غير مصادَق (token عامّ) يبثّ مستنداً حسّاساً خارج النظام (DataRoomController::store). دائرة الانكشاف أوسع من الكشف الداخليّ لأنّ الوجهة الإنترنت العامّ. | vault.reveal — النشر للعموم مقابل العرض الداخليّ المسجَّل | sensitive | secrets flag (مجموعٌ اليوم مع الكشف الداخليّ تحت العلَم نفسه) |
| `users.credentials.reset` | إعادة تعيين كلمة المرور وإطفاء التحقّق بخطوتين لحسابٍ آخر | UserController::update (تعيين كلمة مرور من الإدارة + إنهاء جلسات) و twofaOff — فعلٌ ملاصقٌ لاختطاف الحساب، أوسعُ أثراً من تحرير ملفٍّ عاديّ. | users flag العامّ (إنشاء/تحرير/حذف/استعادة) — إعادةُ الاعتماد ليست كتحرير الاسم أو الوظيفة | sensitive | users flag (مجموعٌ اليوم؛ محروسٌ فعلاً بـStaff::mayTouch + step-up لكن غير قابلٍ للفصل) |
| `endpoints.enroll` | سكّ رموز تسجيل الأجهزة (MDM) | EndpointEnrollController::mint — يُصدر رمز تسجيلٍ يسمح لجهازٍ بالانضمام لإدارة الأجهزة. اليوم بوّابتُه monitor (لوحاتٌ تحليلية) لا العلَم المخصّص mobile. | monitor flag (قراءة اللوحات) — سكُّ اعتمادِ جهازٍ فعلٌ كتابيّ حسّاس لا قراءة | sensitive | mobile flag (الأنسب) أو owner؛ حالياً monitor flag |
| `security.monitor.view` | عرض مراكز الرصد الأمنيّ (النتائج/الهوية/الجلسات/الأجهزة/العناوين) | القراءة المطموسة المنطَّقة لمراكز الأمان (SecurityController::findingsReadGate). جمهورُها فريقُ الأمان (SOC). | analytics.view (لوحات الأداء والتكلفة والطاقة) — كلاهما تحت monitor اليوم رغم اختلاف الجمهور والحساسية | sensitive | monitor flag (مجموعٌ اليوم مع اللوحات التحليلية العامّة) |

## بوّابة العميل

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `fin.cost.view` | عرض تكاليف ومشتريات المالية | إظهار مستندات fin_documents ذات الأنواع الشرائية/التكلفة وأعمدة التكلفة (الإجمالي/المدفوع على المشتريات) في /m/fin وmobile.resource fin — ما يعزله قارئُ البوّابة عمداً (CLIENT_INVOICE_KINDS). | fin.view (فواتير المبيعات والمقبوضات — الآمنةُ للعميل) | sensitive | new — لا مفتاح مصفوفةٍ حاليّاً؛ العزل موجودٌ فقط في قارئ البوّابة (kind filter) لا في محرّك الوحدات |
| `projects.fin.view` | عرض ماليّة المشروع | إظهار أعمدة تكلفة/ميزانية/إيراد-مصروف المشروع (cost/budget/rev_exp) على شاشة المشروع الداخليّة، مقابل بيانات المشروع التشغيليّة. | projects.view (اسم المشروع وحالته وتقدّمه — يراه العميل في البوّابة) | sensitive | monitor flag (التكاليف اليوم محكومةٌ بعلم المراقبة في hub_top_links) |
| `clients.members.manage` | إدارة أعضاء بوّابة العميل | دعوة/سحب أعضاء بوّابة العميل ومنح دور Client Owner (خلق حسابات دخولٍ خارجيّة). | clients.e (تحرير سجلّ العميل في الـCRM) | sensitive | clients 'e' (اليوم البتُّ نفسُه يحرّر الـCRM ويمنح دخولَ البوّابة معاً) |
| `companies.list.view` | استعراض قائمة الشركات في مبدّل السياق | إرجاع أسماء الشركات وعددها في mobile.context/bootstrap. حاليّاً غيرُ منطَّقٍ لحساب العميل (companies بلا عمود عميل). | companies.view (فتح سجلّ شركةٍ بعينه) | sensitive | new — يجب حجبُه بنيويّاً عن hub_is_client بدل الاعتماد على غياب عمودِ العميل |

## 360/العلاقات

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `projects.assets.view` | عرضُ أصولِ المشروع (تخصيصاتها) | قسمُ «🖥️ أصولُ المشروع» في مركزِ المشروع 360 (partials/project_assets) وعدّادُه — رؤيةُ الأصولِ المخصَّصةِ تشغيليّاً للمشروع (الكود/الاسم/النوع/الحالة). | projects.v (رؤيةُ المشروعِ نفسِه) — رؤيةُ المشروعِ لا تعني رؤيةَ عتادِه المخصَّص؛ اليومَ القسمُ يظهر لأيِّ داخليٍّ بلا بوّابة. | sensitive | assets.v (رحّل: ابنِ القسمَ فقط حين hub_can(assets,v)+hub_scope(assets)) — ليس مفتاحاً جديداً لزاماً للإصلاح، بل لمن أراد فصلَ «رؤيةِ أصولِ مشروعٍ بعينه» عن «سجلِّ الأصولِ العام». |
| `assets.projects.view` | عرضُ مشاريعِ الأصل (تخصيصاته) | قسمُ «📁 مشاريعُ الأصل» في مركزِ الأصل 360 (partials/asset_projects) وعدّادُه — رؤيةُ المشاريعِ التي خُصِّص لها الأصلُ (أسماؤها وروابطُها). | assets.v (رؤيةُ الأصلِ نفسِه) — رؤيةُ الأصلِ لا تعني رؤيةَ المشاريعِ التي يخدمها؛ اليومَ التبويبُ يظهر لأيِّ داخليٍّ بلا projects.v. | sensitive | projects.v (رحّل: ابنِ القسمَ فقط حين hub_can(projects,v) + كلُّ مشروعٍ عبرَ hub_scope(projects)) — يطابقُ Asset360::overview وStation360::projectContext أصلاً. |
| `projects.fin.view` | ماليّةُ المشروع (الربحيّةُ والتكلفةُ والميزانية) | تبويبُ «💰 المالية» في مركزِ المشروع 360 (لوحةُ P&L/الإيراد/التكلفة/الهامش) — hub_project_pl. | projects.v — رؤيةُ المشروعِ ≠ اقتصادِه؛ يُضبط اليومَ فقط عبرَ حجبِ projects.cost+projects.budget بـfield-mode (رافعةٌ خشنةٌ تُسقط اللوحةَ كلَّها). | sensitive | field_rules projects.cost + projects.budget. |
| `assets.cost.view` | اقتصادُ الأصل (السعرُ والإهلاكُ وكلفةُ الملكية) | تبويبُ «🧮 الدورة» في مركزِ الأصل 360 — السعرُ والقيمةُ الدفتريّةُ وإجماليُّ الصيانة (Asset360::lifecycle price/maint_total). | assets.v — رؤيةُ الأصلِ ≠ أرقامِ تكلفتِه؛ يُضبط اليومَ عبرَ hub_field_mode(assets,price) الذي يحكم السعرَ والصيانةَ معاً بمفتاحٍ واحد. | sensitive | field_rules assets.price. |

## التصدير/الجماعيّ

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `attend.export` | تصدير الحضور | تصدير CSV لسجل الحضور الشهري (reports.monthly.export) وتصدير وحدة attend الجماعي — فصلُ سحبِ بيانات الحضور دفعةً عن مجرّد عرضها على الشاشة | attend.v (عرض شبكة الحضور الشهرية على الشاشة، صفٌّ صفاً بلا ملفٍّ قابل للنقل) | sensitive | exp (يُمنح لحاملي علم exp)؛ لكن reports.monthly.export اليوم غير محكومٍ بـexp إطلاقاً — فالمفتاح هنا يسدّ ثغرةً قائمة لا يهاجرها فقط |
| `hr.export` | تصدير سجلات الموارد البشرية | تصدير CSV لصفوف وحدة hr/الموظفين (تشمل الأعمدة المرئية للدور، وبعضها ملاصقٌ للراتب) | hr.v (عرض/فتح سجل الموظف على الشاشة، حقلاً حقلاً محكوماً بـfield_rules) — الرؤية ليست حقَّ السحبِ الجماعي | sensitive | exp |
| `fin.export` | تصدير المستندات المالية | تصدير CSV للوثائق المالية/القيود/كشوف الرواتب | docs.v / fin module 'v' (عرض المستند مفرداً) | sensitive | exp |
| `<module>.export` | تصدير الوحدة (لكل وحدة) | مفتاح action لكل وحدة يُستشار في hub_exporter/exportBelt، فيصير التصدير قابلاً للمنح لكل وحدة على حدة بدل علمٍ عالميّ واحد يفتح كل الوحدات الخمس والثمانين | module 'v' (القراءة على الشاشة) وعلم exp العالمي (كل-أو-لا-شيء) | normal | exp (كل من يملك exp اليوم يُمنح <module>.export لكل وحدةٍ يراها، ثم يضيّقه المالك) |
| `phones.iccid.export` | تصدير ICCID للشرائح | تصدير جماعي لأعمدةٍ تشمل ICCID (هوية شريحة SIM الخام — خطر انتحال/استبدال) | phones.export (تصدير أعمدة الهواتف غير الـICCID) و phones.v | owner_only | new (اليوم محكومٌ بـhub_require_stepup فقط داخل exportBelt، بلا مفتاح صلاحيةٍ منفصل) |

## التنبيهات/الأتمتة

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `contracts.v / budgets.v / fin.v (إعادة استخدام مفاتيح العرض القائمة في محرّكات الأتمتة)` | رؤية الوحدة تحكم محتوى إشعارها الآليّ | استحقاق تلقّي إشعارات الأتمتة الحاملة لأسماء العقود ومبالغ الميزانيات والفواتير — يجب أن يُشتق من hub_can(module,'v') لا من راية monitor وحدها | راية monitor (تراقب صحة النظام والأعطال) ≠ رؤية سجلات العقود/المالية. الراية لا تمنح مصفوفة الصلاحيات (تعليق AlertEngine:151) | sensitive | موجود بالفعل (v/a/e/d لكل وحدة) — الإصلاح هو ربط الفان-آوت بها لا مفتاح جديد |
| `integrations.manage` | إدارة مركز التكامل | إنشاء/تعطيل/حذف Webhooks الصادرة والواردة، ربط n8n/أودو، إرسال بريد/تلجرام يدوي | حالياً غير قابل للفصل عن owner إطلاقاً (كل الدوال gate()=hub_is_owner). لا سبيل لتفويض مسؤول تكامل غير مالك | owner_only | new — قد يبقى owner-only حوكمةً؛ يُطرح فقط إن أُريد تفويضه لمشرف موثوق مع إبقاء حارس SSRF |
| `push.admin` | إدارة إعدادات الدفع | عرض حالة FCM واختبار الدفع الإداري (mobile.push.admin.*) | مبعثر حالياً: mobile.push.admin.* = owner فقط، بينما شاشة منصّة الجوال = owner\|\|flag:mobile — عدم اتساق في من يدير الدفع | sensitive | flag:mobile (لتوحيد المسارين تحت الراية نفسها) |

## النطاق/IDOR

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `<module>.export (e.g. attend.export, hr.export, fin.export, payroll.export)` | تصدير سجلات الوحدة (CSV) | Whether a role may stream/download a module's rows as CSV (ModuleController::exportBelt, bulk export, and the export belt for phones/ICCID). | Module view (v): today anyone who can view + holds the org-wide 'exp' flag can export EVERY viewable module, so bulk exfiltration of attendance/HR/finance is not separable from exporting tasks. | sensitive | exp flag (hub_exporter) — migrate: grant <module>.export to any role currently holding exp, per viewable module. |
| `<module>.secret.reveal (e.g. vault.secret.reveal, banks.secret.reveal)` | كشف الحقول السرّية للوحدة | Server-side reveal of 'sec' type fields via revealSecret and API shape() secret unmasking. | Module view: currently a single org-wide 'copySec' flag unlocks secret reveal across ALL modules at once; viewing a module does not imply, and cannot be scoped to, revealing that module's secrets specifically. | owner_only | copySec flag (hub_copy_secrets) — grant per module to holders of copySec; keep row-level allowed_ids allowlist and step-up on top. |
| `projects.fin.view` | عرض ماليات المشروع (الربحية والتكلفة والميزانية) | Visibility of per-project P&L, labor/cost breakdown, budget-vs-actual and margin (CostController, hub_project_pl consumers). | projects:v (seeing a project, its status, timeline, members) — a project manager can be granted project visibility without its cost/profit figures; today financials are gated only by the broad 'monitor' flag, not a project-financial permission. | sensitive | monitor flag — grant projects.fin.view to roles currently relying on monitor for cost screens. |
| `hr.salary.view` | عرض الراتب والبدلات وبيانات الأجر | Visibility of salary/allow/iban/leaveBal on the HR record and everywhere they surface (module screens, team directory, payroll, cost dashboard average). | hr:v (viewing an employee: name, department, status, documents) — seeing the employee file must be separable from seeing pay. Today salary is hidable only via per-role field_rules{hr:{salary:hide}} (a UI field toggle, not a matrix permission) and that toggle is bypassed by the cost dashboard's team-average (see findings). | owner_only | field_rules hr.salary/allow/iban 'hide' + payroll gating — promote to a first-class key and make cost/team-directory/payroll consult it uniformly. |
| `updates.review` | مراجعة/اعتماد تقارير العمل اليومية | Approving, reopening, or returning submitted daily work updates (ReportReview::canReview, review lock enforcement). | updates:e / hr:e — the ability to review-and-lock a subordinate's report is currently conflated with generic edit-any-work-update or hr-edit; a team lead who should only approve their project's reports cannot be granted review without broader edit rights. | normal | hr.e OR (updates.e + project manager) in ReportReview::canReview — map to updates.review, keeping the project-manager path. |
| `endpoints.command.issue` | إصدار أوامر إدارة الأجهزة (MDM) | Issuing device commands (isolate/lock/scan…) from EndpointProtocolController::issue and the MDM console. | endpoints:v (viewing the fleet) and the broad 'monitor' flag — MDM operators who may act on devices are today indistinguishable from any monitor-flag holder; there is no assignable device-operator permission separate from fleet oversight. | owner_only | monitor flag / owner (EndpointProtocolController:431) — grant endpoints.command.issue to designated MDM operators; keep company-scope + step-up. |

## المطابقة

| المفتاح | التسمية | يحكم | متمايز عن | الحساسية | إرث |
|---|---|---|---|---|---|
| `custody.approve` | اعتماد العهدة: إصدار التصاريح والتسويات والعكس | Custody permit issuance, settlement, and reversal (EmployeeCustodyController permit/settlement/reverse). | custody.e (routine advance/charge/expense/deduction/transfer/correct postings). | owner_only | already consumed as hub_can('custody','approve'); map new grant from custody:e holders who also hold the approve flag, or migrate from is_owner |
| `attend.export` | تصدير الحضور والانصراف (CSV) | reports/monthly/export CSV stream and any bulk attendance export. | attend.v / hr.v (viewing monthly attendance on screen). | sensitive | new — currently gated only by attend:v\|\|hr:v; interim map from the global exp flag until per-module export exists |
| `module.export` | تصدير بيانات الوحدة (CSV) | Per-module CSV export, replacing the single global exp flag so HR/fin/secrets exports can be granted independently of a task-list export. | module.v (viewing rows) and the global exp flag (all-or-nothing across every module). | sensitive | exp flag (holders of exp gain module.export on every module they can view during migration) |
| `collab.oversight` | رقابة الاتصالات (قراءة للامتثال) | OversightController index/show — compliance read of all conversations/DMs in scope. | owner bypass and ordinary conversation participation; today it is a role-NAME match, not a permission. | owner_only | migrate from the current setting('collab.oversight_role') role-name match to this matrix key (or a dedicated flag); keep stepup+reason+scope |
| `monitor.finance` | لوحات المالية والتكلفة | costs, service-costs, financial performance cards, CEO-style finance analytics. | monitor.ops (system/perf monitoring) — today one 'monitor' flag unlocks both plus digital-assets and sales. | sensitive | monitor flag |
| `monitor.ops` | مراقبة التشغيل والأداء | performance, capacity, recommendations, impact map, app-quality, control glance, alert center read. | monitor.finance (money dashboards) and secrets (digital-assets vault). | sensitive | monitor flag |
| `dassets.view` | عرض الأصول الرقمية (بيانات اعتماد/مفاتيح) | digital-assets center (🔐) currently opened by the monitor flag. | monitor (analytics) and secrets (vault reveal) — a credentials surface should not ride an analytics flag. | owner_only | monitor flag (interim); target: secrets flag or new key |
