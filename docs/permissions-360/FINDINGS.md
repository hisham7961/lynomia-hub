# نتائجُ التدقيق — Permissions 360 (المرحلة 0)

> 83 نتيجةً من 20 ورشة. **CONFIRMED** = تُحقِّق منها يدويّاً بقراءةِ الكود هذه الجلسة؛ **REPORTED** = من الوكيل، تُحقَّق قبل إصلاحها (إثباتٌ لا ادّعاء).

الخطورة: P0=0 · P1=2 · P2=38 · P3=43

| # | الخطورة | النوع | المجال | الحالة | الموضع | الدليل | الإصلاح المقترح |
|---|---|---|---|---|---|---|---|
| 10.1 | P1 | idor | الحضور/التقارير | ✅ CONFIRMED | app/Http/Controllers/Web/ReportsController.php:196-197 (finalize) | $row = Attendance::whereNull('deleted_at')->whereKey($id)->firstOrFail(); abort_unless(hub_company_scope(Attendance::query(),'attend')->whereKey($row->id)->exists(), 404). hub_company_scope (helpers.p | استبدل الحارس بالتنطيق الصارم كما في day()/monthlyEmployee(): abort_unless(hub_scope(Attendance::query(),'attend')->whereKey($row->id)->exists(), 404); (مع hub_company_scope فوقه ا |
| 15.1 | P1 | bypass | بوّابة العميل | ✅ CONFIRMED | app/Http/Controllers/Web/PortalController.php:30-35 (employee) + app/Http/Middleware/PortalGuard.php:44-48,97 (NAME_ALLOW 'portal.*') | PortalGuard whitelists the pattern 'portal.*', which matches the route name 'portal.employee' (GET employee/{id}). PortalController@employee has NO hub_is_client block — only `abort_unless(hub_can($u, | Add `if (hub_is_client(auth()->user())) abort(404);` at the top of PortalController@employee (and @me if it should be internal-only), OR narrow PortalGuard::NAME_ALLOW from the bro |
| 01.1 | P2 | over_broad | نواة التوثيق | REPORTED | app/Support/helpers.php:1044 (hub_exporter) enforced at app/Http/Controllers/Web/ModuleController.php:796 (exportBelt) | abort_unless(hub_exporter(), 403, 'التصدير يتطلب صلاحية'); and hub_exporter reads a single flag: return hub_flag($user,'exp'). No module dimension anywhere in the export path. | Gate export per module: abort_unless(hub_can($u,$module,'export') \|\| (fallback) hub_exporter(),403) and expose a <module>.export named op. Field-mode/sec masking already strip sens |
| 01.2 | P2 | over_broad | نواة التوثيق | REPORTED | app/Support/helpers.php:964 (hub_monitor) — 38 call sites incl. app/Http/Controllers/Web/DigitalAssetsController.php:19, CostController.php:14, PerformanceController.php:34 | hub_monitor() = hub_flag($user,'monitor'). hub_top_links (helpers.php:417-448) gates perf, sales, costs, kpis, capacity, recs, impact, appq, servicecosts AND dassets on the same $mon = owner\|\|monitor. | Decompose monitor into named keys (perf.view / costs.view / digital.view / kpis.view). One flag currently fuses individual-performance data, org financial/cost data, and the digita |
| 01.3 | P2 | over_broad | نواة التوثيق | REPORTED | app/Support/helpers.php:972 (hub_approver) — app/Support/ApprovalService.php:61, app/Http/Controllers/Web/PayrollController.php:143, app/Http/Controllers/Web/PurchaseController.php:108 | hub_approver() = hub_flag($user,'approve'). PayrollController: abort_unless(hub_approver(),403,'اعتماد المسيّرات...'); PurchaseController: abort_unless(hub_approver(),403,...); ApprovalService: if(! h | Split into per-domain decide keys (approvals.decide.payroll, approvals.decide.purchases, ...). 'Deciding' is distinct from 'viewing' and cross-domain decide authority should not be |
| 01.4 | P2 | bypass | نواة التوثيق | REPORTED | app/Http/Controllers/Web/RoleController.php:311-319 (data() matrix build) vs app/Support/helpers.php:295 (hub_can reads any op) | hub_can: matrix[$module][$op] for ANY $op (fine-grained supported at read). But data(): $matrix = $matrixSent ? [] : ...; foreach(array_keys(hub_modules()) as $mod){ foreach(array_keys($this->ops) as  | Before shipping any fine-grained (module,action) permission, extend the editor's op set (data() + fromTemplate + a form section), preserve unknown op keys on rebuild (merge instead |
| 01.5 | P2 | leak | نواة التوثيق | REPORTED | app/Support/helpers.php:141-164 (hub_scope) | hub_scope applies whereIn only if a column exists: if($col=hub_project_col($module))..., if(($cids=hub_company_ids())!==null && ($ccol=hub_company_col($module)))..., same for client. A module whose ta | Make scoping explicit per module in config/hub.php (e.g. scope: company\|client\|project\|global) and abort/deny for isolated users on modules declared scopable-but-missing-column, or |
| 02.1 | P2 | raw_role | مسارات الويب | ✅ CONFIRMED | app/Http/Controllers/Web/OversightController.php:51-60 (isOversightOfficer) gating oversight.index/oversight.show; setting key collab.oversight_role | public static function isOversightOfficer(?User $user=null): bool { ... $roleName = trim((string) setting('collab.oversight_role','')); if ($roleName==='') return false; return trim((string) $user->ro | Replace role-name matching with an assignable permission that lives in the single engine: a role.flags key (e.g. 'oversight') read via hub_flag, or a matrix key collab.oversight.vi |
| 03.1 | P2 | bypass | API/الجوّال | REPORTED | app/Http/Controllers/Api/ReportsApiController.php:62-71 (teamDaily, route api.v1.reports.daily) | public function teamDaily(Request $r){ if (hub_is_client($r->user())) abort(404); abort_unless(hub_can($r->user(), 'hr', 'v'), 403); ... $emps = hub_company_scope(hub_scope(Employee::query(),'hr'),'hr | Add `abort_unless((request()->attributes->get('api_token')?->allows('hr','v')) ?? true, 403, 'نطاق هذا المفتاح لا يشمل hr:v');` (mirror V1Controller::tokenAllows) before returning  |
| 03.2 | P2 | bypass | API/الجوّال | REPORTED | app/Http/Controllers/Api/AssetProjectApiController.php:27-33 (gate) used by assign(81)/end(99)/projectAssets(45)/assetProjects(63) | private function gate(): void { $u = auth()->user(); abort_unless($u,403); abort_if(hub_is_client($u),404); abort_unless(hub_can($u,'assets','e') && hub_can($u,'projects','v'),403); } — no reference t | In gate(), also require `$t=request()->attributes->get('api_token'); abort_unless(!$t \|\| ($t->allows('assets','e') && $t->allows('projects','v')), 403, 'نطاق المفتاح لا يشمل هذه ال |
| 03.3 | P2 | over_broad | API/الجوّال | REPORTED | app/Http/Middleware/MobilePortalGuard.php:83-87 (MODULE_ALLOW) vs app/Support/ClientPortalData.php:134-142 (invoiceRows kind+column curation); reached via MobileResourceController::listRecords/showRecord, MobileSyncController::sync, and V1Controller::apiIndex/apiShow for module 'fin' (also 'projects'/'engagements') | MobilePortalGuard::MODULE_ALLOW=['engagements','projects','fin'] lets a client account reach the GENERIC CRUD/sync/api-v1 path for these modules; resolveApi only checks in_array($module, MODULE_ALLOW) | For account_type=client, route the generic CRUD/list/show/sync of engagements/projects/fin through the same curated projection ClientPortalData enforces (explicit column select + k |
| 04.1 | P2 | bypass | التنقّل/الاكتشاف | REPORTED | resources/views/layouts/app.blade.php:145 + config/hub_ia.php:463 (admin_bar guard) + app/Support/InformationArchitecture.php:106,175 | Admin bar renders only if `$isOwner \|\| hub_flag(users) \|\| hub_flag(audit) \|\| hub_secrets()` (app.blade.php:145); the IA 'admin_bar' guard is the identical set (InformationArchitecture.php:106) and is  | Derive admin-bar/administration-domain visibility from 'any hub_admin_links(user) link with ok===true' (or widen admin_bar to add hub_monitor and hub_flag('mobile')) so the three s |
| 06.1 | P2 | count_leak | اللوحات/العدّادات | REPORTED | app/Support/KpiCentre.php:129 (hub_metric_bulk_series) rendered at resources/views/kpis/index.blade.php:127-130; gate app/Http/Controllers/Web/KpiController.php:20 (hub_monitor only) | KpiCentre::rows() computes each KPI current value via hub_kpis($user) (scoped by hub_kpi_metric->hub_scope+hub_can), but its 'trend' comes from hub_metric_bulk_series('kpis',ids,['value']) which reads | Either call hub_org_analytics_guard() in KpiController::gate() (matching Performance/Capacity/Workforce/Cost::services), or blank the trend for readers where hub_company_ids()!==nu |
| 06.2 | P2 | leak | اللوحات/العدّادات | REPORTED | app/Support/helpers.php:1802-1810 (hub_org_analytics_guard) | hub_org_analytics_guard() does: abort_if(hub_company_ids()!==null,403); abort_if(hub_client_ids()!==null,403); with NO abort_if(hub_scoped()). A role with scope='proj' and no companies/clients list ha | Add abort_if(hub_scoped(),403,'هذه اللوحة على مستوى المنشأة — غير متاحة لحسابٍ محدود النطاق بمشاريعه') to hub_org_analytics_guard(), so the guard defends project isolation in depth |
| 05.1 | P2 | field_leak | واجهة/أزرار | REPORTED | app/Http/Controllers/Web/FileController.php:157-200 (mayRead) / :22-27 (show); rendered from resources/views/partials/_display.blade.php:40-51 | mayRead() authorizes file/img field downloads with only `if (! hub_can($u, $mk, 'v')) continue;` then `hub_scope($q,$mk)->exists()` — it never consults hub_field_mode(). Contrast ModuleController::sec | In mayRead(), after matching the owning record+module, resolve which file/img field column the path sits in and reject when hub_field_mode($u,$module,$fieldKey)==='hide' (mirror th |
| 08.1 | P2 | idor | التعاون | REPORTED | app/Support/ClientPortalData.php:175 (conversationDetail) and :159 (conversationRows); enabling gap in app/Http/Controllers/Web/ConversationController.php:551 (addMember) | conversationDetail(): return Conversation::whereIn('id',$memberIds)->whereIn('kind',CLIENT_CONV_KINDS)->whereIn('audience',CLIENT_AUDIENCES)->whereNull('archived_at')->whereNull('deleted_at')->findOrF | (1) In ClientPortalData::conversationRows and conversationDetail add the same fail-closed client filter used elsewhere: ->where(fn($w)=>$w->whereIn('client_id',$clientIds)->orWhere |
| 07.1 | P2 | leak | المشاريع | REPORTED | resources/views/modules/custom/projects.blade.php:36-40 (and 331-380) | $pcLogs = $isCli ? collect() : \Illuminate\Support\Facades\DB::table('work_updates')->whereNull('deleted_at')->where('project_id',$row->id)... ->get(['id','created_by','task_id','done','hours','progre | Gate the activity tab (and its count badges + التقارير/التقدّم section) behind hub_can(auth()->user(),'updates','v'); fold the tab out of $pcTabs when the reader lacks updates.v, m |
| 07.2 | P2 | over_broad | المشاريع | REPORTED | resources/views/modules/custom/projects.blade.php:23,297-328 + app/Support/helpers.php:2228 (hub_project_pl) | $pcFin = $pcSee('cost') && $pcSee('budget'); $pcSee = fn($key)=>!$isCli && hub_field_mode($u,'projects',$key)!=='hide'; — field-mode defaults to '' (visible), so a role with matrix.projects.v=1 and no | Introduce projects.fin.view as a named (module,action) key in role.matrix and gate the finance tab + KPI TCV/current-value rows on hub_can($u,'projects','projects.fin.view') in add |
| 09.1 | P2 | leak | الموارد البشرية | REPORTED | app/Support/Employee360.php:132-156 (dailyReports) + app/Http/Controllers/Web/PortalController.php:67,90 (reports tab = module 'updates') | dailyReports gates on `if (! hub_can($u, 'updates', 'v')) return [];` (Employee360.php:134) but its rows include `'physical' => $c['labels']['physical'], 'time_in' => $c['time_in'], 'time_out' => $c[' | In Employee360::dailyReports, additionally require hub_can($u,'attend','v') before including time_in/time_out/physical/late (null them when attendance is not permitted, keeping the |
| 09.2 | P2 | over_broad | الموارد البشرية | REPORTED | config/hub.php:5504,5510,5422,5423,5516,5522,5529,5535 (hr fields) + app/Support/helpers.php:1721-1723 (hub_field_mode default) + app/Http/Controllers/Web/PortalController.php:33 | hub_field_mode returns '' (fully visible) whenever a role has no explicit field_rules entry: `$mode = $rules[$module][$fieldKey] ?? ''` (helpers.php:1721). PortalController::employee gates the whole 3 | Introduce positive fine-grained keys (hr.salary.view, hr.finpii.view, hr.iddocs.view, hr.contact.view) consulted inside hub_field_mode/hub_visible_fields/shape() so the default pos |
| 10.2 | P2 | bypass | الحضور/التقارير | REPORTED | app/Support/ReportReview.php:45 (canReview) | if ($owner) return true; if (hub_can($actor,'hr','e')) return true;  <- لا تنطيق شركة/مشروع على $w. بينما فرع المشروع التالي: if($w->project_id && hub_can($actor,'updates','e')) return hub_scope(proje | أضِف تنطيق الشركة لفرع hr.e قبل return true: تحقّق أن company_id لـ$w (أو شركة كاتبه created_by) ضمن hub_company_ids($actor)، وإلا false — نظير فحص المشروع في فرع updates.e. لا تعت |
| 11.1 | P2 | field_leak | الماليّة | REPORTED | app/Http/Controllers/Api/V1Controller.php:539-566 (shape) + written at app/Http/Controllers/Web/QuoteController.php:431-436 and app/Http/Controllers/Web/ChangeOrderController.php:44-49 | shape() strips only fields DECLARED in $def['fields']: `foreach ($def['fields'] as $f){ $hidden = hub_field_mode(...)==='hide'; ... if($hidden\|\|$secret){unset($arr[$f['col']]);} }`. The `meta` column  | In shape(), after building $arr, whitelist output to declared field columns plus id/timestamps (or explicitly unset 'meta' and any non-field column), instead of only unsetting decl |
| 11.2 | P2 | over_broad | الماليّة | REPORTED | app/Support/helpers.php:1044-1047 (hub_exporter) + app/Http/Controllers/Web/ModuleController.php:794-815 (exportBelt), :881-891 (bulk export) | `function hub_exporter($user=null){ return hub_flag($user??auth()->user(),'exp'); }` and exportBelt gates every CSV with `abort_unless(hub_exporter(),403,...)`. There is no per-module export permissio | Replace the single hub_exporter() gate with a per-module export op (e.g. hub_can(module,'export')) or a module-sensitivity tier, keeping the freeze switch and step-up. Migrate curr |
| 11.3 | P2 | over_broad | الماليّة | REPORTED | app/Http/Controllers/Web/PurchaseController.php:106-112 + app/Http/Controllers/Web/PayrollController.php:141-143 + app/Http/Controllers/Web/QuoteController.php:585 (hub_approver / hub_flag('approve')) | hub_approver()=hub_flag('approve') (helpers.php:970-975). PurchaseController::approve: `abort_unless(hub_approver(),403,...)`. PayrollController::approve: `abort_unless(hub_approver(),403,...)`. Quote | Introduce module-scoped approval ops (purchases.approve, payroll.approve, quotes.approve) resolved via hub_can, mirroring the existing custody.approve named op (EmployeeCustodyCont |
| 12.1 | P2 | bypass | الأصول/النقاط | REPORTED | config/hub.php:5044 (endpoints module reg) + app/Http/Controllers/Web/ModuleController.php:15-19 (resolve) vs app/Http/Controllers/Web/EndpointCentreController.php:32-36 (guard) | EndpointCentreController::guard() asserts the fleet is monitor-only: `abort_unless(hub_is_owner() \|\| hub_monitor(), 403, 'مركزُ النقاط الطرفية للمالك أو المراقب')` and its docblock states 'الداخليُّ ب | Reconcile the two doors: either add an `if ($module==='endpoints') abort_unless(hub_is_owner()\|\|hub_monitor(),403)` shim in ModuleController for the fleet module, or (preferred) re |
| 12.2 | P2 | over_broad | الأصول/النقاط | REPORTED | app/Http/Controllers/Web/ModuleController.php:444 (m.update) + config/hub.php:5100-5120 | Device rows are meant to be mutated only through the signed protocol and audited flows (hostname/os/company_id/pubkey_fp/status/last_heartbeat are all 'locked'=>true in config/hub.php:5067-5145). But  | Mark employee_id/asset_id/station_id 'locked'=>true on the endpoints module (they already are on the Custody/enroll side), or route their edits through an audited endpoint action;  |
| 12.3 | P2 | raw_role | الأصول/النقاط | REPORTED | app/Http/Controllers/Api/EndpointProtocolController.php:431 + app/Http/Controllers/Api/EndpointEnrollController.php:37 | Device command issuance and enrollment-token minting authorize on the coarse RISKY `monitor` flag: issue() does `abort_unless(hub_is_owner() \|\| hub_monitor(), 403)` and mint() the same. hub_monitor()  | Introduce endpoints.command / endpoints.command.critical / endpoints.enroll matrix keys (hub_can reads any op string in the same role.matrix) and gate issue()/mint() on them instea |
| 12.4 | P2 | over_broad | الأصول/النقاط | REPORTED | app/Http/Controllers/Web/CustodyController.php:38-44 (asset() op 'e') used by handover:175, recover:201, permit:294, changeStatus:228, assignStation:259, saveSpecs:160; app/Http/Controllers/Web/InventoryController.php:37-40 | A single `assets:e` op is the gate for ALL of: custody-assignment authority (handover/recover — placing a device in a named person's hands; permit — transfer/exit authorization that notifies a recipie | Split assets:e into the proposed assets.custody.assign / assets.status / assets.station / assets.inventory keys (all legacy_map from assets.e for safe migration); read them with hu |
| 13.1 | P2 | leak | الملفّات | REPORTED | app/Http/Controllers/Web/FileController.php:224-229 (mayRead inbox_documents branch) | if (Schema::hasTable('inbox_documents') && (hub_can($u,'inboxdocs','v') \|\| hub_can($u,'files','v')) && DB::table('inbox_documents')->whereNull('deleted_at')->where('path',$path)->exists()) { return tr | In mayRead's inbox_documents branch, reuse the same scoping as InboxDocController::scoped(): if hub_company_ids() !== null, require the inbox doc's company_id to be IN that set OR  |
| 14.1 | P2 | over_broad | الإدارة/الأمن | REPORTED | app/Support/helpers.php:1038 (hub_copy_secrets) enforced at app/Http/Controllers/Web/ModuleController.php:400 (revealSecret) | function hub_copy_secrets($user=null){ return hub_flag($user,'secrets') \|\| hub_flag($user,'copySec'); } ... revealSecret(): abort_unless(hub_copy_secrets($u),403,'ليست لديك صلاحية رؤية الأسرار'); retu | copySec must NOT be a reveal path. Gate revealSecret on hub_flag(secrets) (or a new vault.reveal key) ONLY; if a distinct clipboard capability is wanted, layer it on top of reveal, |
| 15.2 | P2 | leak | بوّابة العميل | REPORTED | app/Support/helpers.php:159-161 (hub_scope client branch), :184-219 (hub_client_ids returns null on empty active set), :122-129 (hub_company_ids null on empty) | hub_scope guards its client-isolation filter behind `if (($kids = hub_client_ids($user)) !== null && ($kcol = hub_client_col($module)))`. hub_client_ids ends with `return $ids ?: null;` — so a client  | Make hub_scope fail-closed for client accounts: when hub_is_client($user) and hub_client_col($module) exists, always apply the client filter with the coerced set (empty array => im |
| 15.3 | P2 | count_leak | بوّابة العميل | REPORTED | app/Http/Controllers/Api/MobileContextController.php:59-60,99-100 + contextDimension:281-291 | context() and bootstrap() call `contextDimension('companies', ...)` which does `$ids = hub_company_ids();` then `hub_scope($class::query(),'companies')`. For a client account hub_company_ids() is null | In contextDimension, when hub_is_client($user) return an empty/omitted dimension for 'companies' (clients have no company workspace), and for 'clients' force the fail-closed set (h |
| 15.4 | P2 | field_leak | بوّابة العميل | REPORTED | ModuleController buildQuery/columns (app/Http/Controllers/Web/ModuleController.php:28-86,342-386) vs App/Support/ClientPortalData.php:33 (CLIENT_INVOICE_KINDS) & :139 (invoiceRows kind filter) | The portal invoice reader restricts fin to sales invoices + incoming payments and a curated safe column set, documented as 'فواتيرُ العميل = المبيعاتُ والمقبوضاتُ فقط — لا مشترياتٌ تكشف التكلفة'. The  | Apply the client audience/kind curation inside the module engine for client accounts: when hub_is_client, constrain fin queries to CLIENT_INVOICE_KINDS and the portal column allowl |
| 16.1 | P2 | leak | 360/العلاقات | REPORTED | resources/views/modules/custom/projects.blade.php:63,202-203 + resources/views/partials/project_assets.blade.php:8,35,45-48 (root: app/Support/AssetProjectService.php:119-124 activeForProject) | Project 360 'assets' tab is added for every internal reader with no counterpart gate: `['assets', '🖥️ الأصول']` (line 63) — only clients are excluded, there is NO hub_can($u,'assets','v'). Its body in | Gate the tab and the partial body on hub_can($u,'assets','v') (mirror the Station360 asset tab). In AssetProjectService::activeForProject/historyForProject resolve the eager-loaded |
| 16.2 | P2 | leak | 360/العلاقات | REPORTED | resources/views/modules/custom/assets.blade.php:22,113-117 + resources/views/partials/asset_projects.blade.php:11,24,46 (root: app/Support/AssetProjectService.php:135-140 activeForAsset) | Asset 360 'projects' tab is shown to every internal reader with no counterpart gate: tab `! hub_is_client($u) ? ['projects', '🗂️ المشاريع'] : null` (line 22) and section `@unless (hub_is_client($u))`  | Gate the projects tab and the asset_projects body on hub_can($u,'projects','v') (matching Asset360::overview and Station360::projectContext). In activeForAsset/historyForAsset reso |
| 17.1 | P2 | bypass | التصدير/الجماعيّ | REPORTED | app/Http/Controllers/Web/ReportsController.php:267-301 (monthlyExport) | monthlyExport() calls only guardMonthly() (!hub_is_client + attend:v OR hr:v) then response()->streamDownload(...fputcsv...). There is NO abort_if(setting('security.freeze_exports')==='1',423), NO hub | Route monthlyExport (and any future dedicated CSV) through the same shared export belt before streaming: abort_if(freeze_exports,423); require hub_exporter()/attend.export; hub_req |
| 18.1 | P2 | field_leak | التنبيهات/الأتمتة | REPORTED | app/Console/Commands/HubAutomation.php:442-462 (notifyMonitors) + :306-317 (budgetsAuto); مصدر المستلمين :419-428 | recipientUsers(null) = المالكون + كل حامل hub_flag(u,'monitor'). ثم notifyMonitors يرشّح فقط بـ hub_scope: `if ($md && $recordId && ! hub_scope(DB::table($md['table'])..., $module, $u)->exists()) cont | في notifyMonitors وbudgetsAuto: أضف قبل/مع فحص hub_scope شرط `$u->role?->is_owner \|\| hub_can($u, $module, 'v')` (المالك يمرّ تلقائياً) — بنفس نمط AlertEngine:156. لوحدة 'recur' (:3 |
| 20.1 | P2 | raw_role | المطابقة | ✅ CONFIRMED | app/Http/Controllers/Web/OversightController.php:56-59 (isOversightOfficer) | $roleName = trim((string) setting('collab.oversight_role', '')); if ($roleName === '') return false; return trim((string) $user->role->name) === $roleName; — authorization is decided by matching the f | Replace the role-name comparison with a matrix permission key collab.oversight (hub_can) or a dedicated role flag, so it is granted/revoked in RoleController, explained by Permissi |
| 20.2 | P2 | uncovered | المطابقة | REPORTED | app/Http/Controllers/Web/RoleController.php:29 ($ops=['v','a','e','d']) and data() matrix rebuild loop; consumer app/Http/Controllers/Web/EmployeeCustodyController.php:201,330,356 | EmployeeCustodyController gates permit/settlement/reversal with $this->can('approve') => hub_can(user,'custody','approve'). But RoleController only knows $ops = v/a/e/d, and data() rebuilds the matrix | Either (a) render extra per-module ops (e.g. approve) in the role editor matrix and read them in data(), or (b) in data() preserve unknown/unrendered matrix ops by merging them fro |
| 20.3 | P2 | export_leak | المطابقة | REPORTED | app/Http/Controllers/Web/ReportsController.php:267 (monthlyExport) + guardMonthly:49 + view flag :251 | public function monthlyExport(Request $r){ $this->guardMonthly(); ... streamDownload(full attendance CSV) } where guardMonthly = abort_unless(hub_can(attend,'v')\|\|hub_can(hr,'v'),403). Unlike ModuleCo | Route the monthly CSV through the same export belt as ModuleController::export — call hub_exporter() (or a new attend.export key), honor security.freeze_exports, apply the large-ex |
| 01.6 | P3 | bypass | نواة التوثيق | REPORTED | app/Http/Controllers/Web/RoleController.php:230 (step-up in update) vs store() at :107-114 | update() requires hub_require_stepup only when $riskyAdded\|\|$scopeBroadened. store() (create role) performs no step-up, so a hijacked owner session can CREATE a brand-new role carrying secrets+users+e | Apply hub_require_stepup in store() when the new role includes any RISKY flag or scope=all-with-broad-matrix, mirroring update(); the create path is the same escalation surface the |
| 01.7 | P3 | raw_role | نواة التوثيق | REPORTED | app/Support/helpers.php (whole spine) — NEGATIVE confirmation | grep for role?->name (===\|==\|!=) across helpers.php, RoleController.php, PermissionInspector.php returns nothing; all decisions use role.matrix (permission), role.flags (capability), or role.is_owner  | None required — documented as a strength. Keep it this way: never gate on role name/id; the is_owner boolean is the only identity-based branch and is correctly a hard governance bi |
| 02.2 | P3 | over_broad | مسارات الويب | REPORTED | app/Support/helpers.php:964 hub_monitor (single 'monitor' flag) consumed by CostController.php:14, WorkforceController.php:19, DigitalAssetsController.php:19, TechWorkspaceController.php:56, SalesController.php:16, CapacityController.php:15, KpiController.php:20 | hub_monitor() { return hub_flag($user,'monitor'); } — one flag simultaneously unlocks project Profit&Loss / cost / hour-rates (CostController gate hub_monitor, shows margins), workforce salary aggrega | Split 'monitor' into named (module,action) analytics keys the engine already supports — analytics.financial.view / analytics.workforce.view / analytics.tech.view — mapping existing |
| 02.3 | P3 | over_broad | مسارات الويب | REPORTED | app/Http/Controllers/Web/ModuleController.php:794-815 exportBelt -> app/Support/helpers.php:1044 hub_exporter (single 'exp' flag) | exportBelt(): abort_unless(hub_exporter(),403); hub_exporter(){ return hub_flag($user,'exp'); } — a single global 'exp' flag authorizes CSV export for EVERY module the user can view. There is per-colu | Introduce per-module export keys (e.g. attend.export, payroll.export, hr.export) checked in exportBelt via hub_can(module,'export'), falling back to the legacy exp flag during migr |
| 02.4 | P3 | bypass | مسارات الويب | REPORTED | app/Http/Middleware/PortalGuard.php:80-98 (clientMayReach) MODULE_ALLOW=['engagements','projects','fin'] | if ($request->routeIs('m.*')) { $module = (string)$route->parameter('module'); return in_array($module, self::MODULE_ALLOW, true); } — PortalGuard whitelists MODULES but not HTTP verbs, so a client ac | Make PortalGuard read-only for client accounts on allowed modules: restrict m.* reachability to read verbs (GET m.index/m.show) for engagements/projects/fin, and require an explici |
| 03.4 | P3 | over_broad | API/الجوّال | REPORTED | app/Http/Controllers/Api/V1Controller.php:249-309 (metricsIngest) | metricsIngest resolves each point with resolveApi($mod,'e') (line 284) and writes via hub_metric_put, but unlike apiUpdate/apiPatch/apiDestroy (which return APPROVAL_REQUIRED when hub_needs_approval,  | Low priority (metric_points is append-only telemetry keyed by (module,record_id), not the approval-gated record fields). If parity is desired, short-circuit metricsIngest for modul |
| 04.2 | P3 | over_broad | التنقّل/الاكتشاف | REPORTED | app/Support/WidgetRegistry.php:256 | The dashboard 'links' quick-links widget lists `['t' => 'صندوق الوثائق', 'r' => 'inboxdocs.index', ... 'ok' => true]` unconditionally, while InboxDocController@gate:22 requires `hub_can($u,'inboxdocs' | Change the inboxdocs tile to `'ok' => hub_can($u,'inboxdocs','v') \|\| hub_can($u,'files','v')` to match the controller and the catalog. Users lacking both currently see a quick-link |
| 04.3 | P3 | over_broad | التنقّل/الاكتشاف | REPORTED | app/Support/helpers.php:417-426 (hub_top_links $mon centers) vs app/Support/helpers.php:1802 (hub_org_analytics_guard) invoked by CapacityController@gate:18 | hub_top_links gates capacity/recs/impact/appq (helpers.php:423-426), costs/svccosts (420-421), kpis (422), digital.assets (441) with `ok=$mon` (owner\|\|monitor) and no scope check. Their controllers ca | Add a scope check to the shared analytics predicate (e.g. `$mon && hub_company_ids($user)===null && hub_client_ids($user)===null`) so nav visibility matches hub_org_analytics_guard |
| 04.4 | P3 | over_broad | التنقّل/الاكتشاف | REPORTED | app/Support/helpers.php:429 vs app/Http/Controllers/Web/IdentityController.php:39 | hub_top_links 'identity' entry: `'ok' => hub_can($user,'assets','v')` (helpers.php:429), but IdentityController@center:39 is `abort_unless(hub_can(...,'assets','v') \|\| hub_can(...,'products','v'),403) | Broaden the catalog predicate to `hub_can($user,'assets','v') \|\| hub_can($user,'products','v')` to match the controller, so the product-cataloging audience can discover the center. |
| 06.3 | P3 | count_leak | اللوحات/العدّادات | REPORTED | app/Http/Controllers/Web/KpiController.php:83-85 | 'people' => DB::table('users')->whereNull('deleted_at')->where('status','!=','موقوف')->orderBy('name')->orderBy('id')->limit(200)->get(['id','name']) — no hub_company_ids/hub_scope filter and no hub_c | Scope the people query by hub_company_ids()/hub_scope on an employees join, and/or populate it only when hub_can($user,'hr','v'); otherwise expose owner as a plain id field. |
| 05.2 | P3 | raw_role | واجهة/أزرار | REPORTED | resources/views/modules/custom/payroll.blade.php:52,59-64 vs app/Http/Controllers/Web/PayrollController.php approve() | View wraps every payroll action in `@if ($plCanE)` where `$plCanE = hub_can(auth()->user(),'payroll','e')`, including the `<button ...>✅ اعتماد</button>` (do=approve). But the server: `approve()` does | Gate the payroll Approve button with `hub_approver()` in the view (as purchase_actions does) so it only renders for approve-flag holders/owners. |
| 05.3 | P3 | over_broad | واجهة/أزرار | REPORTED | app/Http/Controllers/Web/CostController.php:31-39 vs resources/views/modules/custom/projects.blade.php:23,297-325 | The project-360 finance tab hides cost/profit/margin/budget when `$pcFin = $pcSee('cost') && $pcSee('budget')` is false (field-mode based). But CostController::index('?p=<id>') gates the identical per | Unify onto one key (proposed projects.fin.view) or, minimally, have CostController additionally require cost/budget field_mode != hide (or route it through the same $pcSee gate) so |
| 05.4 | P3 | bypass | واجهة/أزرار | REPORTED | resources/views/modules/custom/changeorders.blade.php:17-22 vs app/Http/Controllers/Web/ChangeOrderController.php apply() | View gates the 'تطبيقٌ على المشروع' apply button with only `$coCanE = hub_can(auth()->user(),'changeorders','e')`. Server apply() requires BOTH `hub_can(...,'changeorders','e')` AND `hub_can(...,'proj | Add `&& hub_can(auth()->user(),'projects','e')` to the changeorders apply button condition to match the server. |
| 05.5 | P3 | over_broad | واجهة/أزرار | REPORTED | resources/views/modules/index.blade.php:28-30,216-218 + app/Http/Controllers/Web/ModuleController.php:794-812 | CSV export (list header + bulk 'export selected') is gated only by the single global `hub_exporter()` (exp flag). Any exp-flag holder who also has 'v' on hr/payroll/fin/custody can bulk-export those t | Introduce a per-module export key (see fine_grained_needed <module>.export) checked in exportBelt() alongside 'v'; keep exp flag as the owner/master switch. No breaking change: def |
| 08.2 | P3 | over_broad | التعاون | REPORTED | app/Support/CommentService.php:44 (guardTarget 'feed' branch) reached via app/Http/Controllers/Api/MobileCollabController.php:238 (commentReact) | guardTarget(): if ($module==='feed') return ['feed', null]; — the feed branch returns with no company scoping. commentReact() calls CommentService::guardTarget(auth(),$c->module,$c->record_id) then to | Apply the same feed company scope inside guardTarget's feed branch (or in the react entrypoints): when $module==='feed' and hub_company_ids($actor) is not null, load the target com |
| 07.3 | P3 | field_leak | المشاريع | REPORTED | app/Http/Controllers/Web/CostController.php:12-16,31-39 | protected function gate(){ abort_unless(hub_monitor(),403,...); } ... return view('costs.project',['p'=>$p,'pl'=>hub_project_pl($p->id,...)]); — /costs, /costs?p={id} and /service-costs render the SAM | Apply the same field-mode lever (or the proposed projects.fin.view) in CostController::gate/index so the field-level hide is honored consistently; at minimum skip cost/budget/profi |
| 07.4 | P3 | over_broad | المشاريع | REPORTED | config/hub.php:251-409 + app/Http/Controllers/Web/ModuleController.php:444 (update) | The projects module exposes members/manager_id, budget/cost/rev_exp, and url/staging/git/prod as fields under one module; ModuleController::update gates all writes with a single hub_can(projects,'e'). | Split edit into projects.members.manage, projects.fin.edit, projects.tech.edit (named matrix actions) and enforce per-field write authority in ModuleController::fill via hub_field_ |
| 07.5 | P3 | field_leak | المشاريع | REPORTED | resources/views/modules/custom/projects.blade.php:26,108-115,168-192 + app/Support/helpers.php:2385 (hub_project_health) | $pcHealth = $isCli ? [] : hub_project_health($row->id); the delivery-health score (and the overview KPI 'صحةُ التسليم') is shown to every projects.v holder. hub_project_health() internally calls hub_p | When hub_field_mode(user,'projects','budget')==='hide', recompute the health score excluding the budget factor (renormalize weights) rather than only suppressing the display row, s |
| 07.6 | P3 | over_broad | المشاريع | REPORTED | app/Http/Controllers/Web/AssetProjectController.php:36-38 (gate) | abort_unless(hub_can($u,'assets','e') && hub_can($u,'projects','v'),403,...); — the privilege to bind ANY in-scope asset to ANY in-scope project is derived from generic assets.e (edit assets). Anyone  | Introduce projects.asset.assign (or assets.assign) as a distinct action key rather than reusing assets.e, so asset custodianship and project-binding are separable; retain the proje |
| 09.3 | P3 | field_leak | الموارد البشرية | REPORTED | config/hub.php:9287 (hr), 9303 (payroll), 9305 (hrlog), 9302 (attend) mobile_sync.modules | hr, payroll, hrlog and attend are classified 'CACHEABLE_INCREMENTAL' in mobile_sync.modules, meaning their records are cached/persisted incrementally on mobile devices (the enum also offers 'SENSITIVE | Reclassify hr/payroll/hrlog (and reconsider attend) as SENSITIVE_NO_PERSIST, or split a non-sensitive projection for offline cache and keep salary/PII online-only. Add a test asser |
| 09.4 | P3 | over_broad | الموارد البشرية | REPORTED | app/Http/Controllers/Web/ModuleController.php:796 (exportBelt hub_exporter) + 762 (export) — global 'exp' flag | HR export is gated by `abort_unless(hub_exporter(), 403, ...)` (ModuleController.php:796) which checks the single global 'exp' role flag (one of the 8 RISKY flags), plus hr:v. There is no per-module e | Add a per-module export key (hr.export) checked in exportBelt alongside/instead of the global flag, mapping current 'exp' holders forward for migration; keep the freeze/step-up bel |
| 10.3 | P3 | over_broad | الحضور/التقارير | REPORTED | app/Http/Controllers/Web/ReportsController.php:267 (monthlyExport) + guardMonthly:46-50 | public function monthlyExport(Request $r){ $this->guardMonthly(); ...streamDownload... } وguardMonthly: abort_unless(hub_can(user,'attend','v')\|\|hub_can(user,'hr','v'),403). أي حامل لعرض الشهري يملك ا | افصل مفتاح attendance.export (module,action) على monthlyExport، موروثاً افتراضياً من attend.v\|\|hr.v للهجرة الآمنة ثم قابلاً للنزع. مجرّد مفتاح في role.matrix (لا محرّك ثانٍ). |
| 10.4 | P3 | idor | الحضور/التقارير | REPORTED | app/Http/Controllers/Web/WorkdayController.php:18-23 (checkIn) + app/Support/Workday.php:92-94 | 'client_id'=>Rule::exists('clients','id')->whereNull('deleted_at'), 'project_id'=>Rule::exists('projects','id')->whereNull('deleted_at'). Workday::checkIn يخزّن client_id/project_id كما وردا دون hub_s | قيّد project_id/client_id بنطاق المستخدم عند الحضور: تحقّق hub_scope(Project)->whereKey($pid)->exists() وhub_client_ids قبل الحفظ، وإلا تجاهل القيمة. أثر منخفض (صف ذاتي) ولكنه يغلق |
| 11.4 | P3 | over_broad | الماليّة | REPORTED | app/Http/Controllers/Web/CostController.php:12-16 + app/Http/Controllers/Web/SalesController.php:16 + app/Support/helpers.php:962-967 (hub_monitor) | hub_monitor()=hub_flag('monitor'). CostController::gate: `abort_unless(hub_monitor(),403,'لوحة التكاليف للمالكين ومن يحمل صلاحية المتابعة')` guards project revenue/cost/profit/margin (costs.index, cos | Separate a finance-analytics capability (e.g. reports.fin.view or the proposed projects.fin.view) from the operational 'monitor' flag, so profit/margin dashboards can be granted in |
| 11.5 | P3 | leak | الماليّة | REPORTED | app/Http/Controllers/Web/ModuleController.php:1428-1437 (columnsAndLabels via hub_visible_fields) + config/hub.php:354-371 (projects budget/cost/revExp), :6604 (quotes cost), :5746-5771 (banks iban/balance) | Financial figures are ordinary module fields revealed by plain module 'v': projects budget/cost/revExp (config:354-371), quotes cost (config:6604), services cost, banks iban(type 'text')+balance (conf | Ship default field_rules hiding cost/budget/revExp/margin, quote cost, bank iban/balance for non-owner roles, and/or introduce the proposed *.fin.view / banks.figures.view keys enf |
| 11.6 | P3 | bypass | الماليّة | REPORTED | app/Http/Controllers/Web/QuoteBuilderController.php:31-111 (storeLine) vs app/Http/Controllers/Web/QuoteController.php:39 (field_mode enforcement) | QuoteBuilder line writes require only hub_can(quotes,'e') and never consult hub_field_mode(quotes,'cost'); a role with field_rules {quotes:{cost:'ro'}} (intended read-only on internal cost) can still  | In storeLine/apply, drop or reject unit_cost when hub_field_mode(quotes,'cost') !== '' (ro/hide), matching the view's @if($showInternal) gate and the setStatus field-lock pattern u |
| 12.5 | P3 | idor | الأصول/النقاط | REPORTED | app/Http/Controllers/Web/StationController.php:68-77 | assign() validates the assignee as `'user_id' => ['required','uuid','exists:users,id']` and then only checks `if (! $user \|\| hub_is_client($user))`. It never scopes the user to the reader's company (n | Constrain the assignee to the reader's company scope, e.g. resolve via a company-scoped user query (hub_company_ids whereIn on the user's company) before assigning, returning the s |
| 12.6 | P3 | over_broad | الأصول/النقاط | REPORTED | app/Http/Controllers/Web/EndpointReleaseController.php:199-219 | download() gates only on `abort_if(hub_is_client(auth()->user()), 404)`. Any authenticated internal user — with no endpoints permission, no monitor flag, no owner — can GET /endpoints/releases/{id}/do | Acceptable if intentional, but consider gating download behind endpoints.view (or a capability) so binary distribution is auditable/grantable rather than implicit for all internal  |
| 13.2 | P3 | over_broad | الملفّات | REPORTED | app/Http/Controllers/Web/AttachmentController.php:45 & app/Support/AttachmentService.php:107-137 (attach path); mirrored MobileFileController.php:76,193,228 | AttachmentService::guardRecord($data['module'],$data['record_id'],'v') is the only authorization before AttachmentService::attach() writes files onto the record. 'v' is the VIEW op. Comment at Attachm | Introduce documents.attach as a distinct (module,action) key checked alongside 'v' in the upload path (store / mobile attach / upload-complete). For safe migration, map it from mod |
| 14.2 | P3 | over_broad | الإدارة/الأمن | REPORTED | app/Http/Controllers/Web/DataRoomController.php:21 (gate) + :55 (store) | protected function gate(){ abort_unless(hub_secrets(),403,'غرفة البيانات تتطلب صلاحية الأسرار أو المالك'); } — the same 'secrets' flag that authorizes internal vault reveal also authorizes ShareLink:: | Separate 'publish a sensitive document to a public link' (dataroom.share.create) from 'view a vault secret internally' (vault.reveal). They have materially different blast radius ( |
| 14.3 | P3 | over_broad | الإدارة/الأمن | REPORTED | app/Http/Controllers/Api/EndpointEnrollController.php:37 (mint) | abort_if(hub_is_client(auth()->user()),404); abort_unless(hub_is_owner() \|\| hub_monitor(),403,'سكُّ رموز التسجيل للمالك أو المراقب'); — MDM device enrollment-token minting is gated by the 'monitor' fl | Gate mint on the mobile flag (or owner), or a new endpoints.enroll key. It is asset-company-scoped so this is not a cross-tenant leak, but granting 'view dashboards' should not sil |
| 15.5 | P3 | raw_role | بوّابة العميل | REPORTED | app/Http/Controllers/Web/ClientMemberController.php:30 (manageClient) + app/Http/Controllers/Api/MobileClientMembersController.php (manageClient) | invite()/setRole()/revoke() — which create external portal login accounts (AccountActivation::provisionClient) and can grant the Client Owner role — are authorized by `abort_unless(hub_can(auth()->use | Introduce a dedicated named permission (e.g. clients.members.manage in the same role.matrix) gating invite/setRole/revoke, distinct from clients:e (CRM edit). Migrate existing clie |
| 16.3 | P3 | count_leak | 360/العلاقات | REPORTED | resources/views/partials/project_assets.blade.php:35 and resources/views/partials/asset_projects.blade.php:24 (via AssetProjectService::activeForProject/activeForAsset, app/Support/AssetProjectService.php:119-140) | Independently of the name leak, the badges `{{ $paActive->count() }} مخصّص` and `{{ $apActive->count() }} نشط` publish the exact number of counterpart-assignments computed from an unscoped, ungated qu | After gating the tab bodies on the counterpart module's v and wrapping the service queries in hub_scope, compute each badge from the same scoped query used for the list, not from a |
| 17.2 | P3 | over_broad | التصدير/الجماعيّ | REPORTED | app/Support/helpers.php:1042-1048 (hub_exporter) consumed at app/Http/Controllers/Web/ModuleController.php:796 (exportBelt) | function hub_exporter($user=null): bool { return hub_flag($user??auth()->user(),'exp'); } — a single global RISKY flag. exportBelt() gates all 85 modules' CSV export on this one boolean; there is no p | Keep 'exp' as an owner-level global override but have exportBelt() also accept a per-module action key: abort_unless(hub_exporter() \|\| hub_can($u,$module,'export'),403). Seed <modu |
| 17.3 | P3 | field_leak | التصدير/الجماعيّ | REPORTED | app/Http/Controllers/Web/ReportsController.php:289-296 (monthlyExport row emit) | fputcsv($out, array_map($safe, [$emp->name, $emp->dept ?: '', $d, $c['time_in']..., ...])) reads Employee->name/->dept and compliance labels directly and never consults hub_field_mode(user,'hr',field) | Before emitting each column, skip any field whose hub_field_mode(auth()->user(),'hr',$key)==='hide' (mirror columnsAndLabels/hub_visible_fields) so the attendance CSV honors the sa |
| 17.4 | P3 | bypass | التصدير/الجماعيّ | REPORTED | routes m.import/m.import.map/m.import.run (routes-full.txt:452-454) — middleware [web\|auth] only | m.export carries throttle:20,1 and m.bulk carries throttle:30,1, but all three import routes carry only [web\|auth] with no throttle. ImportController::run (ImportController.php:81-197) reads and inser | Add throttle (e.g. throttle:20,1) to the import routes to match the export/bulk mutating endpoints; the 5000-row transactional import is a heavier operation than the throttled expo |
| 18.2 | P3 | field_leak | التنبيهات/الأتمتة | REPORTED | app/Support/FlowRunner.php:230-252 (tpl) + :162-179 (act notify، الفرع to != 'owners') | act() فرع notify: `$targets = ($a['to']??'owners')==='owners' ? hub_approvers_for($module,$m->id) : [(string)$a['to']];` — الفرع 'owners' مُنطّق (hub_approvers_for)، أمّا فرع المستلم الصريح فيُشعِر أي | في فرع المستلم الصريح طبّق نفس بوّابة الرؤية (hub_can(v)+hub_scope->exists) قبل الإنشاء، أو اقصر مستلمي المسار على قائمة مُنطّقة. وفي tpl احترم hub_field_mode (تخطّ الحقول hide/ro  |
| 18.3 | P3 | leak | التنبيهات/الأتمتة | REPORTED | app/Http/Controllers/Web/NotificationController.php:21,42 + app/Http/Controllers/Api/MobileCommController.php:474 (notificationShape) مقابل app/Support/PushService.php:200-224 | index/mini وnotificationShape يسلسلان n->text المخزون حرفياً لكل إشعار يملكه المستلم، دون إعادة ترشيح إن سُحبت صلاحيته على السجل بعد الإنشاء. لا يوجد مسار يحذف/يخفي الإشعارات القديمة عند سحب الوصول. P | خيار متحفظ: قبول السلوك كأثر تاريخيّ لإشعارات المستخدم نفسه (منخفض الأثر). خيار أمتَن: للأنواع المرتبطة بسجل (module+record_id) أعِد فحص hub_can+hub_scope عند العرض وأخفِ/عمّم نص ا |
| 18.4 | P3 | uncovered | التنبيهات/الأتمتة | REPORTED | app/Console/Commands/HubOutbox.php:155-170 (telegram) + AlertEngine.php:262-270 / :670-673 (إنشاء OutboxMessage بـ target=null) | OutboxMessage من AlertEngine يُنشأ بـ target=null وuser_id=$canSee->first()?->id ونص يحمل اسم سجل مُنطّق. HubOutbox::telegram يحلّ الوجهة: `$chat = $msg->target ?: userPref(user_id,'tg') ?: setting('n | هذا اختيار حوكمة (مؤلّف القاعدة يختار قناة تلجرام) والقناة يضبطها المالك؛ لكن يُفضّل: عند سقوط الوجهة إلى notify.tg_chat لرسالة تحمل record_id مُنطّقاً، إمّا تعميم النص (بلا اسم ال |
| 19.1 | P3 | field_leak | النطاق/IDOR | REPORTED | app/Http/Controllers/Web/CostController.php:70 (renders app/Support/helpers.php:2197 via resources/views/costs/index.blade.php:72) | CostController::index (gate = hub_monitor() only) does `'rates' => hub_hourly_rates()` and the view prints `متوسط الفريق حالياً {{ $rates['avg'] }}`. hub_hourly_rates() builds rate = (salary+allow)/(d | Do not compute/pass hub_hourly_rates() (or expose only a coarse, permission-gated value) unless the viewer passes a salary-visibility gate: guard the average behind hr.salary.view  |
| 19.2 | P3 | leak | النطاق/IDOR | REPORTED | app/Support/helpers.php:2199 (cache 'cost:rates') consumed by hub_project_pl → CostController::index/show | hub_hourly_rates() is memoized under a global key `cost:rates` with no scope component and reads ALL employees regardless of company. hub_project_pl() uses this map to compute each project's labor cos | Either call hub_org_analytics_guard() on CostController::index (blocking isolated accounts as services() already does), or scope hub_hourly_rates() by hub_company_ids() and key its |
| 20.4 | P3 | over_broad | المطابقة | REPORTED | app/Support/helpers.php:1042-1047 (hub_exporter) + ModuleController.php:796 (exportBelt) | function hub_exporter($user=null){ return hub_flag($user,'exp'); } — a single global 'exp' flag authorizes CSV export of EVERY module the user can view, with no per-module distinction. A user granted  | Introduce per-module export keys (module.export) consumed by exportBelt, with the global exp flag retained only as a migration fallback. Sensitive modules (hr, fin, payroll, attend |
| 20.5 | P3 | over_broad | المطابقة | REPORTED | app/Support/helpers.php:962-967 (hub_monitor) + hub_top_links analytics group helpers.php:416-448 | function hub_monitor(){ return hub_flag(user,'monitor'); } gates a heterogeneous set behind one flag: performance, sales.dashboard, costs.index, servicecosts, kpis, capacity, recs, impact, appquality, | Split monitor into monitor.finance vs monitor.ops, and move digital-assets to the secrets flag or a dedicated dassets.view key, so a sales/ops viewer is not implicitly granted fina |
