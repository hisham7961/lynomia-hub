# Permissions 360 — التقريرُ النهائيّ (المراحل 0–4)

> **الغاية:** صلاحيّاتٌ دقيقةٌ قابلةٌ للمنحِ والمنعِ والتفسير، **على المحرّكِ القائمِ نفسِه**
> (`role.matrix` + `role.flags` + `field_rules` + `hub_scope`) — لا محرّكَ توثيقٍ ثانٍ،
> ولا كسرَ لأحد: كلُّ تفكيكٍ إضافيٌّ (الرايةُ الأصليّةُ تبقى مفتاحاً رئيساً) أو مصحوبٌ
> بهجرةٍ عديمةِ الخسارة (من كان يرى مُنح).

**نطاقُ الإنجاز:** v2.478.0 → v2.489.0 · **46/83** نتيجةً مُغلقةً (اثنتا P1 كلتاهما، و26 من P2، و18 من P3) · كلُّ دفعةٍ خضراءُ على المحرّكين (SQLite + MySQL، ~3800 اختباراً).

---

## 1) الآليّاتُ المبنيّة (لا محرّكَ ثانٍ)

| الآليّة | الموضع | ما تفعله |
|---|---|---|
| كتالوجُ المفاتيحِ الدقيقة | `config/hub_permissions.php` | كلُّ مفتاحٍ مسمّى (label/hint/modules/risky) — المحرّرُ يحفظُه ويعرضُه، والفاحصُ يفسّرُه. أسماءٌ بلا نقاط (قيدُ تحليلِ المحرّر) |
| قراءةُ المفاتيح | `hub_can($u, $module, $key)` | نفسُ دالّةِ المصفوفة تقرأ أيَّ مفتاحٍ مسمّى (المالكُ يتجاوز) |
| مجموعاتُ المراقبة | `hub_monitor_group($group)` | `hub_monitor() \|\| hub_flag($group)` — monitor تبقى جامعةً، والمجموعةُ منحٌ أضيق |
| الوثائقُ الحسّاسة | `hub_doc_sensitive` + `DocumentPolicy` (docsec) | نوعُ وثيقةٍ حسّاسٌ يُحجب إلا عن حاملِ `docsec` على وحدتِه |
| الحقولُ الحسّاسة | `config/hub_field_sec.php` + `hub_field_mode` (fieldsec) | حقلٌ حسّاسٌ يُحجب قبل قواعدِ الدورِ إلا عن حاملِ `fieldsec` — يسري على كلِّ سطحٍ يستشير الدالّة |
| نطاقُ مفتاحِ API | `ApiToken::allows` عند كلِّ بوّابة | المفتاحُ يُقيّد كصلاحيّةِ المستخدم لا يتجاوزها |
| التفسير | `PermissionInspector::finePerms/flags` + `/admin/access` | «ماذا يفتح هذا المفتاحُ ولمن مُنح؟» دون قراءةِ الكود |
| هجراتٌ عديمةُ الخسارة | `grant_docsec…` · `grant_fieldsec…` | من كان يرى يومَ التفعيل مُنح — لا فقدَ فوريّاً، والمنعُ صار ممكناً |
| بوّابةُ سلامةِ الكتالوجات | `Permissions360CatalogIntegrityTest` | CI يُسقط أيَّ انحرافٍ يجعل حمايةً ميّتةً صامتةً (حقلٌ أُعيد تسميتُه، وحدةٌ وهميّة، مفتاحٌ بنقطة) |

## 2) مصفوفةُ الثوابت (عدديّة — كلُّ ثابتٍ محروسٌ باختبارٍ يفشل أولاً)

| # | الثابت | الاختبار (عددُ الاختبارات) |
|---|---|---|
| 1 | حسمُ approve لكلِّ مجالٍ على حدة، والرايةُ جامعة | `ApprovalDecompositionTest` (5) |
| 2 | تصديرُ كلِّ وحدةٍ بمفتاحِها أو الرايةِ الجامعة + حزامُ التجميد/التصعيد | `ExportDecompositionTest` (4) |
| 3 | الوثائقُ الحسّاسةُ خلف docsec (رؤيةً وتنزيلاً ورابطاً) | `DocumentAccessPolicyTest` (+sensitivity/migration) |
| 4 | لا وصولَ لملفٍّ عبر رابطٍ خام دون سياسةِ وثيقتِه | `FileAccessHardeningTest` (2) |
| 5 | لوحاتُ المنشأةِ محجوبةٌ عن معزولِ المشاريع؛ حضورُ 360 خلف attend:v؛ لا meta خام في API | `Permissions360LeakClosureTest` (3) |
| 6 | مرجعُ الكتابةِ يُنطَّق (حضور⇢مشروع، محطّة⇢شركة) | `Permissions360ScopeIdorTest` (3) |
| 7 | custodyAssign/assetStatus/assetStation/assetInventory تفتح عمليّتَها دون assets:e؛ endpoints:command/enroll دون monitor؛ membersManage دون clients:e | `Permissions360FineGrainedTest` (6) |
| 8 | مفتاحُ API لا يقرأ hr بلا «hr:v» ولا يربطُ أصلاً بلا «assets:e»؛ والعميلُ لا يرى قائمةَ الشركات | `Permissions360ApiScopeTest` (3) |
| 9 | assetAssign يفتح التخصيصَ دون assets:e؛ إنشاءُ دورٍ حسّاسٍ خلف تصعيدِ هويّة؛ الاستيرادُ خلف حدِّ معدّل | `Permissions360KeysHardeningTest` (4) |
| 10 | كلُّ مجموعةِ مراقبةٍ تفتح مراكزَها وحدَها؛ monitor تفتح الكلَّ؛ الشريطُ يطابق البوّابة | `Permissions360MonitorGroupsTest` (5) |
| 11 | الراتبُ/الهويّاتُ/ماليّةُ المشروعِ خلف fieldsec؛ المالكُ يرى؛ قواعدُ الدورِ فوقَ المفتاح؛ الهجرةُ تمنح من كان يرى | `Permissions360FieldSecTest` (5) |
| 12 | الفاحصُ يفسّر المفاتيحَ والراياتِ بصدق (oversight ليست موروثةً للمالك) وتظهر في /admin/access | `Permissions360InspectorTest` (3) |
| 13 | الكتالوجاتُ مطابقةٌ للسجلّ — لا حمايةَ ميّتةً صامتة | `Permissions360CatalogIntegrityTest` (4) |
| 14 | حارسُ IA == بوّابةُ المتحكّم == ظهورُ الشريط، لكلِّ وجهة | `IaGuardParityTest` + `IaLeakageTest` + شخوصُ `MonitorPersona/ManagerPersona` |
| 15 | كلُّ مسارِ GET إنسانيٍّ مُصنَّف (لا UNCLASSIFIED) | `IaRouteCoverageTest` |

## 3) سجلُّ الإغلاق بالنسخ

انظر «سجلَّ الإغلاق» في `FINDINGS.md` — 46 نتيجةً بنسخةِ إغلاقِها واختبارِها.

## 4) المتبقّي (37) — مصنَّفٌ بأسبابِه

| الصنف | النتائج | لماذا لم تُغلق الآن |
|---|---|---|
| جراحةُ نطاقٍ سلوكيّة | 16.3 · 08.2 · 10.2 | تغييرُ سلوكٍ مرئيّ (عدّاداتُ 360، تفاعلُ القناة، مراجعةُ HR عبر الشركات) يحتاج دفعةً معزولةً بدلالاتِ null-company محسومة |
| عملُ محرّك | 15.4 · 03.3 · 07.4 · 08.1 · 01.5 | تقييدُ أعمدةِ العميلِ داخل المحرّك، تفكيكُ projects:e كتابيّاً، نطاقٌ مُعلَنٌ لكلِّ وحدة — أعمالٌ متوسّطةُ الحجم على النواة |
| تضييقٌ يحتاج قرارَ سياسة | 12.1 · 12.2 · 12.6 · 14.1 · 15.2 · 13.2 · 14.2 · 10.3 · 17.3 · 09.3 · 07.5 | كلُّها تسحب سلوكاً قائماً (v تمنح الإرفاق، copySec يكشف، أعمدةُ endpoints تُقفل…) — تُغلق بنمطِ الهجرةِ الآمنِ متى قرّر المالكُ التضييق |
| حوكمةُ إشعارات | 18.1 · 18.2 · 18.3 · 18.4 | المسارُ الآليُّ يُشعر من سمّاه المالكُ في تعريفِ المسار — تقييدُه قد يكسر أتمتةً مقصودة |
| تجميلُ تنقّل (لا تسريب) | 04.1 · 04.2 · 04.4 · 05.2 · 05.3 · 05.4 | الرابطُ يظهر والمتحكّمُ يصدّ — إزعاجُ اكتشافٍ لا ثغرة |
| مخفَّفةٌ جزئيّاً | 19.1 · 19.2 · 02.4 · 03.4 · 06.3 · 07.3 | 19.x خُفِّفت بـfieldsec (الراتبُ محجوبٌ حقلاً) وfinAnalytics (لوحةُ التكاليف منحٌ أضيق)؛ والبقيّةُ خلف بوّاباتٍ قائمةٍ أعلى |

## 5) قواعدُ الاستمرار

1. **مفتاحٌ جديد** يُعلَن في `hub_permissions.php` (بلا نقاط) قبل أوّلِ `hub_can` عليه — وإلا محاه المحرّرُ عند الحفظ (تحرسه بوّابةُ السلامة).
2. **حقلٌ حسّاسٌ جديد** يُضاف إلى `hub_field_sec.php` بمفتاحِ الحقل — البوّابةُ تُسقط أيَّ مفتاحٍ ميّت.
3. **تضييقُ سلوكٍ قائم** لا يمرّ إلا بهجرةٍ عديمةِ الخسارة تمنح من كان يرى.
4. **كلُّ بوّابةٍ جديدة** بثلاثيّتِها معاً: بوّابةُ المتحكّم + ظهورُ الشريط + حارسُ IA (وإلا سقط `IaGuardParityTest`).
