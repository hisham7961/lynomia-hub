# نتائجُ المجلس — السجلُّ الموحَّد

> **عمودُ «التحقّق» هو عمودُ هذه الوثيقة.** بلاغُ الخبيرِ ليس نتيجةً حتى
> يُتحقَّق منه بالمصدر. و**أربعةُ بلاغاتٍ لم تصمد** — مسجَّلةٌ في القسمِ الأخير،
> لأنّ حذفَ ما سقط يُكذّب الصورةَ مثلَ إخفاءِ ما نجح.

## مفتاحُ التحقّق

| الرمز | المعنى |
|---|---|
| **✅ مُتحقَّق** | فحصتُه بنفسي في الشيفرةِ أو حيّاً، وأثبتُّه |
| **🔒 مُغلَق** | مُتحقَّقٌ **وأُصلح** باختبارٍ سقط أوّلاً وبوّابةِ محرّكين |
| **📋 مُبلَّغ** | بلاغُ خبيرٍ بشاهدٍ حيٍّ، **لم أتحقّق منه بعد** — لا يُبنى عليه قرار |
| **❌ سقط** | فُحص ولم يصمد |

---

## ١ · الصنفُ الحاكم: **«سؤالٌ واحدٌ · تعريفان»**

**هذا ليس بنداً في القائمة — بل القائمةُ نفسُها.** ظهر الصنفُ **ستَّ مرّاتٍ**
في ستّةِ مجالاتٍ لا يجمعها إلّا آليّتُه:

> تُكتب السلطةُ المركزيّةُ صحيحةً · ثمّ يكتب قارئٌ تعريفَه الخاصَّ **حين كان ذلك
> التعريفُ صحيحاً** · ثمّ ينزلق المعنى تحته · ويبقى القارئُ على حاله.

| # | السؤالُ الواحد | السلطة | القارئُ المنحرف | الحالة |
|---|---|---|---|---|
| **A-1** | «هل المهمّةُ مفتوحة؟» | `hub_open_scope` | صحّةُ المشروع (قائمةٌ حرفيّة) | 🔒 **مُغلَق** v2.500.0 |
| **A-3** | «هل تُخصم هذه الإجازة؟» | `deduct_types` / `Workday::onLeave` | `hub_capacity` (بلا تصفيةِ نوع) | 🔒 **مُغلَق** v2.500.0 |
| **A-5** | «من يبتّ في طلبِ الإجازة؟» | `LeaveDecisionController` | النموذجُ العامُّ + ٣ أبوابٍ أخرى | 🔒 **مُغلَق** v2.501.0 |
| **P-16** | «من يرى مركزَ الجودة؟» | `QualityController::tabGate` | `helpers.php:6178` (المالكُ وحدَه) | ✅ **مُتحقَّق** · لم يُصلَح |
| **ت-٣** | «هل النسخةُ الاحتياطيّةُ سليمة؟» | — (لا سلطةَ واحدة) | `SecurityPosture` (نبضة) ≠ `OpsController` (ملفّات) | ✅ **مُتحقَّق** · لم يُصلَح |
| **AUT-01** | «ما المكوّنُ غيرُ السليم؟» | `/admin/control` (البطاقة) | `/admin/control` (طابورُ الانتباه) | 📋 مُبلَّغ |
| **N-1** | «متى يُمنع نقلُ الملفات؟» | `Middleware/WorkHours` | `ReportsController::monthlyExport` (لا يسأل أصلاً) | 🔒 **مُغلَق** v2.512.0 |
| **N-3** | «أهذا يومٌ يعبر منتصفَ الليل؟» | `Attendance` (رايةٌ صريحة) | `Workday::checkOut` (حسابٌ ضمنيّ بلا راية) | 🔒 **مُغلَق** v2.513.0 |
| **N-10** | «ما الصفُّ المفتوحُ الآن؟» | `Workday::checkOut` (اليومُ + أمسٍ عابر) | `Workday::mine()` (اليومُ وحدَه) | 🔒 **مُغلَق** v2.514.0 |

**والسابعةُ كانت لي:** أوّلُ إصلاحٍ كتبتُه لـA-5 أنشأ **تعريفاً خامساً** في
المتحكّم بدل قراءةِ السلطةِ القائمة. فالصنفُ لا يُصيب من كتب المنتجَ وحدَه —
**يُصيب من يُصلحه أيضاً**، وهذا أخطرُ ما فيه.

---

## ٢ · العيوبُ المُغلَقة (🔒)

| المعرّف | الوصف | الإثبات |
|---|---|---|
| **A-1** | مهمّةٌ بلا حالةٍ تختفي من عدّادِ المتأخّر — `NULL NOT IN (…)` لا يصدُق في SQL | `CouncilAuthoritySplitTest` |
| **A-3** | طلبُ «سلفة» معتمدٌ يخصم **٤ أيّامِ عمل** من طاقةِ صاحبِه | اختباران |
| **قفلُ الحساب** | ميزةُ أمانٍ بلا بابِ استرداد — «ليست أماناً بل **فخّاً**» بتعبيرِ المستودع | `CouncilAccountUnlockTest` |
| **A-5** | اعتمادٌ ذاتيٌّ من **أربعةِ أبواب** · خصمُ رصيدٍ وإعفاءٌ من الحضور | `CouncilSelfApprovalTest` — ١١ اختباراً |
| **N-1** | حظرُ التصديرِ الليليّ يردّ `m/hr/export` ويُسلّم كشفَ الرواتبِ الشهريَّ في الدقيقةِ نفسِها | `CouncilNightExportTest` — ٥ اختبارات |
| **N-2أ** | نقلُ مسيّرِ رواتبٍ إلى شركةٍ فيها مسيّرٌ لذلك الشهر يمرّ صامتاً — راتبٌ مصروفٌ مرّتين | `CouncilRetest8Test` |
| **N-2ب** | استعادةُ مسيّرٍ من السلّة إلى شهرٍ صار مشغولاً تمرّ صامتاً | `CouncilRetest8Test` |
| **N-2ج** | الفهرسُ الفريدُ يعدّ المحذوفَ محتلّاً لشهرِه — حذفُ مسيّرٍ يحرق شهرَه بخطأِ قاعدةٍ خام | `CouncilRetest8Test` |
| **N-3** | «انصراف» معطَّلٌ لكلِّ ورديةٍ تعبر منتصفَ الليل — يومٌ بساعاتٍ فارغةٍ تُغذّي الرواتب | `CouncilRetest8Test` — ٨ اختبارات |
| **N-10أ** | نافذةُ ٠٠:٠٠–٠٧:٥٩ تكتب ٢٣٫٧٨ ساعةً ملفَّقةً في كشفِ الرواتب بلا شذوذٍ ولا وسم | `CouncilRetest9Test` |
| **N-10ب** | بطاقةُ يومِ العمل لا تعرض «انصراف» للورديّةِ العابرة — فتُهجَر الورديّة | `CouncilRetest9Test` |
| **N-10ج** | مهلةُ التقريرِ تقع قبل الانصرافِ الحقيقيِّ بـ٢٢ ساعة — إدانةٌ بتأخيرٍ لم يقع | `CouncilRetest9Test` |
| **N-4** | مستنداتُ الأعمالِ الثنائيّة (`quotes.pdf` · `changeorders.pdf`) خارج حظرِ الليل | `CouncilNightDocumentsTest` |

---

## ٣ · مُتحقَّقٌ منه ولم يُصلَح بعد (✅) — **أوّلُ الدفعةِ التالية**

### ت-٣ · «نسخةٌ حديثة» و«صفرُ ملفّات» في آنٍ — **الأعلى ثمناً**

| القارئ | يسأل عن | وجد |
|---|---|---|
| `SecurityPosture::backupFresh()` | `heartbeat.backup` (**ختمُ زمن**) | `08:04 · result:"ok"` ⇒ **✅** |
| `OpsController::backupsPanel()` | `glob(app/backups/hub-*.json*)` (**ملفّات**) | **فارغ** |

**والمهمّةُ عملت فعلاً** — ولهذا العلامةُ الخضراءُ **مكتسَبةٌ بنبضةٍ ومكذوبةٌ
بالقرص**. فحصتُ التلوّثَ البيئيَّ قبل التسجيل: `heartbeat.backup` ليس مبذوراً،
ومسارُ الكتابةِ هو عينُ مسارِ القراءة. ⇒ **العيبُ أنّ القارئَ لا يسأل قطّ «هل
ثمّة ما يُستعاد؟»**، لا أنّ الملفّاتِ اختفت (ذاك بيئيّ).

**والمنتجُ يعرف هذا الصنفَ ويحرسه — في الكاتبِ فقط:** تعليقُ v2.312 في
`HubBackup` يصفه بالحرف: «**والمشغّل يقرأ ✓ ويطمئن**».

### P-16 · مركزُ الجودةِ مفتوحٌ ومخفيٌّ معاً
حاملُ رايةِ `opsAnalytics` يفتح `/admin/quality` بالرابطِ المباشر ولا يرى إليها
رابطاً في شريطِه. **العلاجُ إضافةٌ:** يقرأ التنقّلُ شرطَ الحارسِ نفسَه.

### PROD-05 · ثلاثُ شاشاتٍ متناقضةٍ على «ما الذي يحتاج انتباهي؟»
**العَرَضُ صمد · والسببُ سقط** (انظر §٦). الرادارُ يمسح `hr.iqamaExp` فعلاً؛
والعيبُ أنّه **منطَّقٌ بصلاحيّةِ الوحدةِ بلا استثناءٍ لصاحبِ الشأن** — فمن تنتهي
إقامتُه هو الوحيدُ الممنوعُ من إنذارِ نفسِه، ثمّ تجزم الشاشةُ **«كل أصولك بأمان»**.

> **أُغلق على مرحلتين — والأولى كانت نصفَ إصلاحٍ أنتج عيباً جديداً.** في
> v2.503.0 أُضيف الاستثناءُ إلى `hub_expiry()` واختُبر **باستدعاءِ الدالّة** فمرّ؛
> ثمّ أثبت التحقّقُ المستقلُّ (§١٨) أنّ الشاشةَ لم تتغيّر: **مرشِّحٌ ثانٍ** في
> `AlertController` (ومثلُه في ودجةِ `expiry`) كان يُسقط الصفَّ الذي أُضيف
> خصّيصاً لها — **فالشارةُ «١» والصفحةُ «لا شيء ينتهي»**. وفي **v2.505.0** حُذف
> التعريفُ الثاني من الموضعين، وصارت وجهةُ الصفِّ تُقرأ من `hub_expiry_url()`
> الواحدة (صفُّ صاحبِ الشأنِ يفتح `/me` لا سجلَّ الوحدةِ الذي يردّه ٤٠٣).
> **والدليلُ حيّ:** الشارةُ «١» والصفحةُ «🟠 خلال ٧ أيام · ١»، والرابطُ يُفتح **200**.
>
> **ثمّ لم تكفِ تلك الجولةُ أيضاً.** أعاد التحقّقُ الاختبارَ على v2.505.0 فوجد
> **عارضاً رابعاً** (صفحةُ المساحةِ وشارتُها) يُسقط الصفَّ بالتعريفِ نفسِه،
> **وإفشاءً أحدثه الإصلاح**: صفُّ صاحبِ الشأنِ — وهو **بالمستخدم** — دخل مخبأَ
> «مركزِ الفعل» المُفتاحِ **بالدور**، فقرأ زميلٌ بلا `hr:v` اسمَ زميلِه وتاريخَ
> انتهاءِ إقامتِه، ثمّ مُحي إنذارُ صاحبِه عنه حين أعاد الزميلُ بناءَ المخبأ.
> وكشف الإغلاقُ بندين آخرين: `->first()` بلا `orderBy` (قرعةٌ أنذرت بالأبعد
> وحجبت الأقرب) وقناعَ حقلٍ لا يسري على مسحِ صاحبِ الشأن. **أُغلقت الخمسةُ في
> v2.506.0** — وبقي مُعلَناً: وثائقُ ملفِّه لا تصله، ورابطُ الجوالِ العميقُ
> يردّ ٤٠٣ حتى يقرأ التطبيقُ رايةَ `self`.

---

## ٤ · مُبلَّغٌ بشاهدٍ حيّ — بانتظارِ تحقّقٍ مستقلّ (📋)

### الأشدُّ خطورةً
| المعرّف | الوصف | الدرجة |
|---|---|---|
| **AUT-01** | ١٠ من ١١ مجدولةً «لم تنبض» · `/admin/control` يعدّها ثمّ يُسقطها من طابورِ الانتباه · و`health.scheduler` **يُقيَّم بوظيفةٍ مجدولة** | A |
| **AUT-02** | طابورٌ عالقٌ يبدو أصحَّ من فاشل: «بالانتظار ٢ · فاشلة ٠» والزرُّ يردّ «لا رسائل فاشلة» | A |
| **AUT-07** | قالبُ مسارٍ حلَّ **راتبَ موظّفٍ (1401.000)** في نصِّ إشعار · ووجهاتُه تشمل **حسابَي عميل** | A |
| **DB-01** | مسيّرا رواتبَ لشهرٍ واحد (يحتاج هجرةَ `unique`) | A |
| **CEO-06 / PROD-10** | **صفرٌ من صفرٍ = ١٠٠٪** — «الامتثال ١٠٠٪» على صفرِ سجلّات | A |
| **PROD-09** | «التنبّؤُ المرجَّح = 0.000» بجانب خطِّ أنابيبَ ٢٥١٬٠٠٠ — و«٠» تعني «لم يُقَس» | A |
| **CEO-18 / ت-٦** | CRM بلا عملة · مسارُ مبيعاتٍ يجمع عملاتٍ مختلفة | A/E |
| **م-1** | النموذجُ مجموعيٌّ بالدور لا مستأجَرٌ بالشركة | E |
| **2FA** | تغطيةُ التحقّقِ الثنائيِّ **٠٪ من ٣٩ حساباً** · ومفتاحُ الإلزامِ مبنيٌّ ومُطفأ | A |

### البقيّةُ مصنَّفةً في تقاريرِها
`CEO-01…22` · `N-COO-01…15` · `PMO-01…18` · `A-2` · `A-4` · `E-1` · `D-1` · `C-1` ·
`م-2` · `م-3` · `ت-١…٥` · `DB-02…13` · `AUT-03…20` · `PROD-01…14`.

---

## ٥ · فجوةُ الاتّساقِ في العرض — **٨٤ رابطاً ظاهراً يردّ منعاً**

مسحُ **١٬٥٦٨ رابطاً** عبر أربعِ شخصيّات. والمسحُ المعاكسُ (٣٠٨ فحصَ مسار) أعطى
**٢٦٠ منعاً صحيحاً** وستَّ حالاتٍ فُحصت يدويّاً: **خمسٌ ليست أعطالاً** وواحدةٌ
من الدرجة D.

> **فحارسُ المسارِ في لينوميا ليس مثقوباً.** كلُّ العطبِ في الاتّجاهِ الآخر:
> روابطُ تُرسَم لما لا يُبلَغ. والمشكلةُ في **اتّساقِ العرضِ** لا في **متانةِ المنع**.

---

## ٥٫٥ · N-1 · **بابانِ لملفٍّ واحد** — ودليلُه من الساعةِ الثالثةِ فجراً

سؤالٌ واحدٌ، حسابٌ واحد (مسؤولةُ الموارد البشريّة)، **دقيقةٌ واحدة** على بيئةِ
العرضِ الحيّة — ٠٣:١٩ بتوقيت الكويت، و`sec.strict_files = 1`:

| الطلب | الرد |
|---|---|
| `GET /m/hr/export` | **403** «نقل الملفات ممنوع خارج وقت العمل» |
| `GET /reports/monthly/export?month=2026-08` | **200** · `text/csv` · ٧٣٤٦ بايت |
| `GET /reports/monthly/export?…&mode=payroll` | **200** · `text/csv` · وفيه عمودُ **«الراتب الأساسي»** |

**الضابطُ الذي وُجد ليمنعَ سحبَ البياناتِ ليلاً كان يمنعُ كشفَ وحدةٍ ويُسلّمُ
كشفَ الرواتب.** والسببُ ليس سهواً في قائمةٍ، بل **تعريفٌ ثالث**: نافذةُ «خارجِ
الدوام» كانت مكتوبةً في الوسيط، ومنسوخةً يدويّاً في `ModuleController`،
و**غائبةً** عن `ReportsController`.

**وأخطرُ ما فيه أنّ الوثيقةَ والاختبارَ كانا يشهدان بالسلامة:** تعليقُ الدالّةِ
يعدّ أحزمةَ التصدير — «تجميدُ الطوارئ + تصعيدُ الحجمِ الكبير + بصمةُ التدقيق» —
فيُسمّي ثلاثةً من أربعة؛ واختبارٌ اسمُه `test_monthly_export_wears_the_export_belt`
يفحص تلك الثلاثةَ بعينِها.

> **القاعدةُ المستخرَجة:** *حزامٌ يُعدَّد ثلاثةُ أرباعِه يقرأ كاملاً. فمن كتب
> «نفسُ الحزام» وعدّد أجزاءَه أثبت ما عدّد و**نفى ما أغفل بلا أن ينتبه** — والاختبارُ
> الذي يتبع التعليقَ يرث الثغرةَ ويُصادق عليها.*

**والإغلاقُ بتعريفٍ واحد:** `hub_after_hours()` للنافذةِ الزمنيّة، وفوقَها
`hub_export_night()` بمفتاحَي التشغيلِ واستثناءِ المالك — يسألُهما البابان.
واختبارُ حدٍّ يُثبت أنّهما يتحرّكانِ في الدقيقةِ نفسِها: ١٩:٥٩ مفتوحان، ٢٠:٠٠
مغلقان.

### وبابانِ تُركا مفتوحَين — **قراران موثَّقان لا سهوان**

| الباب | لماذا لم يُغلَق |
|---|---|
| `endpoints.releases.download` | ثنائيّةُ الوكيلِ الداخليّةُ ليست بياناتِ عمل، والتوزيعُ لكلِّ موظّفٍ مُصادَقٍ **قرارٌ موثَّقٌ** (Permissions 360 · 12.6)، وكلُّ تنزيلٍ مقيَّدٌ في `download_log`. وإغلاقُه يحبس موظّفاً عن تثبيتِ وكيلِه ليلاً بلا استثناءٍ يقابله. |
| `portal.document.download` | بابُ **العميل** لوثائقِه هو. ونافذةُ الدوامِ بتوقيتِ الشركة، والعميلُ قد يكون في منطقةٍ زمنيّةٍ أخرى — فإغلاقُه **نزعُ قدرةٍ** من غيرِ الجمهورِ المقصودِ بالضابط (الوسيطُ يُعرّف نفسَه: «للموظفين غير المالكين»). |

**وفحصُ الاكتمالِ أُجري ولم يُدَّعَ:** `fputcsv` في موضعَين اثنين لا ثالثَ لهما
(كلاهما خلفَ حزامٍ الآن)، ولا نظيرَ تصديرٍ على `/api` (نقاطُ التقاريرِ هناك JSON
لا نقلَ ملفات)، ورفعُ القطع (`upload.chunk`) محروسٌ بالحارسِ الوصفيِّ
`allFiles()` أصلاً.

---

## ٥٫٧ · التحقّقُ الثامن — ما بقي **مفتوحاً بقرارٍ مكتوب**

| # | ما هو | لماذا لم يُغلق في v2.513.0 |
|---|---|---|
| ~~**N-4**~~ | **أُغلق في v2.514.0 — بنطاقٍ أضيقَ ممّا طلب البلاغ.** فحصتُ ما تُعيده الأبوابُ الخمسةُ فعلاً لا عددَها: `quotes.doc` و`purchases.doc` و`esign.doc` تُعيد `view(...)` — **شاشاتُ طباعةٍ بـ`text/html` لا ملفّات**، وحجبُها باسمِ «حظرِ نقلِ الملفات» نزعُ قدرة (وأشدُّها `esign.doc` إذ لا وحدةَ `esign` فلا مفتاحَ استثناءٍ لها). فحُرست **الثنائيّةُ وحدَها** (`quotes.pdf` · `changeorders.pdf`)، واتّسع **الاستثناءُ** معها: خريطةُ «بابٌ ← وحدتُه» في الوسيط و`changeorders` في كتالوجِ `exportNight`. واختبارٌ يحرس الاتّجاهَين. | — |
| **N-5** | الرادارُ يُنذر صاحبَ الشأنِ بوثيقتِه ويسوقه إلى `/me` — **و`/me` لا يعرض أيَّ مرفقٍ إطلاقاً** (لا رابطَ `att.view`/`att.dl` في `resources/views/portal/` كلِّها) | إنذارٌ بلا وجهة. الإغلاقُ **إضافةُ قسمِ وثائقٍ** إلى بوّابة الموظّف — عملُ ميزةٍ لا إصلاحُ سطر. |
| **N-6** | نافذةُ الرادار ليست رقماً واحداً: حقولُ الوحدات **+٣٠ يوماً** ووثائقُ صاحبِ الشأن **±٦٠** | توحيدُها يوسّع رادارَ كلِّ مستخدمٍ أو يضيّقه — **قرارُ منتجٍ لا إصلاحُ عيب**. صُحّحت جملةُ الفراغِ التي كانت تدّعي «٣٠ يوماً» فقط. |
| **N-7** | تصديرُ المالكِ ليلاً يُكتب `تصدير` بلا وسم «خارج الدوام» (`audits#3505`: المالك · كشفُ الرواتب · ٠٣:٤١) | المالكُ مستثنًى من الضابطِ بالتصميم، فلا «استثناءَ» يُوسَم. والتحسينُ المطلوبُ وسمٌ زمنيٌّ مستقلٌّ عن الاستثناء — **إضافةٌ مؤجَّلةٌ لا إصلاح**. |
| **N-8** | زرُّ «⬇ تصدير المحدد» بلا إنذارٍ ليليٍّ رغم أنّ `m.bulk` محظور · ورسالةُ ٤٠٣ تَعِد بمفتاح `exportNight` **غيرِ معروضٍ** لوحداتٍ يصدّرها حاملُ رايةِ `exp` (المفتاحُ في ١١ وحدةً فقط) | الأوّلُ سطرٌ واحد، والثاني **توسيعُ كتالوجِ صلاحيّات** — يُجمعان في دفعةِ اتّساقٍ واحدة. |
| **N-9** | `AccountLockout` يكتب `users` بـ`DB::table` خامّاً بلا `hub_data_bump('users')` | **سابقٌ لهذه الدفعات** وأثرُه شاشةٌ محسوبةٌ قد تتأخّر حتى انتهاء المهلة. خطورةٌ منخفضة. |

**وبلاغٌ من التقريرِ لم يصمد** (انظر §٦): «الشارةُ ٥٠ تناقض الصفحةَ ٥٥».

---

## ٦ · ما لم يصمد (❌) — يُسجَّل لأنّ إخفاءَه تزوير

| البلاغ | المصدر | لماذا سقط |
|---|---|---|
| **`/team` وصولٌ غيرُ مقصود** | خبيرُ الصلاحيّات | فتح الصفحةَ **ونظر**: سطرُها الأوّلُ يُعلن غرضَها، والحسّاسُ محجوبٌ فعلاً — **قرارُ تصميمٍ مُعلَن**. سحبه بنفسِه. |
| **محرّكُ الجودةِ يكتشف الحقولَ الإلزاميّةَ تلقائيّاً** | خبيرُ السيناريوهات | أعاد الاختبارَ فوجد العدّادَ ثابتاً ٧١٦/٢٧؛ ما رآه **أثرُ زميلٍ** على القاعدةِ المشتركة. صحّح نفسَه — **والحقيقةُ أسوأ**: لا شاشةَ تسرد الناقص. |
| **إجازةٌ مفتوحةُ النهايةِ لا تُخصم** | جولةٌ سابقة | اختبارُه **نجح قبل الإصلاح** — النموذجُ يملأ `date_to`. سُجّل **كامناً لا واقعاً**. |
| **سببُ PROD-05** | خبيرُ المنتج | «حقولُ وثائقِ الموظّفِ خارجَ مسحِ الرادار» — **والرادارُ يمسح `hr` صراحةً**. ولو نُفِّذت التوصيةُ لأُضيف مسحٌ قائمٌ **وبقي العطلُ**. |
| **A-1 يفسّر «٧ من ٩ مشاريعَ سليمة»** | **أنا** | استعلمتُ قاعدةَ العرضِ: **صفرُ مهامٍّ بلا حالة**. أسقطتُ الادّعاء. |
| **«القفلُ ثلاثُ ساعات»** | **أنا** | قِستُ بساعةِ UTC والتطبيقُ على **آسيا/الكويت** — كان **١٥ دقيقة**. صُحّح. |
| **«شارةُ التنبيهات تناقض الصفحة»** | التحقّقُ الثامن | `hub_expiry_count()` = `days <= 7`، وهو **مجموعُ سلّتَي الصفحةِ الأوليَين بالضبط** (owner: ٢١+٢٩=٥٠). فالشارةُ عدّادُ العاجلِ لا عدّادُ الصفحة — لا تناقض. **ونصفُ البلاغِ صمد:** جملةُ الفراغِ كانت تدّعي «٣٠ يوماً» والنافذةُ مختلطة. |

**ستّةُ بلاغاتٍ سقطت، اثنانِ منها لي.** وهذا معدّلُ خطأٍ يقارب **١٠٪** من
البلاغاتِ الجوهريّة — ولولا قاعدةُ التحقّقِ لدخلت كلُّها السجلَّ بوصفِها حقائق.
