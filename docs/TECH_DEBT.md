# سجلّ الدَّين التقنيّ — Lynomia Business Hub

**آخرُ قياس: v2.585.0** · والقياسُ السابق v2.400 (١٧٧ إصداراً قبله).

> **سجلٌّ لا يُحدَّث يتوقّف عن كونِه سجلّاً** — يصير وثيقةً تصف حالةً انقضت،
> فيطمئنّ قارئُها إلى ماضٍ. ولذلك تُعاد مقايسةُ كلِّ بندٍ على الشيفرةِ الحاليّة
> **بدليلٍ يُذكَر**، لا بنسخِ حكمٍ قديم.

**الأولوية:** P1 موثوقية/تخويل · P2 جودة/تكامل · P3 أداء/صيانة · P4 تجربة.

**ووسمُ القياس:** `قِيس ٢٠٢٦-٠٩` بندٌ أُعيدت مقايستُه في هذه الجولةِ بأمرٍ
حقيقيّ · `مَنقولٌ من v2.400` بندٌ **لم يُعَد قياسُه** ويُحمَل كما كُتب —
**ويُقرَأ بحذرٍ حتّى يُقاس**.

---

## ٠) ما تغيّر منذ v2.400 — الخلاصةُ أوّلاً

| الحكم | العدد | البنود |
|---|---|---|
| **أُغلق** (بدليل) | ٣ | #9 · #10 · #17 (جزئيّاً) |
| **ما زال مفتوحاً** (بدليل) | ١٣ | #1 · #5 · #8 · #11 · #12 · #13 · #22 · #25 · #26 · #29 · #31 · #33 · #34 |
| **نما** | ٢ | #30 · #31‑QE10 |
| **جزئيّ** | ١ | #28 |
| **مَنقولٌ بلا إعادةِ قياس** | ١٥ | #2·#3·#4·#6·#7·#14·#15·#16·#18·#19·#20·#21·#23·#24·#27·#32 |
| **مؤجّلاتُ مستوى التحكّم** | ١٥ | D1–D15 — حدودٌ مُعلَنةٌ لا عيوب |
| **بنودٌ جديدةٌ في هذه الجولة** | ٤ | #35 · #36 · #37 · #38 |

**والبندانِ اللذانِ نَمَيا لم ينمُوَا بقرار — بل لأنّ لا أحدَ كان يعدّ.**
وقد وُضع عليهما سقفٌ آليٌّ في هذه الدفعة (#35).

---

## ١) ما أُغلق — بدليل

| # | المرجع | كان | القياسُ الآن (٢٠٢٦-٠٩) |
|---|---|---|---|
| 9 | CFG-03 | Laravel 11 خارج نافذة الدعم الأمنيّ | **`Laravel Framework 12.69.1` · `PHP 8.4.19`** ⟵ أُغلق |
| 10 | CFG-10 | `league/commonmark` بإرشادٍ أمنيّ | **`composer audit`: لا تنبيهات** ⟵ أُغلق |
| 17أ | AUD-12 | `hub:audit-verify` يحمّل الفهرسَ كلَّه في الذاكرة | **`HubAuditVerify.php:109,300` يستعمل `array_chunk` بـ`self::BATCH`** ⟵ أُغلق. **و١٧ب (ثوابتُ أفعالِ التدقيق) ما زال مفتوحاً** |

---

## ٢) ما نما — وصار له سقف

| # | المرجع | ط | v2.400 | v2.585 | الحارسُ الجديد |
|---|---|---|---|---|---|
| 30 | ARCH-05 | P3 | `helpers.php` ٥١٠٠ سطراً | **٨٠٤٣ سطراً (+٥٨٪)** | `TechDebtCeilingTest::test_سقفُ_ملفِّ_المساعدات` |
| 31 | QE-10 | P2 | `::first()` بلا ترتيب في ٣٦ موضعاً | **٦٢ موضعاً مشبوهاً (+٧٢٪)** | `TechDebtCeilingTest::test_سقفُ_الصفِّ_الأوّلِ` |

**وتصحيحٌ في القياسِ نفسِه:** مسحٌ بسطرٍ واحدٍ أعطى ٧٩ موضعاً، لأنّ
`->orderBy(...)` كثيراً ما تقع في **السطرِ الذي فوق** `->first()` فلا يراها.
والمسحُ بنافذةِ أربعةِ أسطرٍ أعطى **٦٢** — وهو الأصدق. **والحارسُ نفسُه كشف
الرقمَ الكاذبَ عند أوّلِ تشغيل.**

---

## ٣) ما زال مفتوحاً — بدليلٍ من هذه الجولة

| # | المرجع | ط | الوصف | دليلُ ٢٠٢٦-٠٩ | ما يلزم |
|---|---|---|---|---|---|
| 1 | AUD-07 | P2 | قفلُ صفِّ رأسِ سلسلةِ التدقيق يُحمَل طوالَ المعاملة | `AuditEntry.php:82` — `->where('id',1)->lockForUpdate()` | ختمٌ مؤجَّل (outbox) أو سلسلةٌ لكلِّ شركة + اختبارُ تزامنٍ على MySQL |
| 5 | DI-09 | P4 | `record_locks` سكّةٌ بلا مستهلك | `app/Models/RecordLock.php` قائمٌ، ولا قارئَ غيرَ النسخِ الاحتياطيّ | استعمالُه لقفلِ التحريرِ المتزامن أو إسقاطُه في إصدارٍ رئيسيّ |
| 8 | CFG-02 | P1 | النسخةُ محلّيّةٌ فقط | `config/filesystems.php:26` — `backups` بسائقٍ `local` وحدَه | قرصُ S3/SFTP + `backup.disk` + تمرينُ استعادةٍ مسجَّل |
| 11 | CFG-11 | P4 | مفاتيحُ `services.php` ميّتة | `config/services.php:4,11,18` — `clamav`/`updates`/`telegram` والإعداداتُ الحيّةُ في القاعدة | حذفٌ مع مدخلٍ في سجلِّ الإصدارات |
| 12 | FE-03 | P3 | لا `script-src` في CSP | `SecurityHeaders.php:34-39` — التعليقُ نفسُه يُقِرّ بأنّ الحصرَ يكسر الصفحات | Report-Only أوّلاً ثمّ nonce تدريجيّاً |
| 13 | FE-02 | P3 | بلاطاتُ الخريطةِ من طرفٍ ثالث | ٣ مواضعَ تشير إلى OpenStreetMap | خادمُ بلاطاتٍ خاصّ أو وكيلٌ داخليّ |
| 22 | FS-04 | P3 | قوائمُ الامتداداتِ غيرُ موحَّدة | `hub_upload_blocklist` **غيرُ موجودةٍ** (٠ موضع) | دالّةٌ واحدة + اختبارٌ يمرّ على المساراتِ الخمسة |
| 25 | PERF-05 | P3 | استبطانُ المخطَّطِ لكلِّ طلب | **٦٧ موضعَ `Schema::hasColumn`** في `app/` | خريطةُ أعمدةٍ في كاشٍ لكلِّ عمليّةٍ لا لكلِّ طلب |
| 26 | PERF-06 | P3 | البحثُ العامُّ بـLIKE على أعمدةٍ متعدّدة | `SearchController.php:228` يوثّق «٨١ استعلام LIKE للضغطة»، و**لا هجرةَ فهرسِ بحثٍ واحدة** | جدولُ `search_index` أو FULLTEXT |
| 29 | ARCH-06/07/08/09 | P3 | قواعدُ مكرّرةٌ بين الويبِ والتوقيعِ والمحاسبة | **`app/Services/` غيرُ موجودٍ أصلاً** | `QuoteAcceptance` · `JournalPosting` · توحيدُ الإقرارات |
| 31 | QE-02 | P3 | `HasFactory` على ٨١ نموذجاً بلا مصانع | **`database/factories/` فارغٌ (٠ مصنع)** | مصانعُ للنماذجِ الجوهريّةِ أوّلاً |
| 31 | QE-05 | P2 | لا تحليلَ ساكن | **لا `phpstan.neon` ولا `psalm.xml` ولا أداةٌ في `vendor/bin`** | انظر #36 |
| 33 | CP-49-A | P3 | `whereDate()` غيرُ قابلٍ للفهرسة في محرّكِ التنبيه | `AlertEngine.php:129,130,145,211,216` — **خمسةُ مواضعَ الآن** | عمودُ `dedup_day` مفهرَس + اختبارُ «انزياحُ الكرون لا يُعيد الإطلاق» |
| 34 | CP-49-B | P4 | `privileged()` يقرأ صفوفاً خارجَ النطاق | `SecurityController.php:560-565` — لا `whereIn` على المعروضِ بعد | `whereIn('entity_id', …)` + اختبارُ نطاق |

---

## ٤) بنودٌ مَنقولةٌ من v2.400 — **لم تُعَد مقايستُها**

> تُحمَل كما كُتبت. **ولا تُقرَأ حكماً على الحاضر** حتّى تُقاس — فثلاثةٌ من
> إخوتها ثبت أنّها أُغلقت، واثنان ثبت أنّهما نما.

`#2` DI-05/06 تنصيبٌ على `explicit_defaults_for_timestamp=OFF` · `#3` DI-07
كتاباتٌ بلا معاملة · `#4` DI-11 حذفُ أبٍ يترك أبناءً معلّقين · `#6` F-12
المتكرّراتُ ليست ذرّية · `#7` F-13 النسخةُ تُبنى في الذاكرة · `#14` API-10
2FA لا تنطبق على رموزٍ سابقة · `#15` AUTH-08/09 درجةُ الخطرِ عرضٌ لا فعل ·
`#16` AUD-11 ملفّاتُ الحقولِ بلا أثرِ تنزيل · `#17ب` AUD-13 أفعالُ التدقيقِ
نصوصٌ حرّة · `#18` OPS-09 صفحةُ التشغيلِ تعدّ جداولَ كاملة · `#19` ERR-09
فشلُ تخزينِ ردِّ Idempotency لا يمنع تنفيذاً ثانياً · `#20` INT-05 ربطُ
التوقيعِ بالزمنِ اختياريّ · `#21` FS-03/05/06 روابطُ غرفةِ البياناتِ غيرُ
منطَّقة · `#23` OPS-04 مفتاحُ الرجلِ الميّتِ يعتمد على زيارةِ ويب · `#24`
AUTH-10 كلمةُ سرِّ التوقيعِ قد تكون ٤ أحرف · `#27` PERF-11/12/13 صفحةُ
المشروعِ ٧٢ استعلامَ أبناء · `#32` DI-05 ملاحظةُ CI.

**وإعادةُ قياسِ هذه الخمسةَ عشرَ بنداً هي أوّلُ ما يُفعَل في الجولةِ التالية.**

---

## ٥) ما لا يُصلَح من الشيفرة — **قراراتُ مالك**

> هذه ليست دَيناً هندسيّاً. لكلٍّ منها **كلفةٌ على طرفٍ ثالث** — تكاملٌ قائمٌ
> أو مُرسِلٌ أو دلالةٌ مكتوبة — فإصلاحُها بلا قرارٍ **يكسر شيئاً يعمل**.

| # | البند | ما يكسره الإصلاحُ بلا قرار |
|---|---|---|
| 14 | فرضُ 2FA على رموزِ API الصادرة سلفاً | التكاملاتُ القائمةُ تتوقّف |
| 20 | فرضُ `X-Hub-Timestamp` على الوارد | المُرسِلونَ القدامى يُرفَضون |
| 24 | رفعُ الحدِّ الأدنى لكلمةِ سرِّ التوقيع | قوالبُ وطلباتٌ قائمة |
| 33 | تحويلُ `whereDate` إلى مدًى | **دلالةُ** مانعِ التكرارِ تتغيّر من «يوم» إلى «٢٤ ساعة» |
| 5 | حذفُ `record_locks` | **هجرةٌ مدمِّرة** |

---

## ٦) الطبقاتُ ٣ و٤ و٥ — **مفتوحةٌ لا مُلغاة**

> برنامجُ المراجعةِ السداسيّ (`docs/ultimate-platform-review/`) أُغلقت طبقاتُه
> ١ و٢ و٦. **ثمّ انقطع البرنامجُ لا لأنّه أُلغي، بل لأنّ العملَ تحوّل إلى
> مركزِ الذكاء** (المراحل ٠→٥). فبقيت الثلاثُ حيث تركها الانقطاع.

| # | الطبقة | ط | ما سُلّم | ما بقي | شرطُ الإغلاق |
|---|---|---|---|---|---|
| 35أ | **٣ · حقيقةُ الميزات** | **P2** | سطحُ الإعداداتِ فُحص: **١٧٧ مفتاحاً مقروءاً · صفرُ مخالف** | **~١٠٦ إعداداً غيرَ أمنيٍّ لم يُقلَب ويُرصَد أثرُه** | كلُّ مفتاحٍ يُقلَب ويُثبَت أنّ أثرَه الفعليَّ يطابق وصفَه |
| 35ب | **٥ · الواجهة** | **P2** | ثلاثُ وثائق — **وكلُّها عيوبٌ أمنيّةٌ حقيقيّة** (وثائقُ سرّيّةٌ تُسمّى · كاشُ تقويمٍ يعبر بين المستخدمين · اسمٌ بلا إذن) | بقيّةُ الرحلاتِ لم تُفحَص | قبل/بعد بلقطاتٍ للرحلةِ نفسِها |
| 35ج | **٤ · فجواتُ المنتج** | P4 | ثلاثُ وثائق (قاعدةُ الأدلّة · ما تقوله المنصّةُ بلا سؤال · قراران للمالك) | مجلسُ الخبراءِ لم يكتمل | مخرَجُه **مقترحاتٌ يقرّرها المالك** لا إصلاحاتٌ تُنفَّذ |

**والأعلى قيمةً ٣٥أ:** مفتاحٌ يَعِد في الشاشةِ ولا يفي **كذبةٌ على
المستخدم**، وشرطُ إغلاقِه واضحٌ وقابلٌ للقياس.
**و٣٥ب عائدُه مُثبَتٌ بالسابقة:** ما سُلّم منها كان عيوباً أمنيّةً لا تجميلاً.

---

## ٧) بنودٌ جديدةٌ في هذه الجولة

| # | ط | البند | الحال |
|---|---|---|---|
| 36 | P2 | **لا تحليلَ ساكن** — ١١٠ آلافِ سطرٍ بلا شبكةٍ ثانية | **لم يُركَّب في هذه الدفعة**: `composer require --dev phpstan/phpstan` أخفق بـ«Could not authenticate against github.com» في بيئةِ التنفيذ. **يُركَّب من الخادمِ أو من بيئةٍ بشبكة**، ثمّ `--generate-baseline` يقبل الحاضرَ ويمنع الغد. **وتركيبُه يغيّر `composer.lock` فيلزم `composer install --no-dev` عند النشر.** وسُدَّ صنفانِ منه مؤقّتاً بـ#35 |
| 37 | P3 | **زمنُ البوّابة ~٣٠ دقيقة** — SQLite ١٠:٤١ + MariaDB ١٩:٠٣ | ضريبةُ سرعةٍ تكبر مع كلِّ اختبار. الحلُّ توازٍ (`paratest`) أو تقسيمُ الحزمةِ في CI — **ولا يُخلَط بتقليلِ التغطية** |
| 38 | P4 | **رمزا إخفاقٍ مُعلَنانِ بلا مُنتِج** — `UNSAFE_TOOL_ARGUMENTS` · `NO_ACCESSIBLE_DATA` | **دَينٌ مُدار**: يحرسه `AskAdversarialSweepTest` ويُسقِط الحزمةَ إن صار لأحدِهما مُنتِجٌ بلا اختبار. يُترَك عمداً |
| 35 | P2 | **سقوفُ الدَّين** — `TechDebtCeilingTest` | **أُنجز في هذه الدفعة**: ثلاثةُ سقوفٍ تقبل الحاضرَ وتمنع الغد |

---

## ٨) مؤجّلاتُ مستوى التحكّم (D1–D15) — **حدودٌ مُعلَنةٌ لا عيوب**

> بندٌ لا يمكن إنجازُه صادقاً لغياب بياناتٍ أو بنيةٍ أو مزوّدٍ حقيقيّ، فسُلّم
> بدلَه ما يمكن إثباتُه، وصُرِّح بالنقصِ في الشاشةِ نفسِها.

`D1` نسبٌ مضبوطةٌ (تحتاج t-digest) · `D2` تاريخٌ قبل النشرِ (زمنٌ فقط) ·
`D3` حكمُ SLO (نافذةٌ متراكمة) · `D4` التحميلُ المتزامن (عاملُ طوابيرَ حقيقيّ)
· `D5` geo-IP (مزوّدٌ + سياسةُ خصوصية) · `D6` `first_seen_at` التاريخيّ (يُغلق
بالزمن) · `D7` ملءٌ رجعيٌّ للتدقيق (يُحلّ مع #1) · `D8` إغلاقُ الحادثةِ آليّاً
(**قرارُ تصميمٍ لا دَين**) · `D9` تكاملٌ خارجيٌّ حقيقيّ (بيئةُ تكامل) · `D10`
استعادةٌ من الويب (تدفّقُ تأكيدٍ متعدّد) · `D11` `script-src` (يُحلّ مع #12) ·
`D12` تدويرُ رمزِ مستخدمٍ آخر (**قرارُ منتج**) · `D13` فحصُ متصفّحٍ حقيقيّ
(متصفّحٌ في CI) · `D14` قياسُ أداءٍ بحملٍ إنتاجيّ (نسخةٌ مجهَّلة) · `D15`
مراقبةٌ خارجيّة (نقطتا امتدادٍ موصوفتان).

**التفصيل:** `docs/CONTROL_PLANE.md` وجدولُ DEFERRED في
`docs/control-plane/PLAN.md`.

---

## ٩) وما ليس دَيناً — يُقال إنصافاً

- **صفرُ `TODO`/`FIXME`/`HACK`/`@deprecated`** في ١١٠ آلافِ سطر. الاصطلاحُ
  هنا: التأجيلُ يُوثَّق بسببِه وشرطِ إغلاقِه، **لا يُترَك علامةً في الشيفرة**.
- **الحزمتان خضراوان** على المحرّكين: ٥٣٢٨ اختباراً · صفرُ إخفاق.
- **`composer audit` نظيف** · `hub:schema-check` بلا فروقات على المحرّكين.
- **كلُّ تأجيلٍ هنا بثلاثةِ أعمدة**: ماذا · لماذا لم يُصلَح · وما يلزم لإغلاقِه.

---

## ١٠) الجولةُ التالية — بهذا الترتيب

1. **إعادةُ قياسِ الخمسةَ عشرَ بنداً المَنقولة** (§٤) — فثلاثةٌ من إخوتها
   ثبت أنّها أُغلقت، واثنان نما. **لا يُصلَح بندٌ لم يُتحقَّق أنّه قائم.**
2. **#36 التحليلُ الساكن** من بيئةٍ بشبكة — أوسعُ حارسٍ بأقلِّ كلفة.
3. **#35أ الطبقة ٣** — ١٠٦ إعداداً تُقلَب ويُرصَد أثرُها.
4. ثمّ يختار المالكُ من §٣ و§٥.
