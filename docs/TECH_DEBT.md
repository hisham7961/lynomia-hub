# سجلّ الدَّين التقنيّ — Lynomia Business Hub (v2.399)

> ما عُرف ولم يُصلَح في هذه الدفعة، **بسببه** وبما يلزم لإغلاقه. كلُّ بندٍ هنا وُثّق من
> تدقيقٍ حيٍّ (اختبارٌ أو أمرٌ أو استعلام) لا من قراءةٍ فقط إلا حيث يُقال `STATICALLY_REVIEWED`.
> الأولوية: P1 موثوقية/تخويل · P2 جودة/تكامل · P3 أداء/صيانة · P4 تجربة.

| # | المرجع | الأولوية | الوصف | لماذا لم يُصلَح الآن | ما يلزم |
|---|---|---|---|---|---|
| 1 | AUD-07 | P2 | قفلُ صفّ رأس سلسلة التدقيق (`audit_chain`) يُحمَل طوالَ المعاملة الخارجية (شراءٌ كبير يحبس كلَّ كاتبٍ آخر حتى الالتزام). `DATABASE_VERIFIED` (جلسةٌ ثانية تسقط بـ1205). | إعادةُ هيكلة الختم إلى ما بعد الالتزام تُغيّر دلالةَ السلسلة (قيدٌ يُختم بعد المعاملة لا داخلها) وتحتاج تصميماً وهجرةً. | ختمٌ مؤجَّل (outbox للتدقيق) أو سلاسلُ لكل شركة؛ اختبارُ تزامنٍ على MySQL. |
| 2 | DI-05/06 | P2 | تنصيبٌ جديد على خادمٍ `explicit_defaults_for_timestamp=OFF` مع sql_mode الصارم يفشل في `create sessions_log` (عمود timestamp ثانٍ NOT NULL بلا افتراضي). `DATABASE_VERIFIED`. | تعديلُ ١٥ هجرةً قائمة يمسّ ملفاتٍ طُبّقت في الإنتاج؛ يحتاج تحقّقاً على MariaDB 10.6 وMySQL 8 معاً. | `->useCurrent()`/`nullable()` للأعمدة المعنيّة + تشغيلُ الهجرات على قاعدةٍ بالوضعين في CI. |
| 3 | DI-07 | P2 | كتاباتٌ متعدّدة بلا معاملة: توليدُ الرواتب، حفظُ طلب التوقيع، ترقيةُ الفكرة، الفواتيرُ المتكرّرة. `STATICALLY_REVIEWED`. | إحاطتُها بمعاملةٍ تُطيل حبسَ قفل السلسلة (#1) — يُحلّان معاً. | بعد #1: `DB::transaction` حول كلٍّ منها + اختبارُ فشلٍ في المنتصف. |
| 4 | DI-11 | P3 | حذفُ أبٍ (عميل) يترك أبناءه (فواتير) بمراجعَ معلّقة بلا تحذير. `RUNTIME_VERIFIED`. | لا مفاتيحَ أجنبية بالتصميم (مفتاحٌ متعدّد الأشكال)؛ التحذيرُ يحتاج خريطةَ اعتماديّاتٍ في السجلّ. | إعلانُ `depends_on` في `config/hub.php` وفحصٌ قبل الحذف الناعم. |
| 5 | DI-09 | P4 | `record_locks` / `RecordLock` سكّةٌ بلا مستهلك. | حذفُ جدولٍ هجرةٌ مدمِّرة؛ الإبقاءُ بلا ضرر (مُعلَنٌ عابراً في النسخة). | إمّا استعمالُه لقفل التحرير المتزامن أو إسقاطُه في إصدارٍ رئيسيّ. |
| 6 | F-12 | P2 | توليدُ المتكرّرات ليس ذرّياً لكل دورة (مستندٌ ثم إشعاراتٌ ثم المؤشّر). `STATICALLY_REVIEWED`. | مرتبطٌ بـ#1/#3. | معاملةٌ لكل دورة + `idempotency` على (recurring_id, cycle). |
| 7 | F-13 | P3 | `hub:backup` يبني النسخةَ كلَّها في الذاكرة ثم يُرمّزها (ضعفُ الحجم). | يعمل ضمن حدود الاستضافة الحالية؛ البثُّ يحتاج كاتبَ JSON تدفّقياً وتغييرَ صيغة القراءة. | كتابةٌ تدفّقية (JSON Lines لكل جدول) مع دعم القراءة القديمة. |
| 8 | CFG-02 (جزئياً) | P1 | النسخةُ محلّيةٌ فقط (لا نسخَ خارج الخادم). التشفيرُ (`backup.encrypt`) والتحقّقُ (`--verify`) أُضيفا. | لا قرصَ بعيداً مضبوطاً على الاستضافة الهدف؛ أيُّ رفعٍ خارجيّ يحتاج اعتماداً ومزوّداً. | قرصُ `backup` في `filesystems.php` (S3/SFTP) + إعدادُ `backup.disk` + تمرينُ استعادةٍ مسجَّل في وحدة «اختبارات الاستعادة». |
| 9 | CFG-03 | P1 | Laravel 11 خارج نافذة الدعم الأمني. | ترقيةٌ رئيسية (12/13) تحتاج تشغيلَ الحزمة على المحرّكين ومراجعةَ التغييرات الكاسرة — لم تُدرَج في هذه الدفعة كي لا تختلط بالتحصين. | `composer require laravel/framework:^12` على فرعٍ منفصل + الحزمتان + مراجعةُ upgrade guide. |
| 10 | CFG-10 | P2 | `league/commonmark` بإرشاداتٍ (غيرُ متاحةٍ من شيفرة التطبيق). | حلُّه بترقية الحزمة يُغيّر `composer.lock` ويحتاج شبكةَ Composer على الخادم. | `composer update league/commonmark` + بوّابةُ `composer audit` في CI (أُضيفت كخطوةٍ غيرِ حاجزة). |
| 11 | CFG-11 | P4 | مفاتيحُ `config/services.php` (telegram/updates/clamav) ميّتة — الإعداداتُ الحيّة في القاعدة. | حذفُها لا يُغيّر سلوكاً؛ يُؤجَّل إلى تنظيفٍ عامّ. | حذفٌ مع مدخلٍ في سجل الإصدارات. |
| 12 | FE-03 | P3 | لا `script-src` في CSP (٢٢ سكربتاً مضمّناً و٤٥ معالجَ حدثٍ مضمّناً). | تبنّي nonce يتطلب إعادةَ كتابة كل سكربتٍ مضمّن — عملٌ عرضيّ كبير. | مرحلةُ Report-Only أولاً ثم nonce تدريجياً. |
| 13 | FE-02 (جزئياً) | P3 | خريطةُ المسار الميدانيّ تجلب البلاطات من OpenStreetMap (يتسرّب موقعُ الموظف إلى طرفٍ ثالث). Leaflet صار محلّياً. | لا خادمَ بلاطاتٍ ذاتيّاً. | إعدادُ `map.tiles` لخادمٍ خاصّ أو وكيلٍ داخليّ. |
| 14 | API-10 | P3 | سياسةُ 2FA للمميّزين لا تنطبق على رموز API الصادرة سلفاً. | الرمزُ اعتمادٌ مستقلّ بتصميمه؛ فرضُ 2FA عليه يكسر التكاملات القائمة. | خيارُ `api.require_2fa_owner` يمنع سكَّ رموزٍ لحسابٍ بلا 2FA حين تُفعَّل السياسة. |
| 15 | AUTH-08/09 | P3 | درجةُ الخطر و«السفر المستحيل» عرضٌ لا فعل؛ لا تاريخَ لكلمات المرور ولا استعادةً ذاتية. | ميزاتٌ جديدة لا إصلاحات. | سياسةُ كلمات مرور (تاريخ ٥) + استعادةٌ بالبريد + فعلٌ عند القفز الجغرافيّ. |
| 16 | AUD-11 | P3 | ملفاتُ حقول الوحدات المخدومة من `FileController` بلا أثر تنزيل. | المسارُ يخدم بالمسار لا بالمرفق؛ ربطُه بسجلٍّ يحتاج فهرسَ مسارٍ ← سجلّ. | جدولُ `file_refs` أو تسجيلٌ بالمسار في `download_log` بعمود nullable. |
| 17 | AUD-12/13 | P3 | `hub:audit-verify` يحمّل فهرسَ السلسلة كلَّه في الذاكرة؛ أفعالُ التدقيق نصوصٌ حرّة (~١٢٠) بلا ثوابت. | التصنيفُ القانونيّ أُضيف (`SecurityEvents::CODES`) فوق النصوص؛ الثوابتُ تغييرٌ واسع. | تحقّقٌ بالدفعات + ثوابتُ الأفعال تدريجياً. |
| 18 | OPS-09 | P3 | صفحةُ التشغيل تعدّ جداولَ كاملة وتمشي المجلدات في كل تحميل. | لم يُقَس أثرُها بعد؛ يُصلَح بالقياس لا بالحدس. | `Cache::remember` ٦٠ ثانية للعدّادات + قياسٌ قبل/بعد. |
| 19 | ERR-09 (جزئياً) | P3 | فشلُ تخزين ردّ Idempotency صار يُبلَّغ (`report`) لكنه لا يمنع تنفيذاً ثانياً. | المنعُ يحتاج تخزينَ الردّ في المعاملة نفسِها مع الكتابة. | تخزينٌ داخل المعاملة أو رفضُ الإعادة حين يغيب الردّ المخزَّن. |
| 20 | INT-05 (جزئياً) | P3 | ربطُ توقيع الوارد بالزمن اختياريٌّ (بترويسة `X-Hub-Timestamp`)؛ المُرسِلون القدامى يبقون بتوقيع الجسم. | فرضُه يكسر المُرسِلين القائمين. | إعدادُ `hooks.require_timestamp` يُفعَّل بعد تحديث المُرسِلين. |
| 21 | FS-03/05/06 | P3 | روابطُ غرفة البيانات غيرُ منطَّقة بالشركة/المنشئ؛ رمزُ OTP للتوقيع محدودٌ لكل IP لا لكل رمز؛ رفعُ الأجزاء بلا سقفٍ لكل مستخدم. | كلٌّ منها تغييرُ سياسةٍ يحتاج قراراً (من يرى روابطَ من؟). | تنطيقُ الروابط بـ`created_by`/الشركة؛ عدّادُ محاولاتٍ لكل رمز؛ سقفُ أجزاءٍ متزامنة لكل مستخدم. |
| 22 | FS-04 | P3 | قوائمُ الامتدادات المحظورة غيرُ موحَّدة بين مسارات الرفع. | دمجُها في دالّةٍ واحدة يمسّ خمسةَ مسارات. | `hub_upload_blocklist()` واحدة + اختبارٌ يمرّ على المسارات كلِّها. |
| 23 | OPS-04 (جزئياً) | P2 | مفتاحُ الرجل الميّت يعتمد على زيارةِ ويبٍ من مستخدمٍ مسجَّل. | لا مراقبةَ خارجيةً مضبوطة. | مراقبٌ خارجيّ يستدعي `/healthz?probe=ready` ويُنذر عند `MAINTENANCE`/`UNAVAILABLE`. |
| 24 | AUTH-10 | P3 | كلمةُ سرّ رابط التوقيع الإلكتروني قد تكون ٤ أحرف (رُفعت غرفةُ البيانات إلى ٨). | ٤٠ اختباراً وقوالبَ تُنشئ طلباتٍ بكلماتٍ من ٥ أحرف؛ رفعُ الحدّ قرارُ منتجٍ يمسّ المُرسِلين القائمين. | قرارٌ + `esign.pass_min` بإعدادٍ افتراضيّه ٨ للطلبات الجديدة + تحديثُ القوالب. |
| 25 | PERF-05/08 | P3 | استبطانُ المخطّط (`Schema::hasColumn`) لكل طلبٍ للمستخدم المعزول (٥٤ استبطاناً في البحث)، وبصمةُ البيانات (`hub_data_stamp`) قراءةُ كاشٍ لكل جدول (٩٠–١٦٥ قراءةً). `RUNTIME_VERIFIED`. | يحتاج ذاكرةَ مخطّطٍ لكل عملية (لا لكل طلب) وبصمةً واحدة لكل صفحة. | تخزينُ خريطة الأعمدة في ملف كاشٍ واحد + بصمةٌ مجمَّعة. |
| 26 | PERF-06 | P3 | البحثُ العامّ يطلق LIKE على أعمدةٍ متعدّدة لكل وحدةٍ مرئية عند كل ضغطة (٨١–١٣٦ استعلاماً). | يحتاج فهرسَ بحثٍ (FULLTEXT أو جدولَ فهرسة). | جدولُ `search_index` يُغذّى من الأحداث + FULLTEXT على MySQL. |
| 27 | PERF-11/12/13 | P3 | صفحةُ المشروع تتفرّع إلى ٧٢ استعلامَ أبناء؛ رادارُ الانتهاء يلفّ التواريخ بـ`DATE()` (غيرُ قابلٍ للفهرسة)؛ صفحةُ الصباح تحسب صحّةَ كل مشروع على البرود. | كلٌّ منها إعادةُ تصميمٍ للقراءة لا خبيئة. | حصرُ الأبناء بما له صفوف؛ نطاقٌ زمنيّ بلا `DATE()`؛ لقطةُ صحّةٍ مخزَّنة. |
| 28 | ARCH-04/16 | P2 | إنشاءاتٌ عابرة للمجالات (عرض ← عقد/فاتورة) لا تمرّ بناقل الأحداث فلا ويبهوك ولا مسار؛ الأحداثُ متزامنة داخل المعاملات بلا سجلٍّ دلاليّ. `RUNTIME_VERIFIED`. | يحتاج تعريفَ أحداثٍ مجاليّة (`quote.converted`…) ونقاطَ إطلاقٍ متّفقاً عليها. | `HubEvents::fire` في مسارات التحويل + جدولُ `domain_events` يُصرَف من العامل. |
| 29 | ARCH-06/07/08/09 | P3 | قاعدةُ قبول العرض تختلف بين الويب والتوقيع؛ نظامان للإقرارات (`policy_acks`/`record_acks`)؛ منطقُ الترحيل المحاسبيّ مكرَّر بعلَمٍ ساكن؛ ~٢٥ مصفوفةَ حالاتٍ مغلقة حرفية. | توحيدٌ يمسّ مساراتٍ حيّة ويحتاج قراراً على الدلالة الواحدة. | خدمةُ `QuoteAcceptance` واحدة؛ ترحيلُ `policy_acks` إلى `record_acks`؛ `JournalPosting`؛ `hub_closed_states` في كل موضع. |
| 30 | ARCH-05/10/11/12/15/18 | P3 | `helpers.php` ٥١٠٠ سطر؛ الأفعالُ المجاليّة ويبٌ فقط (الـAPI يكتب حقولاً خاماً)؛ استعلاماتٌ داخل Blade؛ الطبقةُ المشتركة مقرونةٌ بالطلب؛ ١٩ فرعاً خاصّاً بوحدةٍ في المحرّك؛ `gate()` مكرَّر في ٢٥ متحكّماً. | إعادةُ هيكلةٍ عرضية — لا تُخلَط بدفعة تحصين. | استخراجٌ تدريجيّ إلى `app/Services` بحرّاس الاختبارات القائمة؛ أفعالٌ مجاليّة على `/api/v1/{module}/{id}/actions/{do}`. |
| 31 | QE-02/03/04/05/09/10/11 | P3 | `HasFactory` على ٨١ نموذجاً بلا مصانع؛ الاختباراتُ تكتب في `storage/app` الحقيقي؛ لا `preventStrayRequests`؛ لا تحليلَ ساكناً (phpstan)؛ اعتمادٌ على ساعة الجدار؛ `::first()` بلا ترتيب في ٣٦ موضعاً؛ لا مصدرَ بياناتٍ لصحّة الاختبارات. | كلٌّ منها تغييرٌ في عُدّة الاختبار يمسّ ٣٤٦ صنفاً أو يحتاج حزمةً جديدة عبر الشبكة. | قرصُ تخزينٍ وهمي في `TestCase`؛ `Http::preventStrayRequests` مع تصحيح العامل؛ phpstan بمستوى ١ وقاعدةٍ أساسية؛ `Carbon::setTestNow` في `setUp`. |
| 32 | DI-05 (ملاحظة) | P2 | `hub:schema-check` في CI يعمل على MySQL 8 الافتراضي — لا يغطّي `explicit_defaults_for_timestamp=OFF` (MariaDB القديمة). | انظر #2. | مصفوفةُ CI بصورة MariaDB 10.6 إضافية. |

## ما أُغلق في هذه الدفعة (للمقارنة)

الجولاتُ الأربع (`EnterpriseHardeningRound1/2/3Test` + جولةُ الأداء/المعمارية) أغلقت: AUTHZ-01…09، AUTH-01…07، AUD-01…06/08/09/10/14،
OPS-01…03/05/07/08، ERR-01…08/10/11، INT-01…04/06/07/09/10، API-01…09، F-01…11، DI-01…04/08/12،
FS-01/02، FE-01/02/04، CFG-01/03…10/12/13، PERF-01/02/03/04/07/09/10، ARCH-01/02/03، QE-01/06/07/08. التفصيلُ في `docs/ENTERPRISE_UPGRADE_REPORT.md`.
