# قبولُ الإطلاق — المرحلة ٥

> **W13.** وهذه الوثيقةُ **عقدُ القبول**: ما يُعَدُّ منجزاً، وبأيِّ دليل،
> وما بقي قراراً للمالكِ لا عملاً مؤجَّلاً.

---

## ١) النطاقُ كما جُمّد

الخطّةُ الأصليّةُ للمرحلةِ ٥ في `02-phases.md:48` ثلاثةُ أسطر:

> **المرحلة ٥ — التوسّعُ والإطلاق**
> توسيعُ الأقسامِ ومصادرِ المعرفة · اختبارُ العزلِ والأداء · تجربةُ التشغيلِ
> والاستعادةِ والتحديث.

والمصالحةُ الكاملةُ في `40-phase5-discovery.md`، والنطاقُ المجمَّدُ في
`41-phase5-plan.md`. **ولا `WRITE_TOOLS` في أيِّ مرحلةٍ من الخطّةِ الأصليّة**
— فهي `[]` وتبقى، وتوسيعُها قرارُ مالكٍ لا تفصيلُ تنفيذ.

---

## ٢) مصفوفةُ القبول

| # | الشرط | الدليل | الحالة |
|---|---|---|---|
| ١ | سلسلةُ الثوابتِ كاملةً تُثبَت آليّاً في طلبٍ واحد | `AskReleaseInvariantsTest::test_السلسلةُ_كاملةً…` | ✅ |
| ٢ | السؤالُ يصل النموذجَ نصّاً قائماً بذاته | `AskQuestionPropagationTest` (٩) + الحلقة ① | ✅ |
| ٣ | كتالوجُ الأدواتِ مُعلَنٌ ومن مفرداتٍ مغلقة | الحلقة ② | ✅ |
| ٤ | طلبُ أداةٍ إلى ما لا يملكه السائلُ لا يُنفَّذ | `test_طلبٌ_إلى_ما_لا_يملكه_السائلُ…` | ✅ |
| ٥ | نتيجةُ الأداةِ تعود مربوطةً بـ`tool_call_id` | الحلقة ⑥ | ✅ |
| ٦ | المصادرُ تُبنى من القراءةِ لا من نصِّ الجواب | `test_مصدرٌ_مُختلَقٌ…` + `FORGED_SOURCE` | ✅ |
| ٧ | جوابٌ بلا قراءةٍ يُصنَّف ولا يُعرَض | `NO_SERVER_READ` · `UNSOURCED_NUMBER` | ✅ |
| ٨ | `WRITE_TOOLS = []` مكتوبةٌ في الشيفرةِ ومحروسة | `AskTools::WRITE_TOOLS` + `WRITE_TOOLS_CLOSED` | ✅ |
| ٩ | **ستُّ بوّاباتٍ مفصولة** (قدرة/توافر/صحّة/سياسة/ميزانيّة/ملاءمة) | `AiPurposes` + `AiPurposeSuitabilityTest` (١٨) | ✅ |
| ١٠ | المساعدُ يشترط `chat + tools`، والمجهولُ يُغلِق | `AiPurposes::NEEDS` + fail-closed | ✅ |
| ١١ | `reasoning = true` ليست عدمَ صلاحيّة | `test_التفكيرُ_وحدَه_لا_يجعل…` | ✅ |
| ١٢ | لا اسمَ مزوّدٍ ولا نموذجٍ في طبقةِ القرار | `test_طبقةُ_الملاءمةِ_بلا_اسمِ_مزوّدٍ…` | ✅ |
| ١٣ | ثلاثةُ مصائرَ للإخفاق مفصولة | `AiFallbackBehaviourTest` (٢٠) | ✅ |
| ١٤ | ثلاثةَ عشرَ صنفَ إخفاقٍ لكلٍّ محاكاةٌ أو سببُ استثناء | `test_كلُّ_صنفِ_إخفاقٍ_في_الجدولِ…` | ✅ |
| ١٥ | السياسةُ تُعاد قراءتُها عند كلِّ قفزة | `test_الحوكمةُ_تُعاد_عند_كلِّ_قفزةٍ…` | ✅ |
| ١٦ | حذفُ نموذجٍ لا يُعطّل الكتالوجَ ولا يحذف الاعتماد | `AiModelDeletionRecoveryTest` (١٠) | ✅ |
| ١٧ | أعمدةُ الدفترِ مُعدودةٌ — ولا عمودَ محتوًى | `AiObservabilityContractTest` (١٤) | ✅ |
| ١٨ | الكلفةُ موسومةٌ بمصدرِها، والمجهولُ ليس صفراً | `AiCost::SOURCES` + `COST_UNKNOWN_NOT_ZERO` | ✅ |
| ١٩ | شاشةُ التوجيهِ تقول **لماذا** يصلح النموذجُ أو لا | `AiProfileScreenTest` (٣ جدد) | ✅ |
| ٢٠ | مسحٌ عدائيٌّ على الأسطحِ الجديدة | `AskAdversarialPhase5Test` (١٠) | ✅ |
| ٢١ | بوّابةُ إطلاقٍ آليّةٌ للقراءةِ فقط بثلاثِ درجات | `hub:ai-release-check` + `AiReleaseGateTest` (١٦) | ✅ |
| ٢٢ | دليلُ تشغيلٍ واستعادةٍ وقبولٍ وتقريرٌ نهائيّ | `42` · `43` · `44` · `45` | ✅ |

---

## ٣) ما قِيس فعلاً — ولا يُدَّعى غيرُه

| القياس | القيمة |
|---|---|
| ملفّاتُ اختبارٍ جديدةٌ في هذه المرحلة | ٦ |
| اختباراتٌ جديدة | ٩٧ (+٣ في شاشةِ التوجيه) |
| ملفُّ شيفرةٍ جديد | `AiPurposes` · `AiReleaseCheck` · `HubAiReleaseCheck` |
| عيوبٌ حقيقيّةٌ كُشفت وأُصلحت في هذه المرحلة | ٣ (انظر §٤) |
| **توليدٌ مدفوعٌ نُفِّذ** | **صفر** |
| **نداءُ مزوّدٍ حقيقيٌّ نُفِّذ** | **صفر** |

---

## ٤) عيوبٌ كشفتها المرحلةُ نفسُها

### ٤٫١ ذاكرةٌ ساكنةٌ تُجمّد حكمَ الملاءمة

كُتب `AiPurposes::proven()` بذاكرةٍ `static` مفاتيحُها معرّفاتُ النماذج.
**وأسقطها اختبارٌ كُتب بعدها**: في عاملِ طابورٍ أو خادمٍ مقيمٍ تعمّر الذاكرةُ
بالساعات، فنموذجٌ حُسم «بلا دليل» يبقى كذلك بعد أن يُثبِت نفسَه بعشرِ دورات
— **وهو العطلُ الذي وُجد الصنفُ ليمنعَه، مقلوباً**. أُزيلت الذاكرةُ:
`EXISTS` على فهرسٍ لسلسلةٍ أقصاها ثلاثةٌ لا يستحقّ هذا الثمن.

### ٤٫٢ ترتيبُ «آخرِ الدورات» قرعة

`orderByDesc('created_at')->orderByDesc('id')` — و`created_at` بدقّةِ الثانية
و`id` **معرّفٌ عشوائيٌّ لا تزايديّ**. فمحاولاتُ الطلبِ الواحدِ تقع في الثانيةِ
نفسِها ويُرتّبها المحرّكُ كما اتّفق، **فيقرأ المشخّصُ `fallback` فوق
`initial` ويستنتج أنّ الاحتياطَ سبق الأساسيّ**. صار الترتيبُ
`created_at` ← `request_id` ← `attempt` ← `id`.

### ٤٫٣ `WRITE_TOOLS` وعدٌ في خمسِ وثائقَ بلا حارس

الثابتُ مذكورٌ في `23` و`31` و`34` و`40` و`41` — **وغيرُ موجودٍ في الشيفرةِ
سطراً**. فمن يقرأ الوثيقةَ يطمئنّ، ومن يُضيف أداةً كاتبةً غداً لا يصطدم بشيء.
صار `AskTools::WRITE_TOOLS = []` ثابتاً يقرؤه حارسانِ: فحصُ بوّابةِ الإطلاق،
واختبارٌ يقرأ **ما خرج إلى البوّابةِ فعلاً** لا الثابتَ وحدَه.

---

## ٥) تحفّظاتٌ مفتوحةٌ — تُقرَأ ولا تُبتلَع

| # | التحفّظ | لماذا ليس عطلاً | القرار |
|---|---|---|---|
| ت‑١ | **`ASK_FALLBACK_DEPTH`**: سلسلةُ الإنتاجِ نموذجٌ واحدٌ على مزوّدٍ واحد | التهيئةُ سليمةٌ وتعمل — لكن إخفاقَ المزوّدِ إخفاقُ الطلبِ كلِّه | **للمالك**: ضمُّ نموذجٍ من مزوّدٍ ثانٍ يرفع التحفّظ |
| ت‑٢ | **`GATEWAY_PROBED`**: التوليدُ يُثبَت بنداءٍ مدفوعٍ واحد | لم يُنفَّذ من هنا بقرارِ المالك | **للمالك**: اختبارُ توليدٍ من الواجهة |
| ت‑٣ | **`OBSERVED`**: نموذجٌ يمرّ بدليلِ دفترٍ لا بإعلانِ بوّابة | مقبولٌ بالتصميم — الإثباتُ يعلو الإعلانَ الغائب | يُراجَع بتحديثِ الحقائقِ من البوّابة |
| ت‑٤ | نسخةُ البوّابةِ (`pg_dump`) خارجَ `hub:backup` | قاعدتانِ مستقلّتانِ بتقنيتين | موثّقٌ في `43` §٢ — يُجدوَل خارجيّاً |

**ولا واحدٌ من هذه يُعَدُّ نجاحاً بصمت.** كلُّها تظهر في مخرَجِ
`hub:ai-release-check` بسببِها ووجهةِ إصلاحِها.

---

## ٦) ما لم يُنفَّذ بقرارٍ صريح

| البند | القرار |
|---|---|
| توليدٌ مدفوعٌ لأيِّ غرض | **لم يُنفَّذ** — إذنُ المالكِ لم يُطلَب ولم يُعطَ |
| إدخالُ أيِّ اعتمادِ مزوّد | **لم يُنفَّذ** — من المالكِ ومن داخلِ مركزِ الذكاءِ وحدَه |
| تدويرُ `LITELLM_SALT_KEY` أو `APP_KEY` | **لم يُنفَّذ** — قرارٌ محسومٌ بالمنع |
| توسيعُ `WRITE_TOOLS` | **لم يُنفَّذ** — خارجَ نطاقِ كلِّ مرحلةٍ في الخطّة |
| نشرٌ على الإنتاج | **لم يُنفَّذ** — يُطلَب صراحةً |
| أيُّ بدايةٍ ضمنيّةٍ لمرحلةٍ سادسة | **لم تقع** |

---

## ٧) بعد القبول — الخطوةُ التالية للمالك

1. اسحب الدفعةَ على الخادم (`42` §٢).
2. `php artisan hub:ai-release-check` — واقرأ كلَّ تحفّظٍ باسمِه.
3. افتح مركزَ الذكاء ← التوجيه: وسمُ الملاءمةِ يقول الآن **لماذا** يصلح كلُّ
   نموذجٍ أو لا يصلح.
4. لرفعِ ت‑١: اضمم نموذجاً من **مزوّدٍ ثانٍ** إلى سلسلةِ غرضِ المساعد.
5. سؤالٌ حقيقيٌّ واحدٌ في «اسأل Hub» يعود بمصدرٍ واحدٍ على الأقلّ.
