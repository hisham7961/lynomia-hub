{{-- فتاتُ «مركز الأمان» — **رابطٌ لمن يفتحه، ونصٌّ لمن لا يفتحه** (§49 · §25).

     مركزُ الأمان (`security.index`) للمالك وحدَه (`SecurityController::gate`)،
     بينما تسعُ شاشاتٍ من الطور الرابع تُقرأ كذلك لحاملِ راية المراقبة
     (`findingsReadGate`). فرابطٌ ثابتٌ في الفتات كان يعني أنّ أوّلَ رابطٍ في
     شاشةٍ فُتحت للقارئ بابُ رفضٍ (٤٠٣) — طريقٌ مسدود، وهي القاعدةُ التي يحرسها
     `LockoutAndDeadEndsRound7Test` في سائر المستودع.

     والشرطُ مكتوبٌ هنا **مرّةً واحدة** لا تسعَ نسخٍ في تسعِ شاشات: من غيّر حارسَ
     المركز غيّره في موضعٍ واحد. والنصُّ يبقى في الحالتين — فالقارئُ يعرف أين هو
     وإن لم يملك الصعودَ إلى ما فوقه. --}}
@if (hub_is_owner())
    <a href="{{ route('security.index') }}">مركز الأمان</a>
@else
    <span>مركز الأمان</span>
@endif
