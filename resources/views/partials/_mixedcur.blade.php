{{--
    وسمُ اختلاط العملات — نصٌّ واحدٌ لكل الشاشات.

    الرقمُ المجمّع من عملتين بلا تحويلٍ مؤشّرٌ لا رقم. **وفي النظامِ محرّكُ صرفٍ
    منذ v2.528** — فالوسمُ لم يعد ينتهي عند «اقرأها مؤشّراً»، بل يقول **لماذا**
    لم يُحوَّل و**أين** يُصلَح: زوجٌ بلا سعرٍ مسجَّل. والمالكُ يُرسَل إلى شاشةِ
    الأسعارِ بنقرة (v2.542) — فالميزةُ المبنيّةُ التي لا يعرفها أحدٌ ميزةٌ مخفيّة.

        @include('partials._mixedcur', ['currency' => $currency, 'what' => 'الأرقام المجمّعة هنا',
                                        'missing' => $missing ?? []])
--}}
<div class="card" style="border-inline-start:3px solid var(--bad);margin-bottom:12px">
    <div class="sub">⚠ <b>قد تختلط العملات:</b>
        {{ $what ?? 'الأرقام المجمّعة هنا' }} مبنيّةٌ على صفوفٍ تحمل أكثر من عملة، وتُعرض بعملةٍ واحدة
        ({{ $currency ?? setting('app.currency', 'د.ك') }}) دون تحويل.
        اقرأها مؤشّراً عاماً لا رقماً دقيقاً.
        @if (! empty($missing))
            <br>لا سعرَ صرفٍ مسجَّلٌ لـ<b>{{ implode('، ', (array) $missing) }}</b> في تاريخِ هذه المستندات.
        @endif
        @if (hub_is_owner(auth()->user()))
            <br><a href="{{ route('currency.rates') }}">سجّل سعرَ الصرف</a>
            — وعندها تُوحَّد هذه الأرقامُ بعملةِ الأساس، كلُّ مبلغٍ بسعرِ شهرِه.
        @endif
    </div>
</div>
