{{-- (WP-9.4 · spec §7.13) **راياتُ التشغيل — بطاقةُ قراءةٍ فقط.**

     خمسُ راياتٍ تقلب سلوكَ النظام كلَّه، ولكلٍّ شاشةٌ تملكها: هناك التأكيدُ
     وتصعيدُ الهوية والرسالةُ التي تشرح الأثر، وهناك يُكتب التبديلُ في التدقيق.
     فمفتاحُ تبديلٍ **ثانٍ** هنا ليس تسهيلاً بل بابٌ يُرفع منه قفلُ الطوارئ بلا
     شيءٍ من ذلك — ولذلك لا مفتاحَ في هذه البطاقة: حالةٌ ورابطٌ لا غير.

     ‏`partials.cc.th` رأسٌ **قابلٌ للفرز** يبني رابط ?sort=، وهذا الجدولُ خمسةُ
     صفوفٍ بترتيبٍ ثابتٍ لا يقرأ المتحكّمُ له فرزاً — فرابطٌ لا يفعل شيئاً كذبةٌ
     في الواجهة. يبقى المكسبُ الحقيقيّ: scope="col" صريح (كجدول المفاتيح الداخلية). --}}
<div class="card" id="runtimeflags" style="max-width:860px;margin-top:12px">
    <h3>🚦 رايات التشغيل — قراءةٌ فقط</h3>
    <p class="sub">
        هذه الرايات لا تُحرَّر من شاشة الإعدادات: تُبدَّل من شاشتها بزرٍّ واحدٍ
        مع تأكيدِ الهوية والرسالةِ التي تشرح ما سيتوقّف، ويُسجَّل التبديلُ في
        التدقيق. البطاقةُ هنا تقول ما المرفوعُ الآن وتحيل إلى مالكه.
    </p>

    <div class="tblwrap"><table class="tbl">
        <thead><tr>
            <th scope="col">الراية</th>
            <th scope="col">الحالة</th>
            <th scope="col">المفتاح</th>
            <th scope="col">تُبدَّل من</th>
        </tr></thead>
        <tbody>
        @forelse ($runtimeFlags ?? [] as $f)
            <tr>
                <td>{{ $f['label'] }}</td>
                <td><span class="bdg {{ $f['on'] ? 'wn' : 'ok' }}">{{ $f['on'] ? 'مرفوعة الآن' : 'منزَّلة' }}</span></td>
                <td><bdi class="mono ltr">{{ $f['key'] }}</bdi></td>
                <td>
                    @if ($f['url'] !== '')
                        <a class="btn ghost sm" href="{{ $f['url'] }}">↗ افتح شاشتها</a>
                    @else
                        <span class="sub">شاشتُها غيرُ مسجَّلة في هذا التنصيب</span>
                    @endif
                </td>
            </tr>
        @empty
            @include('partials.empty', ['colspan' => 4, 'icon' => '🚦', 'text' => 'لا رايةَ تشغيلٍ معرَّفة'])
        @endforelse
        </tbody>
    </table></div>

    <div class="sub" style="margin-top:10px">
        وحالةُ التجميدَين (التصدير وسكّ الرموز) تُقرأ من <b>وضعية الأمان</b> نفسِها
        (‏<span class="mono ltr">SecurityPosture</span>) لا من قارئٍ ثانٍ — فرايةٌ رُفعت في
        حادثةٍ ونُسيت مرفوعةً تظهر نتيجةً في مركز الأمن لا تختفي بصمت.
    </div>
</div>
