# خارطة التطوير — Lynomia Business Hub

> الرؤية: **نظام تشغيل أعمال متكامل** — إدارة الموظفين والمهام والتطبيقات وإصداراتها،
> مجتمع داخلي للفريق، إشعارات داخلية وخارجية، رسوم بيانية، API، نسخ احتياطي،
> وربط أودو للعرض المالي. هذه الوثيقة تحوّل الرؤية إلى دفعات تنفيذ مرتبة.
>
> آخر تحديث: v2.4.0

---

## ١) خلاصة دراسة الكود

**البنية:** سجل مركزي (`config/hub.php`) يعرّف ٥١ وحدة أعمال، ومتحكم عام واحد يخدمها كلها:
قوائم، نماذج، تحقق، بحث، كانبان، تصدير CSV، سلة محذوفات، إصدارات سجلات.
إضافة وحدة = تعريف في السجل + موديل + هجرة، بلا كتابة شاشات.

**موجود ويعمل:** صلاحيات دقيقة بنطاق مشاريع (`hub_can`/`hub_scope`)، تدقيق كامل،
رادار «ينتهي قريباً» (٢٤ حقل انتهاء)، أتمتة يومية (متكررات + قواعد تنبيه)، كانبان،
وضع ليلي، لوحة أوامر، بحث شامل (أعيد في v2.4.0).

**جاهز في السجل وينتظر التفعيل** (اكتشاف مهم — رؤيتك مبنية جزئياً في البيانات أصلاً):
- `feats` (خطة العمل): فيها **وزن + نسبة تقدم + حالة اختبار** لكل بند ← أساس محرك نسبة الإنجاز
- `apps`: ٤٠+ حقلاً (نسخة، build، حالات مراجعة آبل/جوجل، روابط متاجر…) ← أساس مركز التطبيقات
- `code` (الإصدارات): نسخة، تطبيق، نوع، حالة، تاريخ ← أساس إدارة التحديثات
- `hr`: ملف موظف كامل (راتب، بدلات، إقامة، جواز، رصيد إجازات…) ← أساس صفحة الموظف
- `outbox`: صف رسائل تلجرام/بريد **بلا مُرسِل فعلي** ← ينقصه عامل التسليم فقط

---

## ٢) رؤية المالك → الدفعات

| مطلبك | وضعه الحالي | الدفعة |
|---|---|---|
| إشعارات فريق: داخلي + تلجرام + بريد | داخلي موجود؛ الخارجي يُصفّ ولا يُرسل | **1** |
| نسخ احتياطي على السيرفر | غير موجود | **1** |
| نظام موظفين كامل + صفحة لكل موظف | الوحدات موجودة مبعثرة؛ لا صفحة موحّدة | **2** |
| إدارة مهام | موجودة + كانبان؛ تحتاج «مهامي» وإشعارات إسناد | **2** |
| إدارة تحديثات التطبيقات (ميزات، رقم، حالة رفع…) | وحدة `code` أساسية؛ تحتاج مركز إصدار متكامل | **3** |
| معدل إنجاز لكل تطبيق (نسبة من مهام/اختبارات/خطوات) | حقول الوزن والتقدم موجودة في `feats`؛ لا محرك حساب | **3** |
| مجتمع داخلي للفريق (منشورات، تعليقات) | غير موجود | **4** |
| واجهة أجمل + رسوم بيانية + شكل احترافي | أساس جيد؛ بلا مخططات | **5** |
| ربط ذكي للأقسام ولوحة التحكم | روابط سجلات موجودة؛ اللوحة عامة لا ذكية | **5** |
| API متكامل | غير موجود | **6** |
| ربط أودو محاسبة (عرض فقط، لكل مشروع) | غير موجود | **7** |

---

## ٣) الدفعات بالتفصيل

### الدفعة 1 — القنوات والموثوقية 🔔💾 — ✅ أُنجزت في v2.5.0
1. ✅ **عامل تسليم Outbox**: `hub:outbox` يرسل تلجرام (Bot API) والبريد،
   حالات queued/sent/failed مع `--retry` واستعادة العالق، مجدول كل ٥ دقائق،
   وإعدادا التوكن والقناة في شاشة الإعدادات.
2. ✅ **نسخ احتياطي**: `hub:backup` يصدّر القاعدة كاملة بصيغة `hub:import`
   إلى `storage/app/backups` يومياً 03:30 مع تدوير (آخر ١٤) — مجرَّب استعادةً.
3. ✅ **مركز إشعارات**: صفحة كاملة بفلتر غير المقروء وترقيم، تعمل بلا JS.

### الدفعة 2 — الموظفون والمهام 👥
4. **صفحة الموظف الشاملة**: بياناته + إجازاته + رواتبه + حضوره + عهدته + سجله الوظيفي
   في صفحة واحدة ببطاقات وعدّادات (رصيد إجازات، انتهاء إقامة/جواز، تقييم).
5. **«مهامي»**: عرض موحّد لمهام المستخدم الحالي عبر المشاريع + إشعار داخلي عند إسناد مهمة له.

### الدفعة 3 — مركز التطبيقات ونسب الإنجاز 📱
6. **صفحة التطبيق المطوّرة**: نسخته الحالية، حالة مراجعة المتاجر، إصداراته (`code`) بخط زمني،
   وكل تحديث بمزاياه وتعديلاته ورقمه وحالة رفعه.
7. **محرك نسبة الإنجاز**: نسبة لكل تطبيق/مشروع تُحسب آلياً من بنود `feats` المرتبطة
   (الوزن × التقدم × حالة الاختبار) ومن المهام — تكبر وتصغر مع العمل، بأشرطة تقدم
   في اللوحة وصفحات التطبيقات والمشاريع.

### الدفعة 4 — المجتمع الداخلي 💬
8. وحدة **منشورات الفريق**: منشور + تعليقات + إشارة @موظف (تولّد إشعاراً) + تثبيت إعلانات،
   ضمن الصلاحيات والنطاق نفسه.

### الدفعة 5 — الوجه الاحترافي والربط الذكي 📊
9. **مخططات SVG خفيفة** (بلا مكتبات خارجية): دخل/مصروف شهري، توزيع المهام، تقدم التطبيقات،
   حضور الفريق — في اللوحة والتقارير.
10. **لوحة تحكم ذكية بالأقسام**: كل مجموعة (كيانات/عمل/مالية/HR…) ببطاقتها وعدّاداتها
    وروابطها السريعة، تُخصَّص حسب دور المستخدم.

### الدفعة 6 — API متكامل 🔌
11. **REST API v1**: رموز وصول لكل مستخدم (إنشاء/إبطال من ملفه)، نقاط CRUD لكل وحدة
    بنفس مصفوفة الصلاحيات والنطاق، ونقاط قراءة للتقارير ونسب الإنجاز — موثّقة في `docs/API.md`.

### الدفعة 7 — أودو للعرض 🧾
12. **عميل قراءة فقط** عبر JSON-RPC: بطاقة مالية لكل مشروع/شركة (أرصدة، فواتير، مدفوعات)
    تُجلب من أودو وتُعرض بلا أي إدخال محاسبي — تفعيل بالرابط وبيانات الاتصال من الإعدادات.
    *(يلزمنا حينها: رابط خادم أودو واسم القاعدة ومستخدم قراءة.)*

### مؤجل مدروس
- مصادقة ثنائية (TOTP) · تصدير Excel/PDF معمّم · إشعارات متصفح · مرفقات متعددة لكل سجل

---

## ٤) سجل الدفعات المنجزة

- **v2.4.0**: البحث الشامل (شريط علوي + صفحة نتائج) · حد محاولات الدخول بالـ IP ·
  إصلاح جوهري: تحويل مفتاح العرض إلى عمود القاعدة (`hub_display_col`) — كانت تسميات
  ٥ وحدات (الشركات، المالية، القيود، عروض الأسعار، حركات المخزون) تظهر فارغة وتكسر MySQL ·
  ملف VERSION.

---

## ٥) مبادئ ثابتة في كل دفعة

- **بلا اعتماديات جديدة** ما أمكن — يبقى النظام صالحاً لأي استضافة PHP عادية
- كل ميزة تحترم `hub_can` و`hub_scope` منذ أول سطر
- تغييرات البنية = هجرات جديدة فقط، لا تعديل هجرات قديمة
- عربي RTL أولاً، وكل شيء يعمل كصفحات عادية لو تعطلت الجافاسكربت
- اختبار حي (دخول فعلي وتصفح) قبل كل دفع
