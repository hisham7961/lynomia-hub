# خارطة التطوير — Lynomia Business Hub

> الرؤية: **نظام تشغيل أعمال متكامل (ERP/Operations)** — موظفون وبوابة وصندوق موحد،
> تطبيقات وإصدارات ونسب إنجاز، عملاء وعقود ومشتريات وقانوني، أمان وموافقات وتدقيق،
> إشعارات داخلية وخارجية، رسوم بيانية ولوحة CEO، API وتكاملات وأودو للعرض.
>
> آخر تحديث: v2.6.0

---

## ١) الأساس المعماري (خلاصة دراسة الكود)

سجل مركزي (`config/hub.php`) يعرّف ٥١ وحدة، ومتحكم عام واحد يخدمها كلها.
صلاحيات بمصفوفة + نطاق مشاريع (`hub_can`/`hub_scope`)، تدقيق وإصدارات لكل سجل،
رادار انتهاءات، أتمتة يومية، كل جدول فيه عمود `custom` (بذرة الحقول المخصصة)
وعمود وسوم — وهذا يجعل كثيراً من الرؤية «تفعيلاً» لا «بناء من صفر».

---

## ٢) المنجز

| نسخة | الدفعة | المحتوى |
|---|---|---|
| v2.4.0 | البحث والأمان | البحث الشامل (حي + صفحة نتائج) · حد محاولات الدخول بالـ IP · إصلاح جوهري لأعمدة العرض (٥ وحدات كانت مكسورة) |
| v2.5.0 | القنوات والموثوقية | `hub:outbox` يرسل تلجرام/بريد فعلياً · `hub:backup` يومي بتدوير ومجرَّب استعادةً · مركز إشعارات كامل |
| v2.6.0 | بوابة الموظف والصندوق الموحد | «بوابتي» /me: **الصندوق الموحد** (موافقات بانتظاري، تذاكري، اجتماعاتي القادمة، قرارات أنفّذها) + ملفي (مهامي، إجازاتي، حضوري، عهدتي، سجلي، سياسات يجب قراءتها) · **الملف الشامل ٣٦٠°** لكل موظف لمن يملك HR · **إشعار إسناد** تلقائي (مهام/تذاكر/مزايا/تصاميم) |
| v2.7.0 | التعليقات والمنشن والقناة | تعليقات وردود ومرفقات على **أي سجل** · منشن `@اسم` عربي يُشعر الزميل · تثبيت · **تحويل تعليق لمهمة** يرث المشروع ويُسند لأول مذكور · سجل قراءة 👁 · **قناة الفريق** /feed للإعلانات |
| v2.8.0 | مركز التطبيقات والنسبة الحية | `hub_progress`: خطة (وزن×تقدم) 50٪ + مهام 30٪ + اختبارات 20٪، تتحرك فور الحفظ · **مركز التطبيق** /app/{id}: خط زمني للإصدارات وحالات المتاجر والأعطال وبنود الخطة · بطاقة تقدم التطبيقات في اللوحة |
| v2.9.0 | الموافقات المُلزِمة | وحدات محمية بإعداد `approval.rules` — التعديل/الحذف يتحول **طلباً معلقاً بحمولته** · صفحة حسم بجدول قبل/بعد واعتماد يُنفّذ أو رفض بسبب · سبب التعديل في نماذج التعديل والتدقيق · إصلاح جوهري لقراءة الإعدادات المنقّطة |
| v2.10.0 | لوحة CEO وصحة الشركة | `/ceo` للمالكين: KPI + **صحة الشركة** (٦ أقسام بنتيجة ومعادلة شفافة) + ٦ أشهر مالية + تقدم المشاريع + الفريق اليوم + أعلى المستحقات · دونات SVG «المهام بالحالة» في اللوحتين · تنسيق طباعة عام |
| v2.11.0 | إعدادات بلا مبرمج + 2FA | **TOTP** مطابق RFC 6238 (تفعيل/رمز عند الدخول/تعطيل بالرمز) · شاشة إعدادات مصنفة: شعار ولون يشتق الهوية، سياسات دخول ومدة جلسة وحجم ملفات، **وضع صيانة** برسالة (503 لغير المالكين) |
| v2.12.0 | مركز الأمان | `/admin/security`: مؤشرات (2FA، مقفلون، خاملون، أسرار قديمة، محاولات فاشلة) + جلسات ومحاولات وأدوار وسجل تصدير · **قفل طوارئ** يقطع الجلسات القائمة ويرفض حتى كلمة المرور الصحيحة لغير المالك · تسجيل الدخول الفاشل والتصدير في التدقيق |
| v2.13.0 | الاستيراد وغرفة البيانات | معالج استيراد CSV+**xlsx حقيقي** بمطابقة أعمدة تلقائية و**حل المراجع بالاسم** وتخطٍّ مُسبَّب · **Data Room**: رابط مؤقت بكلمة مرور وانتهاء وعرض-فقط بعلامة مائية وسجل مشاهدات وإلغاء فوري |
| v2.14.0 | مسار المبيعات | عرض السعر: مستند A4 ببنود محسوبة وتوقيعات · إرسال ← قبول/رفض ← **عقد وفاتورة بنقرة** (بلا تكرار، بروابط رجوع) — الفاتورة تدخل التقارير فوراً |
| v2.15.0 | REST API | `/api/v1`: CRUD الوحدات الـ ٥١ بنفس الصلاحيات والنطاق والتحقق والموافقات (409) · مفاتيح Bearer مجزأة تظهر مرة بإبطال فوري · تقارير progress/health · توثيق `docs/API.md` |
| v2.16.0 | الوجه المؤسسي | إعادة تصميم كاملة (نظام تصميم v3 بروح Linear/Stripe) بنفس عقد الأصناف — بلا لمس أي منطق · وضع ليلي معاد المعايرة · كل الألوان من متغيرات الهوية · إصلاح `hidden` للنوافذ · تحقق بصري بلقطات كروميوم |
| v2.17.0 | المشتريات والقانوني | وحدتا **الموردون والمشتريات** في السجل (كل القدرات تلقائياً) · مسار: اعتماد ← إرسال ← استلام ← **فاتورة مورد للمالية** · أمر شراء A4 · **المركز القانوني**: يستحق التجديد بأيام ملونة + توزيع أنواع + التزامات |
| v2.18.0 | ربط أودو الذكي | إعدادات بفراغات (المفتاح مقنّع) + اختبار اتصال · بطاقة أودو على المشاريع/الشركات/العملاء: بحث بالاسم ← ربط ← **مبيعات وفواتير وغير محصل حيّة** (قراءة فقط، كاش ١٠ دق) · عميل JSON-RPC بلا اعتماديات مُختبَر ضد محاكٍ للبروتوكول |
| v2.19.0 | باني الحقول المخصصة | شاشة مالك: حقول جديدة لأي وحدة (نص/رقم/تاريخ/قائمة/تأشير/مرجع، إلزامي) **بلا كود ولا هجرات** — تظهر فوراً في النماذج والتحقق والعرض، تُخزن في `custom`، وتسري على الاستيراد والـ API والموافقات |
| v2.20.0 | دعم بمؤقتات SLA | قواعد من الإعدادات (أولوية:استجابة:حل) · **لوحة /support**: طابور بالأعجل بمؤقتين ملونين + متوسطا أول رد والحل ونسبة الالتزام (٣٠ يوماً) · أول تعليق = الرد الأول، والإغلاق يختم زمن الحل · تذاكر المتاجر عبر API بـ extId |
| v2.21.0 | لوحة الأداء OKR/KPI | `/performance` (مالك/monitor): مؤشرات شركة محسوبة (إيراد، ربحية، **نمو شهري٪**، عملاء جدد/نشطون، إكمال مشاريع، أعطال) · أهداف بأربعة مستويات بأشرطة · **جدول موظفين ٣٠ يوماً**: منجز، التزام٪، متأخر، تذاكر وزمن حلها، تقييم المدير |
| v2.22.0 | إدارة التغييرات | وحدة «التغييرات التقنية» (٥٥): هدف/مخاطرة/**خطة تنفيذ وخطة تراجع**/نافذة/منفذ/نتيجة · دورة اعتماد للكانبان · تظهر تلقائياً في صفحات السيرفر/التطبيق/الدومين وتعمل مع الموافقات المُلزِمة والـ API |
| v2.23.0 | باني مسارات العمل 🏁 | مسارات بلا كود: حدث (إنشاء/تعديل/تحول حالة) + شرط + إجراءات (إشعار/تلجرام/بريد/مهمة/تعيين حقل) بقوالب `{حقل}` · يعمل على الويب والـ API والكانبان · عدادات وتفعيل/تعطيل — **بهذا اكتملت الرؤية كاملة** |
| v2.24.0 | الجودة المؤسسية | تعريب كامل للرسائل بأسماء الحقول العربية · صفحات أخطاء أنيقة · ترويسات أمنية · حماية إرسال مزدوج · «حفظ وإضافة آخر» · API صارم JSON |
| v2.25.0 | جولة تدقيق مستقلة | تشفير أسرار التكاملات (`enc:`) · بوابة ملفات خاصة `/files` مع أمر ترحيل · إصلاح عرض المفاتيح ≠ الأعمدة مركزياً · `hub_mod` آمن للسجلات النظامية |
| v2.26.0 | مركز التشغيل والأخطاء | `/admin/ops`: قاعدة/قرص/ذاكرة/حمل/نبضات مجدولات/طوابير/آخر نسخة · `/admin/errors`: تجميع PHP/API/متصفح/بطيء بالتكرار و**Request ID** وحالات معالجة · `/healthz` عام لمراقبات Uptime · التقاط JS بـ sendBeacon |
| v2.27.0 | مركز Webhooks 🪝 | اشتراكات لأي نظام خارجي (n8n): أحداث `وحدة.حدث` بأنماط `*` · POST موقّع **HMAC-SHA256** بترويسات حدث ومعرف منع تكرار · إعادات متباعدة وإيقاف مؤقت بعد ١٠ إخفاقات · سجل محاولات واختبار وإعادة يدوية |
| v2.28.0 | تصليب API 🔐 | **Idempotency-Key** يعيد الرد المخزن بلا إنشاء ثانٍ · **نطاقات لكل مفتاح** `وحدة:عمليات` تضييقاً فقط · قائمة **IP/CIDR** · **تدوير** فوري للمفاتيح · اختيار حقول `?fields=` |
| v2.29.0 | جودة البيانات 🧹 | كشف مكررات العملاء (اسم مطبع/بريد/هاتف) · نواقص: بلا شركة/مدير/مسؤول/انتهاء/طرف · بريد غير صالح وركود ٩٠ يوماً · **دمج آمن** يعيد توجيه الإشارات من سجل الوحدات والتعليقات ويملأ الفراغات |
| v2.30.0 | صندوق الوثائق 📥 | ارفع أولاً وصنّف لاحقاً: تبويبا وارد/مصنف · تصنيف بوحدة و**سجل يُحل بالاسم** وشركة وجهة ونوع وتاريخين · شارة انتهاء ملونة · الرفع للجميع والحذف للمالك والملفات خلف بوابة `/files` |
| v2.31.0 | الاختبارات الآلية 🧪 | **٣٤ اختباراً / ١٢٥ تحققاً** بثانية واحدة: دخول وصلاحيات وتدقيق وAPI (نطاقات/Idempotency/IP/حقول) وموافقات وWebhooks ومراكز التشغيل/الأخطاء/الجودة/الوثائق وTOTP بمتجه RFC — قاعدة ذاكرة تُبنى من الهجرات لكل اختبار |
| v2.32.0 | تدقيق مُسلسل 🔏 | كل سجل تدقيق يحمل **تجزئة تشابكية** (sha256 لسابقه + بصمته) برأس يتقدم ذرياً · `hub:audit-verify` يكشف التعديل والحذف والتفرع بدقة السجل · **عرض السر يُسجل** باسم المطلع وجهازه · ٤ اختبارات آلية للسلسلة |
| v2.33.0 | PWA 📲 | تثبيت على الجوال ببيان وأيقونة من هوية النظام · عامل خدمة: أصول من الكاش، صفحات شبكةً أولاً بمهلة ٦ ثوانٍ ثم آخر نسخة ثم صفحة «بلا اتصال» (بلا كاش للأسرار/API/الإدارة) · **مسودات محلية** لنماذج الوحدات كلها بشريط استرجاع/تجاهل |
| v2.34.0 | وضع تجريبي 🎭 | `hub:demo`: ٣٢ سجلاً وهمياً واقعياً موسوماً عبر ١٠ جداول · شريط علوي مخطط بزرَّي **تصفير** و**إنهاء** (للمالك) · المسح بالوسم لا يلمس البيانات الحقيقية · بطاقة تفعيل في مركز التشغيل |
| v2.35.0 | صلاحيات الحقول 🔬 | لكل دور: أي حقل «قراءة فقط» أو «مخفي» من شاشة الدور · يسري على الجداول والصفحات والنماذج والتصدير والـ API · الكتابة المحقونة تُتجاهل · الإلزامي المخفي لا يعطل الحفظ · المالك محصّن |
| v2.36.0 | محوّل الشركات 🏢 | اختر الشركة النشطة من الشريط العلوي فتُصفّى قوائم كل الوحدات عليها (عمود الشركة يُكتشف من السجل) · «كل الشركات» يعيد العرض المجمع · يتطلب رؤية الشركات ويظهر فقط مع أكثر من شركة |

---

## ٣) دفعات الرؤية الأولى (أُنجزت كلها ✅)

### د4 — مركز التطبيقات ونسبة الإنجاز 📱
صفحة تطبيق مطوّرة: النسخة، حالات مراجعة المتاجر، خط زمني للإصدارات (`code`)
وكل تحديث بمزاياه وتعديلاته ورقمه وحالة رفعه. **محرك نسبة الإنجاز**: لكل تطبيق/مشروع
من بنود `feats` (وزن × تقدم × اختبار) والمهام — تتحرك مع العمل، بأشرطة في كل مكان.

### د5 — الموافقات المُلزِمة وتعميق التدقيق 🔏
عمليات محددة (يضبطها المالك) **لا تنفذ إلا بموافقة**: طلب → سلسلة اعتماد → تنفيذ.
تعميق سجل التدقيق: الجهاز، الـ IP، القسم، المشروع، **سبب التعديل**، قبل/بعد (موجودة).

### د6 — الرسوم البيانية ولوحة CEO 📊
مخططات SVG بلا مكتبات: مالية شهرية، مهام، تقدم تطبيقات، حضور.
**لوحة CEO**: صورة الشركة في شاشة — إيرادات/مصروفات، مشاريع، أعطال، موظفون.
**تقرير صحة الشركة**: نسبة لكل قسم (مالية، مشاريع، أمن، HR، امتثال، بنية تحتية).
لوحة تحكم ذكية بالأقسام حسب دور المستخدم.

### د7 — الإعدادات الموسعة بدون مبرمج + المصادقة الثنائية ⚙️
شعار وFavicon وألوان، منطقة زمنية وتنسيق تاريخ وعملة، سياسات كلمات المرور،
مدة الجلسة، أحجام/أنواع الملفات، صفحة صيانة، شروط/خصوصية داخلية،
**تحكم بلا كود**: مفردات الحالات والأولويات والأنواع لكل وحدة. + **2FA (TOTP)**.

### د8 — مركز الأمان 🛡️
شاشة واحدة: الجلسات النشطة، محاولات الدخول الفاشلة، المستخدمون الخاملون،
أسرار لم تُغيَّر منذ مدة، مفاتيح منتهية، سجل التحميل والتصدير، مراجعة صلاحيات،
IP Allowlist، **قفل طوارئ** يعلّق كل الدخول عدا المالك.

### د9 — الاستيراد والتصدير وData Room 📦
استيراد Excel/CSV لأي وحدة بمطابقة أعمدة، تصدير Excel، تقارير PDF،
نسخة كاملة لمشروع/شركة ونقلها. **Data Room**: مشاركة مستندات حساسة برابط مؤقت
بكلمة مرور وانتهاء ومنع تنزيل وعلامة مائية وسجل مشاهدة وإلغاء فوري.

### د10 — CRM والمبيعات والدعم 🤝
عملاء بقيمة إجمالية ومصدر ومسؤول حساب وفرص بيعية. مسار كامل:
عرض سعر → PDF → إرسال → قبول/رفض → **عقد** → فاتورة، توقيع إلكتروني، تجديد وتنبيه انتهاء.
تذاكر بـ SLA ومحادثات وربط تذاكر المتاجر/التطبيقات بالمشروع (حقل `extId` جاهز).

### د11 — المشتريات والقانوني ⚖️
موردون: طلب شراء → عروض → مقارنة → أمر شراء → استلام → فاتورة مورد → دفع → تقييم.
قسم Legal: رخص، تسجيلات حكومية، علامات تجارية، تأمين، قضايا، NDA — بتنبيهات تجديد.

### د12 — API والتكاملات وأودو 🔌
REST API v1 برموز وصول لكل مستخدم وبنفس الصلاحيات والنطاق.
إطار تكاملات قابل للإضافة: n8n، GitHub/GitLab، Firebase، متاجر آبل/جوجل، Cloudflare،
Stripe، Drive/Gmail. **أودو**: عميل قراءة فقط JSON-RPC — بطاقة مالية لكل مشروع/شركة.

### مستقبلي مدروس
إدارة التغييرات (Change Management للبنية التقنية) · سجل قرارات مُثرى (بدائل، أثر، مراجعة) ·
Custom Fields Builder (فوق عمود `custom` الجاهز) · Workflow Builder بلا كود ·
إدارة فروع تحت الشركات · OKRs/KPIs بمستوييها · اجتماعات أعمق (محاضر ومتابعة قرارات — البذرة موجودة).

---

## ٤) المرحلة الثالثة — التشغيل والصلابة والتكامل (الرؤية الجديدة، مرتبة)

### ت1 — مركز التشغيل ومركز الأخطاء ✅ (v2.26.0)
مراقبة القاعدة والقرص والذاكرة والحمل ونبضات المجدولات والطوابير وآخر نسخة احتياطية،
وتجميع كل الأخطاء (PHP/API/متصفح/بطيء) بالتكرار وRequest ID وحالات معالجة، و`/healthz` عام.

### ت2 — مركز Webhooks ✅ (v2.27.0)
أحداث قابلة للاشتراك (إنشاء/تعديل/تحول حالة لأي وحدة) → POST موقّع بسر HMAC،
سجل محاولات بالرد والزمن، إعادة تلقائية بتباعد، إعادة إرسال يدوي، اختبار فوري،
تعطيل مؤقت عند تكرار الفشل، ومنع التكرار بمعرف حدث. التوثيق في docs/API.md.

### ت3 — تصليب API ✅ (v2.28.0)
Idempotency-Key (نفس الطلب مرتين = نتيجة واحدة)، نطاقات لكل مفتاح (قراءة/كتابة/وحدات محددة)،
انتهاء صلاحية ودوران المفاتيح، قائمة IP مسموحة (مفردة وCIDR)، اختيار حقول `?fields=`، حدود ترقيم قصوى.

### ت4 — حزمة الاختبارات الآلية ✅ (v2.31.0)
phpunit بقاعدة ذاكرة: دخول وأقفال، صلاحيات ثلاثة أدوار × عمليات، تدقيق التعديل،
API كاملاً (مصادقة/نطاقات/Idempotency/حقول/IP/نطاق مشاريع)، الموافقات المُلزِمة ويب+API،
Webhooks (تصفية وأسرار)، مراكز التشغيل والأخطاء والجودة وصندوق الوثائق، وTOTP بمتجه RFC —
**أمر واحد: `vendor/bin/phpunit`** قبل كل نشر.

### ت5 — مركز جودة البيانات ✅ (v2.29.0)
عملاء مكررون (اسم/بريد/هاتف)، سجلات بلا شركة، موظفون بلا مدير، مشاريع بلا مسؤول،
دومينات بلا انتهاء، فواتير بلا طرف، بريد غير صالح، عملاء راكدون — مع **دمج آمن**
يعيد توجيه كل الإشارات ويملأ الفراغات ويحذف ناعماً (قابل للتراجع).

### ت6 — صندوق الوثائق الوارد ✅ (v2.30.0)
ارفع أولاً وصنّف لاحقاً: وحدة/سجل بالاسم/شركة/جهة/نوع/تاريخ/انتهاء — فلا تضيع ورقة
لأن مكانها غير جاهز. الرفع للجميع، الحذف للمالك، والملفات خلف بوابة الدخول.

### ت7 — تدقيق غير قابل للعبث ✅ (v2.32.0)
سلسلة تجزئة تشابكية لكل سجل تدقيق + أمر `hub:audit-verify` يكشف التعديل والحذف والتفرع،
وتسجيل **عرض الأسرار** (إلى جانب التصدير المسجل سابقاً). بصراحة: مدير قاعدة يستطيع
الكتابة المباشرة، لكن السلسلة **تكشف** أي عبث — والأرشفة خارج الخادم عبر النسخ الاحتياطي القائم.
تنبيه الشذوذ السلوكي يبقى مستقبلياً.

### ت8 — PWA ✅ (v2.33.0)
تثبيت على الجوال (بيان + أيقونة تتبع هوية النظام)، وضع اتصال ضعيف (شبكة أولاً بمهلة ← آخر
نسخة ← صفحة بلا اتصال، مع استثناء API والأسرار والإدارة من الكاش)، **مسودات نماذج محلية**
تُسترجع بشريط عند العودة — **بصراحة**: إشعارات Push تتطلب خدمة خارجية (VAPID) وتبقى خياراً مستقبلياً.

### ت9 — وضع تجريبي/Sandbox ✅ (v2.34.0)
بيانات وهمية واقعية موسومة 🎭 (شركة وعملاء ومشاريع ومهام وتذاكر وفواتير ودومينات وموظفون
وعرض سعر) للتدريب وتجربة الاستيراد والمسارات والـ API، شريط تحذير علوي، وتصفير/إنهاء بزر —
المسح دقيق بالوسم فلا يلمس بياناتك الحقيقية.

### ت10 — صلاحيات على مستوى الحقل ✅ (v2.35.0)
لكل دور: أي حقل في أي وحدة يصير «قراءة فقط» أو «مخفياً تماماً» (الراتب مثلاً) — يُضبط من
شاشة الدور، ويسري على الجداول والصفحات والنماذج والتصدير والـ API والكتابة (حتى المحقونة).
المالك يرى كل شيء دائماً، والحقل الإلزامي المخفي لا يعطل الحفظ.

### ت11 — تعدد الشركات 🏢 (الطور الأول ✅ v2.36.0 — والعزل الصارم طور لاحق)
**أُنجز**: محوّل شركة بالشريط العلوي يصفّي قوائم كل الوحدات على الشركة النشطة (يُكتشف عمود
الشركة من سجل الوحدات تلقائياً)، صفحة الشركة تجمع سجلاتها عبر الوحدات (السجلات المرتبطة)،
والتقارير تبقى مجمعة. **طور لاحق موثق بصراحة**: عزل بيانات صارم بالصلاحيات، عملة وضريبة
وترقيم لكل شركة، فروع/أقسام/مراكز تكلفة كتسلسل، ومستخدمون مقيدون بشركات محددة —
تغيير معماري عميق يُخطط له عند الحاجة الفعلية.

---

## ٦) المرحلة الرابعة — بقية رؤيتك الموسّعة (مرتبة، وما أُنجز موسوم)

**أُنجز في v2.40–v2.42:**
✅ تكاليف المشاريع الفعلية والربحية (أربع دلاء + تكلفة التأخير) · ✅ صحة كل مشروع بدرجة موزونة ·
✅ مركز التشغيل اليومي · ✅ إدارة الحوادث + Postmortem · ✅ سجل النشر والإصدارات ·
✅ اختبار استعادة النسخ · ✅ المهارات والفجوات · ✅ السياسات والإقرارات · ✅ الطلبات الواردة ·
✅ سجل الاعتماديات · ✅ المنافسون · ✅ العلامات التجارية · ✅ مركز الإعلام · ✅ الملكية الفكرية ·
✅ الأحداث · ✅ الباقات والتسعير · ✅ سجل القرارات والاجتماعات (موجودان ويُثريان)

**التالي بترتيب القيمة:**

### ✅ ر1 — إدارة القدرات والموارد 📊 (v2.43.0)
صفحة `/capacity`: المتاح مقابل المحجوز، الحمل، الاستغلال، الاختناق المتوقع.

### ✅ ر2 — المرفقات الشاملة وخريطة الأثر 🔗 (v2.43.0 + v2.45.0)
قسم مرفقات على كل سجل من كل وحدة، وتنزيل مصادَق عليه مُسجَّل · وخريطة `/impact` من سجل الاعتماديات.

### ✅ ر3 — التتبع الكامل من الفكرة إلى النشر 🧵 (v2.46.0)
صفحة `/trace`: الخيط من أي سجل صعوداً ونزولاً، مع إنذارَي انقطاع الخيط.

### ✅ ر4 — لوحة جودة البرمجيات 🧪 (v2.44.0)
صفحة `/app-quality`: الأخطاء المفتوحة والحرجة، نسبة نجاح الاختبارات، زمن الحل، الأعطال، معدل التراجع.

### ✅ ر5 — التخصيص وشاشة البداية الديناميكية 🎛️ (v2.48.0 + v2.49.0)
`/personalize`: البداية والقائمة والبطاقات · وفي كل جدول: العروض المحفوظة والأعمدة الشخصية.

### ر6 — Sandbox لمسارات العمل 🧪
تشغيل تجريبي قبل التفعيل: يعرض السجلات التي كان سيطابقها والإجراءات التي كان سينفذها،
بلا إرسال حقيقي، مع كشف الحلقات وتقدير عدد التشغيلات.

### ✅ ر7 — تحليل تكلفة الخدمات والتسعير 🧮 (v2.50.0)
`/service-costs`: الهامش الحقيقي لكل خدمة وباقة، الأسوأ أولاً، مع عمر مراجعة السعر.


### ✅ ر8 — تطوير المجتمع والمراسلة الداخلية 💬 (v2.51.0)
رسائل مباشرة خاصة بإيصالات قراءة وشارة غير مقروء · تفاعلات على المنشورات والتعليقات.


### ر9 — العملاء VIP ورحلة العميل والامتثال 🏅
تصنيف العملاء (Gold/Enterprise/Strategic) بتغيير SLA تبعاً للفئة · رحلة العميل من أول زيارة ·
سجل امتثال يجمع السياسات والعقود والتراخيص والتدريب والموافقات في مؤشر واحد.

### ر10 — مركز التوصيات ومنصة الودجات 🤖
النظام يقترح إجراءً لا رقماً فقط · وباني KPI بالمعادلات · ومنصة ودجات تُضاف لأي لوحة.

---

## ٥) مبادئ ثابتة

- بلا اعتماديات جديدة ما أمكن — يعمل على أي استضافة PHP عادية
- كل ميزة تحترم `hub_can` و`hub_scope` منذ أول سطر
- تغييرات البنية = هجرات جديدة فقط
- عربي RTL أولاً، ويعمل بلا JavaScript
- اختبار حي (دخول وتصفح فعلي) قبل كل دفع
