# خارطة التحديثات — Lynomia Business Hub

> دراسة الكود الحالي + خطة التحديثات القادمة مرتبة بالأولوية.
> آخر تحديث: v2.4.0

---

## ١) خلاصة دراسة الكود

**البنية:** النظام مبني على فكرة ذكية واحدة: **سجل مركزي** (`config/hub.php` — ٦٨٨٠ سطراً) يعرّف ٥١ وحدة أعمال بجداولها وحقولها وأنواعها ومراجعها، و**متحكم عام واحد** (`ModuleController`) يخدمها كلها: قوائم، نماذج، تحقق، بحث، كانبان، تصدير CSV، سلة محذوفات، إصدارات. إضافة وحدة جديدة = تعريفها في السجل + موديل + هجرة، بلا كتابة شاشات.

**نقاط القوة الموجودة فعلاً:**
- صلاحيات دقيقة: مصفوفة (عرض/إضافة/تعديل/حذف) لكل وحدة لكل دور + أعلام إدارية + **نطاق مشاريع** يحصر الحساب المحدود بمشاريعه في كل استعلام (`hub_scope`)
- أمان دخول: قفل الحساب بعد محاولات فاشلة، تقييد IP اختياري، انتهاء صلاحية حساب، سجل جلسات، وسياسة كلمات مرور من الإعدادات
- تدقيق كامل + إصدارات لكل سجل مع استعادة
- رادار «ينتهي قريباً»: مسح ٢٤ حقل انتهاء عبر الوحدات (دومينات، عقود، اشتراكات…)
- أتمتة يومية (`hub:automation`): توليد المصروفات المتكررة + قواعد تنبيه بمشغّلات مرنة + صف رسائل خارجية (outbox)
- واجهة htmx بلا إطارات ثقيلة: نوافذ منبثقة، بحث حي، كانبان سحب وإفلات، وضع ليلي، لوحة أوامر Ctrl+K

**الفجوات المكتشفة:** البحث الشامل كان منزوعاً (قالبه اليتيم كان يشير لمسار غير موجود) — أُعيد في v2.4.0. ملف `VERSION` كان مفقوداً فتظهر النسخة 1.0.0. الدخول كان بلا حدّ لمحاولات التخمين الموزَّع عبر حسابات متعددة. رسائل outbox تُصفّ ولا يوجد مُسلِّم فعلي (بريد/تلجرام). لا مصادقة ثنائية. لا تقارير مالية متقدمة.

---

## ٢) ما أُنجز في v2.4.0 (هذه النسخة)

| # | التحديث | الحالة |
|---|---|---|
| 1 | **البحث الشامل**: صندوق في الشريط العلوي بنتائج حية منسدلة + صفحة نتائج كاملة مجمّعة بالوحدات مع عدّادات وشارات حالة — يحترم الصلاحيات ونطاق المشاريع | ✅ |
| 2 | اختصارات: `/` يركّز البحث، Enter لكل النتائج، Esc يغلق | ✅ |
| 3 | **حد محاولات الدخول بالعنوان**: ١٠ بالدقيقة لكل IP (يكمل قفل الحساب الموجود) | ✅ |
| 4 | ملف `VERSION` — النسخة تظهر صحيحة | ✅ |
| 5 | هذه الوثيقة — خارطة عمل دائمة | ✅ |

---

## ٣) الدفعة القادمة المقترحة — بالأولوية

### أولوية عالية (قيمة كبيرة، جهد معقول)

1. **تسليم فعلي لرسائل Outbox** — القواعد تصفّ رسائل بريد/تلجرام ولا أحد يرسلها. أمر `hub:outbox` يرسل البريد عبر SMTP من الإعدادات وتلجرام عبر Bot API، مع حالات (queued/sent/failed) وإعادة محاولة. *بدونه التنبيهات الخارجية حبر على ورق.*
2. **المصادقة الثنائية (TOTP)** — تفعيل اختياري من الملف الشخصي، إلزامي للمالك عبر إعداد. تعمل مع أي تطبيق مصادقة، بلا خدمات خارجية.
3. **نسخ احتياطي تلقائي** — أمر `hub:backup` يصدّر القاعدة كاملة JSON (النظام يستورد هذه الصيغة أصلاً عبر `hub:import`) بجدولة يومية وتدوير النسخ. *أثمن تأمين ممكن للبيانات.*

### أولوية متوسطة

4. **التقارير المالية المتقدمة** — ميزان مراجعة من القيود، أعمار الديون (المستحقات حسب التأخر)، تقرير حسب مركز التكلفة والمشروع. البنية جاهزة (قيود + دليل حسابات + مراكز تكلفة).
5. **تصدير Excel/PDF** — الطباعة الأنيقة موجودة للتقرير المالي؛ تعميمها على كل قائمة وصفحة عرض (نافذة طباعة منسّقة = PDF عملياً بلا مكتبات).
6. **مرفقات متعددة لكل سجل** — جدول `attachments` موجود أصلاً بلا شاشة؛ ربطه بصفحة العرض: رفع عدة ملفات، معاينة، حذف.

### تحسينات لاحقة

7. لوحة تحكم قابلة للتخصيص (اختيار البطاقات والوحدات المثبتة)
8. مخططات في التقارير (خطوط SVG خفيفة بلا مكتبات)
9. بحث داخل لوحة الأوامر Ctrl+K في السجلات (لا الوحدات فقط) — عبر مسار البحث الجديد
10. إشعارات متصفح (Web Push) للتنبيهات الحرجة

---

## ٤) مبادئ نلتزم بها في كل تحديث

- **بلا اعتماديات جديدة** ما أمكن — يبقى النظام قابلاً للرفع على أي استضافة PHP عادية
- كل ميزة تحترم `hub_can` (الصلاحيات) و`hub_scope` (نطاق المشاريع) منذ أول سطر
- كل تغيير بنية قاعدة = هجرة جديدة قابلة للتراجع، لا تعديل هجرات قديمة
- الواجهة عربية RTL أولاً، وتعمل بلا JavaScript كصفحات عادية (htmx تحسين لا شرط)
